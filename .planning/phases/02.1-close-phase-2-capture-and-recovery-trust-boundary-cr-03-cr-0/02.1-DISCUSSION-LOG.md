# Phase 02.1: Close Phase 2 capture and recovery trust boundary — CR-03 CR-04 CR-12 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-19
**Phase:** 02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-04-cr-12
**Areas discussed:** Trusted archive boundary, Durable-prefix reconstruction, Atomic publication and races, Exit evidence and claims

---

## Trusted archive boundary

| Question | Option | Description | Selected |
|---|---|---|---|
| Replay admission | Opaque `VerifiedArchive` capability | Only the pinned verifier can construct it; raw URLs cannot enter replay. | ✓ |
| Replay admission | Raw URL through shared verifier | Replay accepts a URL but invokes the shared verifier itself. | |
| Replay admission | Capability plus full replay revalidation | Recovery creates a capability and replay repeats all validation. | |
| Capability lifetime | Generation-bound plus read-time hash checks | Eagerly validate, bind to an immutable generation/inventory, then bounded-read and recheck member hashes when consumed. | ✓ |
| Capability lifetime | Fully materialized snapshot | Hold all verified packet/event bytes in memory. | |
| Capability lifetime | Short-lived capability with full revalidation | Retain a location and repeat full verification before each replay. | |
| Validation ownership | Single injected verifier | One fail-closed verifier owns schema and cross-record validation and alone constructs the trusted type. | ✓ |
| Validation ownership | Split policy | Recovery and replay validate different portions. | |
| Validation ownership | Duplicate complete policy | Both layers independently repeat complete validation. | |
| Salvage boundary | Strict root validation with verified-tail salvage | Reject a contract-invalid root; recover only a verified prefix from a contract-valid open source with terminal tail damage. | ✓ |
| Salvage boundary | Best-effort invalid-root salvage | Interpret enough of a schema-invalid root to attempt recovery. | |
| Salvage boundary | Reject every incomplete archive | Disable recovered-prefix behavior entirely. | |

**User's choice:** The user explicitly selected the opaque `VerifiedArchive` capability, then directed the workflow to use the recommended choice for every remaining discussion question.

**Notes:** The recommendation preserves a single trust authority while preventing a stale validation verdict from surviving member substitution.

---

## Durable-prefix reconstruction

| Question | Option | Description | Selected |
|---|---|---|---|
| Repair strategy | Physical-journal reconstruction | Scan the actual fsynced journal and rebuild the last verified contiguous prefix without redesigning the writer. | ✓ |
| Repair strategy | Capture-writer generation overhaul | Replace journal/projection updates with writer generations. | |
| Repair strategy | Hybrid overhaul | Reconstruct old archives and redesign the writer simultaneously. | |
| Projection source | Exact referenced bytes under journal authority | Derive frames/events and all sequences/digests from journal entries plus exact packet/event bytes. | ✓ |
| Projection source | Merge journal and stale arrays | Reconcile the physical journal with the manifest projections. | |
| Projection source | Prefer self-consistent manifest | Retain manifest arrays when their internal digests agree. | |
| Corruption classification | Terminal-tail recovery; interior fails closed | Recover only terminal incomplete/corrupt data; reject gaps, reorder, duplicates, later-valid records, and contradictions. | ✓ |
| Corruption classification | First-fault truncation | Treat everything following the first fault as a recoverable suffix. | |
| Corruption classification | Skip and continue | Drop invalid records and resume at later records. | |
| Change scope | Preserve contracts and minimize writer change | Keep CON-001/002 and record-first ordering; add only essential internal recovery metadata with compatibility coverage. | ✓ |
| Change scope | Version external capture contracts | Publish a new CON-001/002 version as part of this repair. | |
| Change scope | Replace writer persistence | Move the complete writer to a new persistence architecture now. | |

**User's choice:** Recommended defaults authorized for the full area.

**Notes:** This choice fixes already-created Phase 2 archives and preserves the canonical journal-first durability order. It avoids hiding CR-04 by weakening durability or broadening the phase into a contract migration.

---

## Atomic publication and races

| Question | Option | Description | Selected |
|---|---|---|---|
| Commit protocol | Immutable generation plus active pointer | Fsync and verify one content-addressed generation, then atomically replace the active pointer last. | ✓ |
| Commit protocol | Lock plus sequential renames | Serialize callers but publish archive/quarantine separately. | |
| Commit protocol | Unique result per attempt | Publish a new recovered sibling for every invocation. | |
| Bundle boundary | One atomically committed recovery bundle | Inventory-bind recovered archive, invalid suffix, quarantine metadata, and source/prefix identity in one generation. | ✓ |
| Bundle boundary | Separate publications | Keep archive and quarantine as independent destinations. | |
| Bundle boundary | Archive only | Rely on the immutable source instead of publishing explicit quarantine evidence. | |
| Race result | Reuse identical winner; conflict on different winner | Return the same verified generation idempotently; preserve and report a different valid result without overwrite. | ✓ |
| Race result | Every loser conflicts | Surface a conflict even when the winner is byte-identical. | |
| Race result | Last writer wins | Replace the pointer with the latest publisher's result. | |
| Cleanup ownership | Owned staging only | Remove only paths uniquely created by the failed invocation. | ✓ |
| Cleanup ownership | Delete nonpublished destination | Remove a destination whenever the current invocation did not publish it. | |
| Cleanup ownership | Global recovery cleanup | Sweep the recovery directory after any failure. | |

**User's choice:** Recommended defaults authorized for the full area.

**Notes:** A complete but unreferenced generation is safe. It is preferable to deleting data whose ownership cannot be proven, especially during a two-publisher race.

---

## Exit evidence and claims

| Question | Option | Description | Selected |
|---|---|---|---|
| CR-03 proof | Production-path adversarial contract suite | Rehash contract-invalid archives and prove the production verifier rejects them before replay authority exists. | ✓ |
| CR-03 proof | Schema unit tests only | Test the validator independently of recovery/replay. | |
| CR-03 proof | Snapshot comparisons only | Infer trust from replay-output equality. | |
| CR-04 proof | Production recovery over complete crash matrix | Run every existing writer fault boundary through `CaptureRecovery` and capability-only replay and compare with physical durability. | ✓ |
| CR-04 proof | Test scanner remains pass path | Continue using the separate `scanDurablePrefix` implementation. | |
| CR-04 proof | One regression only | Add only the stale-manifest case. | |
| CR-12 proof | Full publication fault matrix and race stress | Inject every write/fsync/rename/pointer boundary and repeated two-publisher races. | ✓ |
| CR-12 proof | Single rename failure | Cover only one process-death point. | |
| CR-12 proof | Happy-path idempotency | Test repeat recovery without crashes or concurrency. | |
| Completion claim | Close only CR-03/04/12 | Verify these repairs now while leaving Phase 2, `GATE-001`, and v1.0 open. | ✓ |
| Completion claim | Mark Phase 2 and gate complete | Treat the three repairs as sufficient for overall completion. | |
| Completion claim | Defer repair verification | Implement now but verify only in the next milestone. | |

**User's choice:** Recommended defaults authorized for the full area.

**Notes:** This directly reflects the earlier milestone decision: foundational repairs remain part of v1.0 and are verified before expensive physical evidence, but automated repair evidence cannot impersonate the still-pending device/human gate.

---

## the agent's Discretion

- Exact Swift protocol/type names and dependency-injection layout.
- Internal versioned generation, inventory, and pointer filenames/encoding.
- Deterministic fault-harness implementation and race repetition count, provided every named durability boundary is covered.

## Deferred Ideas

- All Phase 2 review blockers other than `CR-03`, `CR-04`, and `CR-12` remain separate closure work.
- Genuine `GATE-001` physical/human evidence and milestone reaudit remain in v1.0 after those invalidating blockers are repaired.
- External contract evolution or a wholesale capture-writer persistence redesign requires separate compatibility and ADR work.
