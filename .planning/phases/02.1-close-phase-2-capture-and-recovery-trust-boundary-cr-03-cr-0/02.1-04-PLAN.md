---
phase: 02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0
plan: "04"
type: execute
wave: 4
depends_on: ["02.1-03"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomReplayRunnerTests/ReplayRunnerTests.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift
  - tools/verify/verify_phase_02_1_trust_boundary.py
  - tools/verify/tests/test_phase_02_1_trust_boundary.py
  - evidence/capture/phase-02/trust-boundary-closure.json
autonomous: true
requirements: [FR-CAPTURE-001, FR-B0-001, NFR-REPLAY-001, NFR-CONTRACT-001, NFR-RESILIENCE-001, SEC-CONSENT-001]
must_haves:
  truths:
    - "All production runner and native inspector paths admit a source through ArchiveVerifier/CaptureRecovery publication before capability-only ReplayCore."
    - "The native inspector retains the label `Recovered — capture may be incomplete` and never equates archive integrity with fixture, device, human, or gate proof."
    - "D-16: exact-source automated evidence can cover only CR-03, CR-04, and CR-12 and is independently rejected on source, test, schema, inventory, or scope drift."
    - "Phase 2, GATE-001, milestone v1.0, physical-device evidence, human evidence, and all other CR/WR findings remain explicitly open."
    - "The full Swift regression, production-callsite build, tracked-secret/release-surface check, and diff checks pass before verification handoff."
  artifacts:
    - path: tools/verify/verify_phase_02_1_trust_boundary.py
      provides: "Fail-closed exact-source test runner and evidence verifier scoped to three CR IDs"
    - path: evidence/capture/phase-02/trust-boundary-closure.json
      provides: "Sanitized machine-readable automated candidate evidence for CR-03/04/12 only"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
      provides: "Native recovery/inspection caller wired to generation-bound admission"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ArchiveVerifier.swift
      via: "CLI builds the pinned validator, admits/publishes input, and passes VerifiedArchive to ReplayCore"
      pattern: "ArchiveVerifier|CaptureRecovery|VerifiedArchive"
    - from: tools/verify/verify_phase_02_1_trust_boundary.py
      to: evidence/capture/phase-02/trust-boundary-closure.json
      via: "Fresh commands, source/schema digests, exact CR allowlist, and self-digest are recomputed before acceptance"
      pattern: "CR-03|CR-04|CR-12"
---

<objective>
Wire the repaired trust boundary into production callers and publish an honest exact-source automated verification handoff.

Purpose: Prove the three repairs on the real call graph without promoting Phase 2, GATE-001, or v1.0.
Output: Runner/native integration, fail-closed evidence tooling, one source-bound automated record, and explicit open-gate handoff.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-CONTEXT.md
@.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-VALIDATION.md
@.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-01-SUMMARY.md
@.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-02-SUMMARY.md
@.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-03-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-REVIEW.md
@.planning/v1.0-MILESTONE-AUDIT.md
@ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
@tools/verify/verify_phase_02_gate.py

<interfaces>
The production flow is `untrusted root -> injected ArchiveVerifier -> CaptureRecovery finalized import or open-prefix reconstruction -> RecoveryPublisher active generation -> VerifiedArchive -> ReplayCore.replay(_:)`. No runner, app, inspector, test helper used as production code, or later B0-facing adapter may call ReplayCore with a URL.

The evidence tool supports a generation mode that runs the exact focused suites and full package regression, computes source/schema/inventory/test-output digests, and writes one canonical sanitized JSON record, plus a verification mode that reruns/recomputes every binding without trusting stored PASS values. The record allowlists exactly `CR-03`, `CR-04`, and `CR-12`; identifies its implementation commit and closed source-file digest; names commands and machine-readable results; labels physical/human evidence `not_run_not_required_for_phase_02_1`; and fixes `phase_2`, `GATE-001`, and `milestone_v1.0` to `OPEN`/`PENDING`. It cannot encode closure of another review ID or a GREEN gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire runner and native inspection through one admission path</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift, ios/Packages/ReRoomContracts/Tests/ReRoomReplayRunnerTests/ReplayRunnerTests.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ArchiveVerifier.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureRecovery.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/RecoveryPublication.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayCore.swift, ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift, ios/Packages/ReRoomContracts/Tests/ReRoomReplayRunnerTests/ReplayRunnerTests.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift</read_first>
  <behavior>
    - "RED: runner/native integration tests fail once the raw ReplayCore URL overload is absent."
    - "Finalized and open interrupted inputs both traverse the injected pinned verifier and pointer-visible generation publisher before replay."
    - "Contract-invalid source roots, interior corruption, member substitution, corrupt active generations, and publication conflicts expose no replay timeline."
    - "Recovered native results keep the exact incomplete-capture label and integrity summary without fixture/GATE provenance."
  </behavior>
  <action>Write runner subprocess and native adapter integration tests first and record RED. Update both production call sites to construct/reuse the already configured pinned ContractValidator, inject ArchiveVerifier into CaptureRecovery, import or reconstruct/publish one generation, receive VerifiedArchive, and only then call capability-only ReplayCore. Test finalized, stale-open-manifest, terminal suffix, interior corruption, schema rejection, post-admission substitution, corrupt pointer/member, identical repeat, and explicit publication conflict. Preserve the prior user-facing `Recovered — capture may be incomplete` presentation and separate integrity verification from frozen fixture agreement or GATE-001. Do not modify the user-owned Xcode project/scheme changes, broaden discovery semantics, close WR-05, or add a compatibility URL bypass.</action>
  <acceptance_criteria>Package runner and native adapter compile against the capability-only API, all accepted replay flows follow the same generation-bound admission path, and all rejected flows expose zero unverified records.</acceptance_criteria>
  <verify><automated>swift build --package-path ios/Packages/ReRoomContracts --product ReRoomReplayRunner &amp;&amp; swift test --package-path ios/Packages/ReRoomContracts --filter ReplayRunnerTests &amp;&amp; xcodebuild -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build</automated></verify>
  <done>The real runner and native inspector consume only verifier-issued active-generation capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a fail-closed three-finding exact-source evidence verifier</name>
  <files>tools/verify/verify_phase_02_1_trust_boundary.py, tools/verify/tests/test_phase_02_1_trust_boundary.py</files>
  <read_first>tools/verify/verify_phase_02_gate.py, tools/verify/tests/test_phase_02_fixtures.py, .planning/phases/02-atomic-capture-and-exact-replay/02-REVIEW.md, .planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-VALIDATION.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, docs/canonical/RISK_AND_KILL_GATES.md</read_first>
  <behavior>
    - "RED D-16: records with an extra/missing CR ID, stored verdict-only PASS, stale source/schema digest, omitted focused suite, mutable HEAD alias, dirty behavior source, physical claim, Phase 2 completion, or GREEN GATE-001 are rejected."
    - "Generation mode executes ArchiveVerifierTests, ReplayCoreTests, CaptureRecoveryTests, CaptureCrashMatrixTests, RecoveryPublicationTests, RecoveryPublicationRaceTests, ReRoomCaptureCoreTests, and the complete package suite."
    - "Verification mode recomputes command results, exact implementation revision, closed source-tree/member digests, canonical record bytes, and record self-digest before accepting automated candidate evidence."
    - "Logs and evidence contain no raw room bytes, credentials, signing identity, private host path, or fabricated physical/human result."
  </behavior>
  <action>Write mutation tests first and record RED, then implement a standard-library-only fail-closed verifier modeled on the existing Phase 2 evidence conventions. Hardcode the exact three-ID allowlist and exact focused suite inventory from 02.1-VALIDATION.md. In generation mode, run commands without watch flags, capture exit status and bounded sanitized output digests, require the behavior-source set to be clean at one immutable Git commit, hash every behavior-bearing source plus the frozen schemas and active generation inventory format, and emit canonical JSON with a self-omitting SHA-256. In verification mode, do not trust stored booleans: confirm schema/field closure, rerun or independently validate every command/binding, compare source bytes to the recorded revision, and reject any claim that Phase 2, GATE-001, v1.0, physical evidence, human evidence, or another CR/WR is complete. Add no dependency.</action>
  <acceptance_criteria>All positive verifier tests pass; every scope/source/verdict/evidence mutation fails with a stable code; the tool cannot produce or accept an overbroad closure record.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_phase_02_1_trust_boundary</automated></verify>
  <done>D-16 evidence semantics are executable, exact-source-bound, sanitized, and limited to the three selected repairs.</done>
</task>

<task type="auto">
  <name>Task 3: Publish automated candidate evidence and preserve every outer gate as open</name>
  <files>evidence/capture/phase-02/trust-boundary-closure.json</files>
  <read_first>tools/verify/verify_phase_02_1_trust_boundary.py, .planning/phases/02-atomic-capture-and-exact-replay/02-REVIEW.md, .planning/v1.0-MILESTONE-AUDIT.md, docs/canonical/RISK_AND_KILL_GATES.md</read_first>
  <action>After Task 2 is committed, run the verifier's generation mode from that clean implementation revision to create the evidence record, then run verification mode against the checked-in bytes. Require fresh PASS results for all focused and full Swift suites, runner/native build coverage, source/schema/generation binding, release-surface/tracked-secret scanning, and diff checks. Record the three automated findings as review candidates only; state that independent phase verification/code review must decide closure. Explicitly retain CR-01, CR-02, CR-05 through CR-11, all WR findings, Phase 2 verification, GATE-001 physical/human evidence, and milestone v1.0 as open. Do not add physical fields, copy stale Phase 2 evidence, edit old evidence hashes, or call this artifact a gate report.</action>
  <verify><automated>python3 tools/verify/verify_phase_02_1_trust_boundary.py --full --output evidence/capture/phase-02/trust-boundary-closure.json &amp;&amp; python3 tools/verify/verify_phase_02_1_trust_boundary.py --verify-evidence evidence/capture/phase-02/trust-boundary-closure.json &amp;&amp; swift test --package-path ios/Packages/ReRoomContracts &amp;&amp; scripts/verify-reroom-release-surface &amp;&amp; git diff --check</automated></verify>
  <done>One verified exact-source automated record is ready for `$gsd-verify-work 02.1`, and it makes no completion claim beyond CR-03/04/12 software evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Production path -> repaired core | A legacy raw-path call could bypass the new verifier/publication boundary. |
| Test execution/source tree -> evidence JSON | Stored PASS text or stale source bindings could impersonate current automated proof. |
| Phase 02.1 evidence -> milestone/gate status | Narrow software repairs could be overstated as physical, human, phase, gate, or milestone completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02.1-09 | Elevation of privilege | Runner/native replay call sites | high | mitigate | Task 1 removes legacy URL admission and tests the sole verifier -> publisher -> capability call graph. |
| T-02.1-10 | Spoofing | Exact-source evidence | high | mitigate | Tasks 2-3 bind an immutable revision, behavior-source/schema digests, fresh command results, canonical bytes, and self-digest. |
| T-02.1-11 | Repudiation | Closure scope/status | high | mitigate | Tasks 2-3 enforce the exact three-ID allowlist and reject Phase 2/GATE-001/v1.0 or physical/human promotion. |
| T-02.1-12 | Information disclosure | Logs/evidence | medium | mitigate | Tasks 2-3 bound and sanitize output, retain hashes/stable IDs only, and run the existing release-surface/secret check. |
</threat_model>

<verification>
Run all three task commands; run the full Swift package suite from a clean implementation revision; inspect the production call graph for any `ReplayCore.replay(root:)` or directly constructible VerifiedArchive; verify the evidence allowlist is exactly CR-03/04/12 and Phase 2/GATE-001/v1.0 remain open; run the smallest GSD consistency check and `git diff --check`. Do not run or fabricate signed-device/human evidence.
</verification>

<success_criteria>
Production callers use one repaired trust path, all focused/full regressions pass against one exact source revision, and the automated handoff truthfully supports review of CR-03/04/12 while every outer gate and unrelated finding stays open.
</success_criteria>

<output>
Create `.planning/phases/02.1-close-phase-2-capture-and-recovery-trust-boundary-cr-03-cr-0/02.1-04-SUMMARY.md` when done.
</output>
