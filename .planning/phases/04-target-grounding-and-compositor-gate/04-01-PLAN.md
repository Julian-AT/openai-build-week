---
phase: 04-target-grounding-and-compositor-gate
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
autonomous: true
requirements: [FR-TARGET-001]
must_haves:
  truths:
    - "One explicit unambiguous manual candidate grounds one stable chair/table target in the current ARKit world epoch without changing the scene revision."
    - "Missed, ambiguous, unhealthy, stale, or wrong-epoch input leaves the prior target unchanged and reports a stable actionable reason."
    - "Tracking loss/reset revokes unsafe target-dependent readiness in the next model snapshot; explicit reseed either restores the same stable identity in the current epoch or clearly fails."
    - "Select, place, replace, remove, and restore expose independent canonical readiness states and coded reasons."
  artifacts:
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      provides: "Pure target lifecycle/readiness reducer and immutable room-edit presentation snapshot"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
      provides: "Reducer/model mutation and nonmutation tests"
  key_links:
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      to: ReRoomContracts SceneState/TargetContext
      via: "stable object ID plus exact world_frame_id/world_frame_version; renderer identities never enter canonical context"
---

<objective>
Implement the deterministic manual target lifecycle and independent operation-readiness model.

Purpose: Make every AR input fail closed before target state can influence proposals or mutation.
Output: Tested target reducer/value types integrated into `RoomEditSnapshot`.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-CONTEXT.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-RESEARCH.md
@docs/contracts/scene-state.schema.json
@ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
</context>

<feature>
  <name>Manual target lifecycle and readiness reducer</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift</files>
  <behavior>
    - RED first: zero candidates is a typed miss; multiple candidates is ambiguity; both preserve target and revision.
    - RED first: one current-epoch candidate creates the fixed stable `object_*` identity and a versioned manual frozen-proxy snapshot without changing `scene_revision`.
    - RED first: limited/unavailable tracking, interruption, failure, and world reset revoke unsafe target readiness immediately while committed edit visibility and restore eligibility remain independent.
    - RED first: reseed preserves semantic object identity, replaces only current-epoch spatial evidence, and fails on miss/ambiguity/unhealthy input without inventing correction transforms.
    - RED first: readiness uses only unavailable/warming/ready/degraded/failed with stable reason codes; remove remains unavailable for Phase 6 reveal evidence, restore is transaction-derived, and replace is at most degraded manual-proxy readiness while formal gates are pending.
  </behavior>
  <implementation>Add Equatable/Sendable target candidate, lifecycle, failure-reason, capability, readiness, frozen-proxy, and snapshot types plus a pure reducer in the existing model file. Use a stable prefixed target ID and bind every spatial sample to the exact world ID/version and captured revision. Extend `RoomEditSnapshot` and proposal binding so a grounded target contributes `selectedObjectID` and current target context, while target selection/reseed itself never calls `NativeBranchAuthority` and never changes revision. Coarsen model publication to meaningful state transitions. Keep place/restore behavior and Phase 3 durability exact; do not fabricate mask-volume, OBB, reveal, semantic-provider, or gate readiness.</implementation>
</feature>

<verification>
<automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/RoomEditModelTests</automated>
</verification>

<success_criteria>
- All target lifecycle/readiness cases are deterministic and tested before ARKit glue.
- Target failures are nonmutating and every operation remains independently explainable.
</success_criteria>

<output>
Create `.planning/phases/04-target-grounding-and-compositor-gate/04-01-SUMMARY.md` when done.
</output>
