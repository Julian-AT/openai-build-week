---
phase: 04-target-grounding-and-compositor-gate
plan: "04"
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - scripts/verify-phase-04-targeting
  - tools/verify/tests/test_phase_04_targeting.py
  - evidence/targeting/phase-04/automated-preflight.json
autonomous: true
requirements: [FR-TARGET-001, NFR-RENDER-001]
must_haves:
  truths:
    - "One fail-closed command proves deterministic target lifecycle, shared-session/raycast behavior, UI recovery, Debug/Release builds, release-surface compatibility, and local render independence."
    - "The sanitized report records automated functional checks and the selected manual/no-dense/local fallbacks while GATE-003/004/005/007/012 remain PENDING."
    - "No evidence contains raw room/user content, machine paths, credentials, fabricated device measurements, or unsupported P0/performance/provider claims."
  artifacts:
    - path: scripts/verify-phase-04-targeting
      provides: "Quick/full fail-closed Phase 4 verification entry point"
    - path: tools/verify/tests/test_phase_04_targeting.py
      provides: "Mutation tests for orchestration, evidence, and claim boundaries"
    - path: evidence/targeting/phase-04/automated-preflight.json
      provides: "Sanitized revision-bound automated fallback-slice result"
  key_links:
    - from: scripts/verify-phase-04-targeting
      to: evidence/targeting/phase-04/automated-preflight.json
      via: "atomic publication only after every declared full check passes"
---

<objective>
Verify the Phase 4 sprint fallback slice and publish an honest pending-gate automated report.

Purpose: Detect lifecycle/render-boundary regressions without converting simulator automation into physical/provider evidence.
Output: Mutation tests, quick/full verifier, and sanitized preflight evidence.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-CONTEXT.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-03-SUMMARY.md
@scripts/verify-phase-03-transactions
@tools/verify/tests/test_phase_03_transactions.py
@evidence/transactions/phase-03/automated-preflight.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject incomplete checks, unsafe dependencies, and overstated claims</name>
  <files>tools/verify/tests/test_phase_04_targeting.py, scripts/verify-phase-04-targeting</files>
  <behavior>
    - Mutation tests reject a missing reducer/AR/UI/build/release/secret/whitespace/local-independence check or partial publication.
    - Mutation tests reject an absent or reordered camera/reveal/occluder/asset/debug/SwiftUI descriptor assertion and reject falsely available reveal/occluder layers.
    - Static boundary audit rejects production learned/cloud/network/scripting dependencies in the target/raycast/representable path while allowing test fixtures.
    - Report validation rejects green gate states, MEASURED performance/provider claims, physical observations, raw/private values, and absolute paths.
  </behavior>
  <action>Adapt the established Phase 3 quick/full verifier pattern. Quick mode runs focused target/model and AR policy tests plus Python mutation tests, including a required exact compositor-layer descriptor assertion for camera, reveal, occluder, asset/proxy, debug, SwiftUI and explicit unavailable reveal/occluder states. Full mode additionally uses a unique simulator for the room-edit UI journey, builds Debug and Release, runs the existing release-surface verifier, audits only the production Phase 4 render/raycast files for external wait/dependency constructs, scans tracked files for secrets, and runs `git diff --check` without staging user files. Make the mutation suite fail when the ordering check is missing, reordered, duplicated, omitted, or promotes an absent layer. Make every command failure fatal and publish nothing from quick or partial full runs.</action>
  <verify><automated>python3 -m unittest tools.verify.tests.test_phase_04_targeting</automated></verify>
  <done>Mutation tests prove the verifier cannot omit required checks, accept external render waits, or overstate evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run full preflight and atomically publish pending-gate evidence</name>
  <files>scripts/verify-phase-04-targeting, tools/verify/tests/test_phase_04_targeting.py, evidence/targeting/phase-04/automated-preflight.json</files>
  <behavior>
    - Full success records exact implementation revision, source/check digests, commands, counts, fallback selections, and sanitized outcomes.
    - GATE-003/004/005/007/012 are all present and exactly PENDING with their deferred physical/provider work.
    - Re-running from the same revision is semantically stable and never stages or deletes user dirty/untracked artifacts.
  </behavior>
  <action>Complete atomic evidence publication after one authoritative full run. Bind the report to an exact `git:<40-lowercase-hex>` implementation revision and closed source digest set, state only `automated sprint fallback slice passed`, enumerate the manual target/no-dense/local selections, and retain the five pending gate records. Store no raw frames, target transforms, user/device identifiers, credentials, or machine paths. Run quick then full and validate the emitted report with the mutation tests.</action>
  <verify><automated>scripts/verify-phase-04-targeting quick &amp;&amp; scripts/verify-phase-04-targeting full &amp;&amp; python3 -m unittest tools.verify.tests.test_phase_04_targeting</automated></verify>
  <done>Quick/full pass and the only published report is sanitized, revision-bound, and incapable of marking a deferred gate green.</done>
</task>

</tasks>

<verification>
- `scripts/verify-phase-04-targeting quick` passes focused deterministic checks.
- `scripts/verify-phase-04-targeting full` passes UI/build/release/boundary/secret/whitespace checks and atomically publishes evidence.
- Mutation coverage proves the exact compositor-layer assertion cannot be removed, reordered, or weakened.
- The evidence lists all five formal gates as PENDING.
</verification>

<success_criteria>
- One command fails closed on target, session, UI, build, boundary, claim, secret, or provenance drift.
- Automated evidence is useful for the demo sprint and makes no physical/provider/P0 claim.
</success_criteria>

<output>
Create `.planning/phases/04-target-grounding-and-compositor-gate/04-04-SUMMARY.md` when done.
</output>
