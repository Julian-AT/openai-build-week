---
phase: 04-target-grounding-and-compositor-gate
plan: "03"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofUITests/RoomEditJourneyTests.swift
autonomous: true
requirements: [FR-TARGET-001, NFR-RENDER-001]
must_haves:
  truths:
    - "The release room-edit surface shows the live AR camera behind a local conservative proxy and SwiftUI controls, with one explicit tap/reseed interaction."
    - "Target identity/world epoch and all five operation readiness states/reasons are visible and accessible; manual/no-dense/local fallbacks and pending gates are explicit."
    - "Tracking loss/reset disables unsafe target actions while the last committed place remains visible and restore remains transaction-derived."
    - "The available compositor order is camera, reveal, occluder, asset/proxy, debug, SwiftUI; absent reveal/occluder layers are unavailable rather than fabricated."
  artifacts:
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
      provides: "Shared-session RealityKit camera/proxy representable and compact accessible overlay"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      provides: "Live/fixture object graph and render-safe immutable snapshots"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProofUITests/RoomEditJourneyTests.swift
      provides: "Target seed/reseed/readiness plus Phase 3 journey regression"
  key_links:
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
      to: ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift
      via: "one retained ARView/session/raycast adapter; representable update consumes coarsened snapshots only"
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
      to: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      via: "tap and Reseed target Button call model entry points; model snapshots drive proxy/readiness presentation"
---

<objective>
Integrate manual target grounding and a local shared-session compositor into the native demo surface.

Purpose: Give the user one legible camera-grounded interaction while preserving transaction and render-loop boundaries.
Output: RealityKit/SwiftUI surface, live/fixture wiring, model tests, and UI journey regression.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-CONTEXT.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-RESEARCH.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-01-SUMMARY.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-02-SUMMARY.md
@ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire one live AR object graph and deterministic fixture graph</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift</files>
  <behavior>
    - Live factory constructs one ARView, uses its session in the existing controller/DeviceProofModel, and injects the bounded raycaster into RoomEditModel.
    - Prepare starts permission/tracking once; tap/reseed and controller events reduce into current-world target/readiness state.
    - UI-test factory uses deterministic healthy/miss/ambiguous/tracking-loss fixtures without asking the simulator for AR tracking.
    - Existing place/cancel/confirm/relaunch/restore tests remain exact and target selection never increments revision.
    - An ordered compositor descriptor is exactly camera, reveal, occluder, asset/proxy, debug, SwiftUI; reveal and occluder remain present as explicitly unavailable layers rather than disappearing or being fabricated.
  </behavior>
  <action>Extend the live factory/owner object graph to retain one automatically-unconfigured ARView, one session controller backed by that view's session, one DeviceProofModel, and one RoomEditModel. Register target tracking/reset observation without displacing the device proof observer. Convert selected transforms only at the boundary and expose an immutable render snapshot; never expose canonical mutation or transaction actors to the view. Define a closed ordered compositor-layer descriptor with exactly camera, reveal, occluder, asset/proxy, debug, SwiftUI and an availability/reason value per layer. Add focused model assertions that reject reordering, omission, duplication, or falsely available reveal/occluder layers. Provide deterministic UI-test fixtures and retain Phase 3 local store/restart behavior. Keep all frequently changing AR state out of SwiftUI environment/body.</action>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/RoomEditModelTests</automated></verify>
  <done>Model tests prove one-session wiring, seed/reseed/revocation, immutable render snapshots, exact six-layer ordering/availability, and unchanged transaction revisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present camera, proxy, readiness, recovery, and honest fallbacks</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofUITests/RoomEditJourneyTests.swift</files>
  <behavior>
    - A tap on the AR surface selects the fixture target; a reticle/guidance and explicit Reseed target Button expose success/failure.
    - Camera remains the background; one local translucent conservative proxy/committed asset layer is synchronized without network/model/storage work in representable update.
    - Readiness rows use stable identity/reasons and Dynamic Type/VoiceOver-friendly native controls.
    - UI automation covers seed, ambiguity/miss, reseed, readiness, tracking revocation, and the complete Phase 3 place/restore regression.
  </behavior>
  <action>Read the SwiftUI expert skill plus latest API, state, view-structure, performance, and accessibility references before editing. Add a thin `UIViewRepresentable` for the retained ARView with a coordinator only for tap callbacks; update local entities from immutable snapshot differences and keep `makeUIView`/`updateUIView` free of file, network, model, worker, web, or transaction waits. Compose peer camera/render/UI layers intentionally, use native Buttons and stable accessibility identifiers, show the target ID/epoch/readiness matrix and fallback/pending-gate labels, and keep reveal/occluder unavailable. Preserve exactly four operation controls and the existing explicit-confirm/restore journey. Add simulator fixture controls through launch arguments only, never production debug buttons.</action>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofUITests/RoomEditJourneyTests</automated></verify>
  <done>The UI test completes target/recovery/readiness and place/restore journeys; camera/proxy/UI are local and no deferred gate is presented as passed.</done>
</task>

</tasks>

<verification>
- Focused model and UI tests pass on a uniquely booted iPhone 17 simulator.
- Focused model tests assert the exact six-layer compositor descriptor and explicit unavailable reveal/occluder states.
- Debug and Release products build.
- Existing release-surface and GATE-001 launch controls remain intact.
</verification>

<success_criteria>
- A user can tap/reseed one stable target and understand readiness/recovery without semantic/dense/cloud dependencies.
- The native surface uses one AR session and a render-safe local snapshot boundary.
</success_criteria>

<output>
Create `.planning/phases/04-target-grounding-and-compositor-gate/04-03-SUMMARY.md` when done.
</output>
