---
phase: 04-target-grounding-and-compositor-gate
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/ARSessionPolicyTests.swift
autonomous: true
requirements: [FR-TARGET-001, NFR-RENDER-001]
must_haves:
  truths:
    - "The room-edit target path and existing device-proof path observe one ARSession without stealing each other's tracking/reset events."
    - "A screen tap resolves through an injected bounded raycast seam to value-type current-world candidates; transient AR anchors/entities never become semantic IDs."
    - "The AR callback path performs no network, model, worker, web, filesystem, transaction-store, or unbounded queue work."
  artifacts:
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift
      provides: "Multi-observer AR event boundary and testable RealityKit raycast adapter"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProofTests/ARSessionPolicyTests.swift
      provides: "Shared-session, observer, raycast, and revocation tests"
  key_links:
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift
      to: ARKit/RealityKit
      via: "one injected ARSession and synchronous bounded raycast conversion at the main-actor boundary"
---

<objective>
Expose one bounded shared-session AR event/raycast adapter for manual target grounding.

Purpose: Preserve ARKit as sole healthy-session authority while making target input deterministic and testable.
Output: AR session observer/raycast seam with focused tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-CONTEXT.md
@.planning/phases/04-target-grounding-and-compositor-gate/04-RESEARCH.md
@ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProofTests/ARSessionPolicyTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve all consumers on one AR event stream</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/ARSessionPolicyTests.swift</files>
  <behavior>
    - Two registered observers receive running/tracking/plane/reset/recovery events in registration order; removing one does not affect the other.
    - Existing `DeviceProofModel` behavior remains source compatible and interruption/failure still revokes running state synchronously.
    - Duplicate no-op state does not create unbounded buffered work.
  </behavior>
  <action>Replace the single-consumer callback limitation with a small main-actor observer registry using stable opaque tokens and bounded synchronous delivery. Retain a compatibility property only if existing callers need it, but wire `DeviceProofModel` without monopolizing events. Never dispatch high-rate frame buffers through the registry. Tests cover two observers, removal, reset, interruption, failure, ordering, and no second driver/session run.</action>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/ARSessionPolicyTests</automated></verify>
  <done>Multiple native consumers share one controller/session and receive every safety transition without queue growth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert manual screen taps into bounded value candidates</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/ARSessionPolicyTests.swift</files>
  <behavior>
    - Existing horizontal geometry is preferred; an estimated horizontal plane is the explicit fallback; no hit returns an empty result.
    - Results are bounded, finite, converted once to RR-COORD-1 row-major `Matrix4`, and carry current world epoch/captured revision supplied by the caller.
    - Raycast adapter has no network/model/storage/transaction dependency and never selects semantic identity.
  </behavior>
  <action>Add a protocol/closure-backed raycast boundary and an Apple-framework adapter in the existing source file. Compiler-check the installed RealityKit API rather than assuming a setter for `ARView.session`: construct the view once with automatic session configuration disabled and inject its own session into `SystemARSessionDriver`. Raycast on explicit tap only, cap candidate count, reject nonfinite transforms, prefer detected geometry before estimated fallback, and return value candidates for the reducer. Tests use a fake resolver; do not require ARKit behavior from the simulator.</action>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/ARSessionPolicyTests</automated></verify>
  <done>The adapter compiler-checks and deterministic tests prove bounded nonsemantic raycast conversion with one session and no external wait.</done>
</task>

</tasks>

<verification>
- Focused ARSessionPolicyTests pass.
- `rg -n 'URLSession|WebSocket|NWConnection|Data\(contentsOf|TransactionStore' ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift` finds no render/raycast dependency.
</verification>

<success_criteria>
- One AR session owns tracking/world truth for capture, target selection, and rendering.
- Tap conversion is bounded, testable, and independent of semantic/dense/cloud providers.
</success_criteria>

<output>
Create `.planning/phases/04-target-grounding-and-compositor-gate/04-02-SUMMARY.md` when done.
</output>
