---
phase: 08-p0-hardening-and-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - tools/verify/verify_phase_08_evidence.py
  - tools/verify/tests/test_phase_08_evidence.py
  - scripts/verify-phase-08-evidence
  - evidence/hardening/phase-08/evidence-index.json
  - evidence/hardening/phase-08/pending-gates.json
  - docs/demo/PHASE_08_DEMO_RUNBOOK.md
  - docs/demo/BUILD_WEEK_SUBMISSION_HANDOFF.md
autonomous: true
requirements:
  - NFR-LATENCY-001
  - NFR-RESILIENCE-001
  - SEC-CREDENTIAL-001
  - SEC-AGENT-001
  - OPS-LICENSE-001
  - OPS-GOLDEN-001
  - OPS-SUBMISSION-001
must_haves:
  truths:
    - "Per D-03 and D-06, every retained item has a digest/revision/procedure and exact evidence class; automated, device, browser, human, and submission states cannot impersonate one another."
    - "Per D-01, D-02, D-04, D-05, and D-09, the published index consumes only validated existing evidence and adds no product, provider, dependency, gate authority, or raw/private material."
    - "Per D-08, formal gate state and sprint disposition remain separate, and the strongest generated claim is the locked demo-candidate sentence."
    - "Per D-07, the runbook and submission handoff prepare the representative rehearsal, video, rules, and Session-ID steps while publication and submission remain human-owned."
  artifacts:
    - path: "evidence/hardening/phase-08/evidence-index.json"
      provides: "Sanitized digest-bound cross-phase evidence inventory with exact classification"
    - path: "evidence/hardening/phase-08/pending-gates.json"
      provides: "Machine-checkable separation of canonical formal state and sprint disposition"
    - path: "docs/demo/PHASE_08_DEMO_RUNBOOK.md"
      provides: "Operator procedure for automated preflight and one honestly classified native/B0 rehearsal"
    - path: "docs/demo/BUILD_WEEK_SUBMISSION_HANDOFF.md"
      provides: "Human-owned rules recheck, video shot list, claim checklist, Session-ID selection, and submission handoff"
  key_links:
    - from: "scripts/verify-phase-08-evidence"
      to: "evidence/hardening/phase-08/automated-preflight.json, evidence/hardening/phase-08/sprint-bom.json"
      via: "require independently verified Plan 08-01 inputs before indexing or rehearsal publication"
      pattern: "verify-phase-08-hardening"
    - from: "evidence/hardening/phase-08/evidence-index.json"
      to: "evidence/hardening/phase-08/pending-gates.json"
      via: "shared source revision and content digests without deriving formal state from automated checks"
      pattern: "(implementation_revision|sha256|formal_state|sprint_disposition)"
    - from: "docs/demo/PHASE_08_DEMO_RUNBOOK.md"
      to: "scripts/verify-phase-08-evidence"
      via: "exact preflight/rehearsal commands and evidence-classification rules"
      pattern: "verify-phase-08-evidence"
---

<objective>
Publish the D-01 through D-09 honest evidence and operator handoff for the sprint demo candidate.

Purpose: Make the automated integration result and any separately retained representative smoke easy to audit and demonstrate while preserving every unperformed formal, physical, human, license, measurement, and submission step.
Output: A mutation-tested evidence validator, sanitized index, distinct formal/sprint gate report, representative rehearsal runbook, and human submission handoff. All requirement IDs are trace metadata only.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/08-p0-hardening-and-evidence/08-CONTEXT.md
@.planning/phases/08-p0-hardening-and-evidence/08-RESEARCH.md
@.planning/phases/08-p0-hardening-and-evidence/08-01-SUMMARY.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@docs/canonical/RISK_AND_KILL_GATES.md
@evidence/templates/gate-report.schema.json
@evidence/hardening/phase-08/automated-preflight.json
@evidence/hardening/phase-08/sprint-bom.json

<interfaces>
Each evidence-index row has closed keys for stable evidence ID, implementation revision, relative tracked path or opaque external artifact ID, SHA-256, requirement IDs, gate IDs, command/procedure, classification (`automated_check`, `device_smoke`, `browser_smoke`, `human_observation`, or `external_submission`), and actual state (`VERIFIED`, `PENDING`, or `BLOCKED`). VERIFIED tracked entries must exist and match their digest. Non-automated VERIFIED entries require a separately retained artifact with classification-specific provenance; automated output can never create one.

Each gate row keeps `formal_state` (`GREEN`, `RUNNING`, `RED`, `UNRUN`, `WAIVED_BY_HUMAN`, or `NO_REPORT`) separate from `sprint_disposition` (`retained_green`, `running`, `fallback_active`, `deferred_pending`, or `blocked`). A formal state comes only from a schema-valid canonical gate report; absent reports are NO_REPORT, never inferred from tests. Requirement trace state is separately limited to `evidence_present`, `partial`, `pending`, or `blocked` and is never completion metadata.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the closed evidence index and formal-versus-sprint status boundary</name>
  <files>tools/verify/verify_phase_08_evidence.py, tools/verify/tests/test_phase_08_evidence.py</files>
  <behavior>
    - "A complete digest-bound tracked evidence index plus canonical formal-state/sprint-disposition report accepts."
    - "A VERIFIED tracked item with missing/path-escaping/digest-mismatched content rejects; an automated item relabeled as device, browser, human, golden, or submission evidence rejects."
    - "A gate promoted from automated checks, PENDING used as formal state, absent formal report represented as GREEN, or requirement completion field rejects."
    - "Raw room media, private trace/log, absolute path, signing/user/device identifier, credential-shaped value, unsupported performance/license claim, or public-submission assertion rejects with a stable class."
  </behavior>
  <action>Write mutation tests first, then implement a standard-library-only independent verifier over closed JSON objects. Per D-03/D-06, validate exact classification, state, revision, digest, ID, procedure, and evidence-location bindings and require external physical/browser/human evidence to stay opaque and separately proven. Per D-08, load formal gate state only from the canonical schema-valid report set, represent absence as NO_REPORT, and validate the separate sprint disposition without allowing test outcomes to promote gates or requirements. Per D-04/D-05/D-09, reject private/credential/license-promotion/product-expansion content and never echo rejected values. Per D-01/D-02, require Plan 08-01 evidence to verify before any index input is accepted.</action>
  <verify>
    <automated>python3 -m unittest tools.verify.tests.test_phase_08_evidence -v</automated>
  </verify>
  <done>The verifier accepts only digest-bound sanitized evidence with independent formal/sprint/requirement state domains; classification, authority, completeness, privacy, claim, and path mutations fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the sanitized index, pending report, and representative automated rehearsal</name>
  <files>scripts/verify-phase-08-evidence, evidence/hardening/phase-08/evidence-index.json, evidence/hardening/phase-08/pending-gates.json</files>
  <action>Create one executable with `full` and `--verify-evidence` modes. It must first run Plan 08-01 independent verification, then index only schema/shape-valid tracked reports and the exact BOM using repository-relative paths and content digests. Run one representative automated rehearsal by invoking the existing full replacement, removal, and B0 verifiers; classify it only as automated integration and local HTTP evidence. If a separately retained signed-device or real-browser artifact is not explicitly supplied and validated, emit PENDING rows and the runbook procedure instead of inferring success. Build all P0 gate rows from actual canonical reports plus the sprint register: preserve checked-in GREEN/RUNNING/RED/UNRUN/WAIVED_BY_HUMAN values, use NO_REPORT for absence, and record deferred/fallback/blocker disposition separately. Per D-03/D-05/D-08, keep 5/5 golden, latency distributions, resilience campaigns, license closure, and human submission pending/blocked as their evidence dictates. Stage, independently verify, and atomically publish both byte-stable files; no failure or blocked upstream state may replace a prior valid generation.</action>
  <verify>
    <automated>scripts/verify-phase-08-evidence full &amp;&amp; cp evidence/hardening/phase-08/evidence-index.json /tmp/reroom-phase-08-index-first.json &amp;&amp; cp evidence/hardening/phase-08/pending-gates.json /tmp/reroom-phase-08-gates-first.json &amp;&amp; scripts/verify-phase-08-evidence full &amp;&amp; cmp /tmp/reroom-phase-08-index-first.json evidence/hardening/phase-08/evidence-index.json &amp;&amp; cmp /tmp/reroom-phase-08-gates-first.json evidence/hardening/phase-08/pending-gates.json &amp;&amp; scripts/verify-phase-08-evidence --verify-evidence</automated>
  </verify>
  <done>Two runs publish identical independently verified sanitized files; every actual formal report is preserved, every absent/deferred item is explicit, and the representative rehearsal is classified no more strongly than its retained evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Write the operator demo runbook and human submission handoff</name>
  <files>docs/demo/PHASE_08_DEMO_RUNBOOK.md, docs/demo/BUILD_WEEK_SUBMISSION_HANDOFF.md</files>
  <action>Write an operator runbook that starts with free-space/tool/prerequisite checks, runs the two Phase 8 verifiers, launches the existing signed native app and local B0 page without changing signing/PBX/deployment, walks place/replace/remove/restore plus replay, records recovery steps, and assigns evidence classification exactly per D-03/D-06. State that one representative run is rehearsal evidence and OPS-GOLDEN-001 remains pending until canonical 5/5 after blocking gates. Write the human submission handoff per D-07 with the official rules URLs and retrieval date, immediate rules recheck, repository/category/description checklist, public video under three minutes with audio explaining Codex and GPT-5.6, four-operation/B0 shot list, representative `/feedback` Session-ID selection, public-media approval, and final submit action. Use the exact D-08 permitted claim, list pending blockers/deferred resume order, forbid unsupported performance/novelty/license/P0 language, and leave every public upload, Session choice, rules sign-off, and submission unchecked and human-owned. Per D-02/D-04/D-09, document the existing local fallback only; do not add provider, service, upload, account, share, or product behavior.</action>
  <verify>
    <automated>python3 tools/verify/verify_phase_08_evidence.py --docs docs/demo/PHASE_08_DEMO_RUNBOOK.md docs/demo/BUILD_WEEK_SUBMISSION_HANDOFF.md --index evidence/hardening/phase-08/evidence-index.json --gates evidence/hardening/phase-08/pending-gates.json &amp;&amp; git diff --check -- docs/demo evidence/hardening/phase-08</automated>
  </verify>
  <done>The docs provide exact repeatable commands, honest classification and recovery guidance, current official submission requirements, the locked claim and shot list, explicit human-only actions, and the complete deferred resume order without asserting any unperformed result.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest tools.verify.tests.test_phase_08_evidence -v`
- `scripts/verify-phase-08-hardening --verify-evidence`
- `scripts/verify-phase-08-evidence full` twice with byte comparisons
- `scripts/verify-phase-08-evidence --verify-evidence`
- `python3 tools/verify/verify_phase_08_evidence.py --docs ...`
- `git diff --check`
</verification>

<success_criteria>
- Evidence is sanitized, digest-bound, revision-bound, classification-correct, and independently checked.
- Formal gate state, sprint disposition, requirement trace state, and evidence classification cannot promote one another.
- The automated representative rehearsal is retained honestly; absent device/browser/human evidence remains PENDING.
- The runbook is executable and the submission package is prepared, but all public and human decisions remain undone.
- All seven Phase 8 requirements and all gates remain trace-only with actual pending/blocked/evidence-present state; the summary records `requirements-completed: []`.
</success_criteria>

<source_coverage>
GOAL: The original Phase 8 repeatability/security/license/resilience/measurement/submission goal is covered by exact automated checks plus an honest pending/blocker report; the sprint does not claim canonical closure.
REQ: NFR-LATENCY-001, NFR-RESILIENCE-001, SEC-CREDENTIAL-001, SEC-AGENT-001, OPS-LICENSE-001, OPS-GOLDEN-001, and OPS-SUBMISSION-001 each map to a closed trace row and exact resume procedure across 08-01/08-02.
RESEARCH: prerequisite-gated composition, independent validation/atomic publication, separate formal/sprint state, exact BOM, evidence indexing, runbook, official-rules recheck, and deferred resume order are covered.
CONTEXT: D-01 through D-09 are cited in task actions and enforced by the two plan boundaries. Deferred Ideas remain excluded.
</source_coverage>

<output>
Create `.planning/phases/08-p0-hardening-and-evidence/08-02-SUMMARY.md` with `requirements-completed: []`; report the demo-candidate claim, evidence links, and exact pending/deferred state only.
</output>
