---
phase: 08-p0-hardening-and-evidence
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-04"
  - "06-04"
  - "07-03"
files_modified:
  - tools/verify/verify_phase_08_hardening.py
  - tools/verify/tests/test_phase_08_hardening.py
  - scripts/verify-phase-08-hardening
  - evidence/hardening/phase-08/sprint-bom.json
  - evidence/hardening/phase-08/automated-preflight.json
autonomous: true
requirements:
  - NFR-RESILIENCE-001
  - SEC-CREDENTIAL-001
  - SEC-AGENT-001
  - OPS-LICENSE-001
must_haves:
  truths:
    - "Per D-01 and D-02, one command consumes Phase 5–7 behavior only when each expected summary and authoritative verifier exists and passes; an absent prerequisite is BLOCKED_BY_UPSTREAM, never a passing skip."
    - "Per D-04 and D-09, tracked inputs plus built web/app outputs are scanned without adding a provider, credential, dependency, service, product authority, deployment, or PBX/signing change."
    - "Per D-05 and D-08, the exact tracked shipping closure is inventoried while the missing root license/use-and-redistribution decision keeps OPS-LICENSE-001 and GATE-011 pending."
    - "Per D-03, D-06, and D-07, automated checks produce sanitized trace inputs only; they do not become device/browser/human/submission evidence."
  artifacts:
    - path: "scripts/verify-phase-08-hardening"
      provides: "Fail-closed prerequisite, integration, build-surface, safety, and BOM orchestrator"
    - path: "tools/verify/verify_phase_08_hardening.py"
      provides: "Independent closed-shape verifier for readiness, checks, source closure, and license blockers"
    - path: "evidence/hardening/phase-08/sprint-bom.json"
      provides: "Exact tracked dependency/asset/resource inventory with explicit terms and decision state"
    - path: "evidence/hardening/phase-08/automated-preflight.json"
      provides: "Revision-bound automated check report with no gate or requirement promotion"
  key_links:
    - from: "scripts/verify-phase-08-hardening"
      to: "scripts/verify-phase-05-replacement, scripts/verify-phase-06-removal, scripts/verify-phase-07-b0"
      via: "summary-plus-executable readiness checks followed by authoritative verifier invocation"
      pattern: "(05-04|06-04|07-03|verify-phase-0[567])"
    - from: "scripts/verify-phase-08-hardening"
      to: "tools/verify/verify_phase_08_hardening.py"
      via: "stage, independently validate, and atomically publish both closed JSON artifacts"
      pattern: "verify_phase_08_hardening"
---

<objective>
Create the D-01 through D-09 fail-closed hardening boundary for the sprint demo candidate.

Purpose: Prove the available four-operation/B0 implementation, safety boundaries, and exact shipping inventory without manufacturing missing upstream work, measurements, licenses, or formal evidence.
Output: One dependency-free verifier with mutation tests, one composite command, one exact BOM, and one sanitized automated preflight report. Requirement IDs in this plan are trace-only and remain pending.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/08-p0-hardening-and-evidence/08-CONTEXT.md
@.planning/phases/08-p0-hardening-and-evidence/08-RESEARCH.md
@.planning/phases/05-curated-replacement-vertical/05-04-SUMMARY.md
@.planning/phases/06-controlled-multi-surface-removal/06-04-SUMMARY.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-03-SUMMARY.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@docs/canonical/RISK_AND_KILL_GATES.md
@evidence/dependencies/phase-01-package-audit.json
@scripts/verify-phase-03-transactions
@scripts/verify-phase-05-replacement

<interfaces>
The readiness member is closed over Phase 2 through Phase 7. Each row contains only phase ID, required summary, required executable, and one of READY, FAILED, or BLOCKED_BY_UPSTREAM. READY requires both files and a successful declared verifier invocation.

The BOM is derived only from tracked `ios/Packages/ReRoomContracts/Package.resolved`, `tools/javascript/package-lock.json`, `web/package-lock.json`, `tools/python/requirements.lock`, Phase 1 audit facts, and tracked app resources. Every entry records kind, stable name, exact version or content digest, source, applicable terms/attribution reference, shipping surface, and use/redistribution decision. Generated workspace resolution and untracked build products are excluded as authorities.

The preflight report records implementation revision, closed source digests, readiness rows, named check results, built-surface scan digests, BOM digest/status, limitations, and trace-only requirement/gate IDs. It stores no raw output, matched bytes, absolute/temporary path, room data, signing/user/device identifier, or completion claim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the fail-closed readiness, safety, BOM, and preflight contracts</name>
  <files>tools/verify/verify_phase_08_hardening.py, tools/verify/tests/test_phase_08_hardening.py</files>
  <behavior>
    - "A complete READY matrix, closed named PASS checks, exact tracked-source bindings, sanitized build-surface facts, and an honest BOM blocker accept."
    - "A missing summary or verifier classifies BLOCKED_BY_UPSTREAM; a present verifier failure classifies FAILED; neither can be represented as READY or PASS."
    - "Missing/extra keys, digest drift, untracked authority input, omitted lock/resource entry, promoted requirement/gate/license/device/browser state, or private/credential-shaped value rejects."
    - "A tracked or built fixture containing a constructed credential signature rejects with only a stable rule ID and relative artifact class, never the matched bytes."
  </behavior>
  <action>Write mutation tests first against isolated temporary repository trees, then implement a standard-library-only verifier. Per D-01/D-02, require exact summary/executable pairs for Phase 2–7 and keep orchestration status separate from each phase verifier's semantic verdict. Per D-04, expose reusable scanners for tracked source/evidence/captures, web client output, and Release app bundle; scan byte patterns without decoding or echoing matches, reject private paths/identifiers in generated JSON, and verify the transaction/injection check inventory is present. Per D-05, parse exact tracked lock/resolution closures and resources, cross-check Phase 1 audit facts, and require an explicit BLOCKED decision for the proxy/root-license gap rather than inferring shipping permission. Per D-03/D-06/D-07/D-08/D-09, close all JSON keys/enums, permit only automated classifications, and reject formal gate promotion, requirement completion, public-submission state, live-service authority, or unsupported performance/license language.</action>
  <verify>
    <automated>python3 -m unittest tools.verify.tests.test_phase_08_hardening -v</automated>
  </verify>
  <done>The independent verifier accepts only a complete sanitized automated record and exact BOM; readiness, source, build, scan, inventory, privacy, and claim mutations fail with stable non-sensitive classifications.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate upstream checks and inspect the tracked and built shipping surfaces</name>
  <files>scripts/verify-phase-08-hardening, tools/verify/verify_phase_08_hardening.py, tools/verify/tests/test_phase_08_hardening.py</files>
  <action>Create an executable with `prerequisites`, `quick`, `full`, and `--verify-evidence` modes. Start with the exact summary/verifier readiness audit and stop before dependent work unless every required Phase 2–7 surface is READY. Quick mode invokes the authoritative quick verifiers for capture/replay, transactions/injection, targeting, replacement, removal, and B0; do not reproduce their semantic logic. Full mode runs their full checks, builds web and the existing iOS Release configuration in named temporary roots with telemetry/signing disabled, scans tracked files plus `.next` client output and the built `.app`, and cleans only those command-owned temporary roots. Require a free-space preflight with a stable environment failure instead of deleting caches. Record only check IDs, status, sanitized digest, and exact runtime/package identity. Per D-04/D-09, assert the shipping surface introduces no provider/model/tool/upload/deploy/delete/credential path or fifth operation; never change source, manifests, PBX, signing, or dependencies to satisfy the audit.</action>
  <verify>
    <automated>scripts/verify-phase-08-hardening prerequisites &amp;&amp; scripts/verify-phase-08-hardening quick &amp;&amp; python3 -m unittest tools.verify.tests.test_phase_08_hardening -v</automated>
  </verify>
  <done>Prerequisite and quick modes fail closed on missing or failing upstream authority, and the ready repository passes the complete named quick integration, adversarial, source, and privacy checks without mutating product configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the exact sprint BOM and automated hardening preflight</name>
  <files>scripts/verify-phase-08-hardening, evidence/hardening/phase-08/sprint-bom.json, evidence/hardening/phase-08/automated-preflight.json</files>
  <action>In full mode, stage both JSON artifacts under one command-owned temporary directory, derive a stable recorded revision/time from Git rather than wall-clock noise, independently validate every closed key, cross-link, digest, readiness row, scan result, and BOM member, then atomically replace each tracked artifact only after all automated checks pass. Run independent verification after publication. Per D-05/D-08, make the overall automated slice PASS while the BOM explicitly remains BLOCKED for shipping and OPS-LICENSE-001/GATE-011 remain trace-only pending due the missing root license/use decision. Per D-03/D-06/D-07, include no device/browser/human/submission result and publish no raw logs or private material.</action>
  <verify>
    <automated>scripts/verify-phase-08-hardening full &amp;&amp; cp evidence/hardening/phase-08/sprint-bom.json /tmp/reroom-phase-08-bom-first.json &amp;&amp; cp evidence/hardening/phase-08/automated-preflight.json /tmp/reroom-phase-08-preflight-first.json &amp;&amp; scripts/verify-phase-08-hardening full &amp;&amp; cmp /tmp/reroom-phase-08-bom-first.json evidence/hardening/phase-08/sprint-bom.json &amp;&amp; cmp /tmp/reroom-phase-08-preflight-first.json evidence/hardening/phase-08/automated-preflight.json &amp;&amp; scripts/verify-phase-08-hardening --verify-evidence</automated>
  </verify>
  <done>Two full runs publish identical independently verified sanitized artifacts; automated integration checks pass, the exact BOM reports the known shipping blocker, and no formal requirement/gate/device/browser/license claim is promoted.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest tools.verify.tests.test_phase_08_hardening -v`
- `scripts/verify-phase-08-hardening prerequisites`
- `scripts/verify-phase-08-hardening quick`
- `scripts/verify-phase-08-hardening full` twice with byte comparisons
- `scripts/verify-phase-08-hardening --verify-evidence`
- `git diff --check`
</verification>

<success_criteria>
- Upstream readiness is explicit and fail-closed; no missing phase is skipped.
- Existing authorities prove the integrated automated slice; new code owns orchestration/evidence shape only.
- Tracked and built outputs pass sanitized credential/authority-surface inspection.
- The exact BOM includes every tracked lock/resource input and retains the root-license/proxy redistribution blocker.
- `NFR-RESILIENCE-001`, `SEC-CREDENTIAL-001`, `SEC-AGENT-001`, `OPS-LICENSE-001`, and every referenced gate remain trace-only, not completed.
</success_criteria>

<output>
Create `.planning/phases/08-p0-hardening-and-evidence/08-01-SUMMARY.md` with `requirements-completed: []`; describe only trace coverage and actual automated results.
</output>
