---
phase: 03-typed-place-commit-and-offline-restore
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ReRoomContracts/Package.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionContractAdapter.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionContractTests.swift
  - fixtures/transactions/1.0.0/rev-001/manifest.json
  - fixtures/transactions/1.0.0/rev-001/cases.json
  - fixtures/transactions/1.0.0/rev-001/expected-traces.json
autonomous: true
requirements: [FR-PLACE-001, FR-RESTORE-001, FR-TRANSACTION-001, FR-AGENT-001]
must_haves:
  truths:
    - "One ReRoomTransactionCore product represents frozen CON-003/CON-005 values without changing either schema or inventing a parallel lifecycle."
    - "The operation inventory and its stable allowlist order are exactly place, replace, remove, restore; undo is only a presentation alias for restore."
    - "The primary writer identity remains the pair (revision_branch_id, authority_id), while transaction_id and txidem_ identity remain stable and distinct."
    - "Empty, missing, unknown, malformed, wrong-version, wrong-ID-family, and noncanonical contract input rejects before any reducer or storage mutation."
    - "One immutable fixture revision names valid place/restore traces and every required negative truth for all four Phase 3 requirements."
  prohibitions:
    - requirement_id: FR-TRANSACTION-001
      category: correctness
      status: unresolved
      verification: null
      statement: "MUST NOT edit, loosen, regenerate, or reinterpret the frozen CON-003/CON-005 schemas to make an implementation pass."
      reason: "Flagged-unverified descriptor-less prohibition recall; schema byte authority remains external to this plan."
    - requirement_id: FR-AGENT-001
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT add voice, model, provider, network, cloud, or a generic command dictionary to the deterministic transaction module."
      reason: "The approved sprint boundary requires one local typed/tap proposal contract and no deferred ingress work."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift
      provides: "Exact Sendable/Codable scene, transaction, projection, operation, confirmation, and receipt values"
    - path: fixtures/transactions/1.0.0/rev-001/manifest.json
      provides: "Externally pinned immutable transaction/revision fixture inventory"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionContractAdapter.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomContracts/ContractValidation.swift
      via: "Exact CON-003/CON-005 schema ID, version, digest, and JCS validation before semantic use"
      pattern: "ContractSchemaIdentifier\.(sceneState|transaction)|CanonicalJSON"
    - from: fixtures/transactions/1.0.0/rev-001/expected-traces.json
      to: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionContractTests.swift
      via: "Fixture bytes and expected branch/revision/rejection traces are immutable test oracle input"
      pattern: "expected-traces|fixture"
---

<objective>
Freeze the Phase 3 executable type vocabulary and immutable transaction/revision oracle before reducer or UI work begins.

Purpose: Make every later task implement the same exact schema, ID, operation, projection, and trace meanings while preserving the approved no-change identity decision.
Output: ReRoomTransactionCore package product, exact contract adapter/models, and a closed fixture revision.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-RESEARCH.md
@docs/contracts/scene-state.schema.json
@docs/contracts/transaction.schema.json
@docs/canonical/GLOSSARY_AND_ID_REGISTRY.md
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@ios/Packages/ReRoomContracts/Package.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CanonicalJSON.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomContracts/ContractValidation.swift

<interfaces>
Reuse `CanonicalJSON.canonicalize(jsonData:maximumBytes:maximumDepth:)`, `CanonicalJSON.digest`, `ContractSchemaIdentifier.sceneState`, `ContractSchemaIdentifier.transaction`, and `ContractValidator.validate(_:)`. ReRoomTransactionCore may depend on the existing ReRoomContracts and ReRoomCaptureCore targets, but adds no package dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the oracle and implement its exact transaction contract</name>
  <files>ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionContractAdapter.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionContractTests.swift, fixtures/transactions/1.0.0/rev-001/manifest.json, fixtures/transactions/1.0.0/rev-001/cases.json, fixtures/transactions/1.0.0/rev-001/expected-traces.json</files>
  <read_first>ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CanonicalJSON.swift, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/ContractValidation.swift, fixtures/contracts/1.0.0/rev-001/manifest.json, fixtures/contracts/1.0.0/rev-001/cases.json, docs/contracts/scene-state.schema.json, docs/contracts/transaction.schema.json, docs/contracts/README.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, .agents/skills/swift-testing-expert/SKILL.md</read_first>
  <behavior>
    - Valid minimal SceneState and every canonical transaction lifecycle shape validate with stable IDs and exact native authority/branch identity.
    - The case set covers stable place preview/commit/replay, restore, exact retry equality/conflict, malformed/empty/unknown input, operation ordering, authority/CAS, divergence, and typed ingress rejection truths.
    - Manifest membership is closed, lexicographically stable, byte-length/SHA-256 bound, and cannot regenerate its own expected output.
  </behavior>
  <action>Execute one complete RED→GREEN TDD task so its final command is actionable and cannot end with a missing/zero-test target. RED: add the immutable language-neutral JSON cases plus parameterized TransactionContractTests for every Phase 3 requirement and edge probe, temporarily register the focused test target against an intentionally absent ReRoomTransactionCore product, run the exact filtered command, confirm it discovers a nonzero test inventory and fails for the missing production contract, then create the atomic `test(03-01): add failing transaction contract oracle` commit. GREEN: add the static ReRoomTransactionCore library product/target with dependencies on existing ReRoomContracts and ReRoomCaptureCore only; define exact closed Sendable/Codable values for authority, branch, target context, intent, operations, validation, preview, explicit confirmation, commit, complete projection/inverse, reconciliation, SceneState, receipts, and typed failures; and implement the adapter through existing CanonicalJSON and frozen ContractValidator before typed decode and canonical round-trip equality. Bind exact fixture bytes and expected normalized traces in a closed manifest, use deterministic UUIDs/timestamps, preserve branch/authority as primary identity, and create the atomic `feat(03-01): implement exact transaction contract` commit only after the same final filtered command passes with a nonzero test count. Do not edit canonical schemas, duplicate canonicalization/validation/ID logic, or derive expected results from the implementation under test.</action>
  <verify>
    <automated>zsh -o pipefail -c 'trace=$(mktemp); swift test --package-path ios/Packages/ReRoomContracts --filter TransactionContractTests 2&gt;&amp;1 | tee "$trace" &amp;&amp; rg -q "Test run with [1-9][0-9]* tests passed" "$trace"'</automated>
  </verify>
  <done>Separate RED and GREEN commits exist; the immutable manifest pins every case byte/trace; the final filtered run discovers at least one test and all contract/fixture tests pass, including fail-closed malformed, empty, missing, unknown, duplicate, wrong-version, and wrong-ID-family inputs.</done>
</task>

</tasks>

<verification>
- `swift test --package-path ios/Packages/ReRoomContracts --filter TransactionContractTests`
- `swift package --package-path ios/Packages/ReRoomContracts describe` lists ReRoomTransactionCore without any new external dependency.
- `git diff --check` passes and frozen schema file digests remain unchanged.
</verification>

<success_criteria>
- One exact Swift transaction vocabulary and one immutable oracle cover every Phase 3 requirement.
- Closed input and identity/order edge probes reject deterministically without storage access.
- No canonical schema, dependency lock, voice/provider/cloud surface, or deferred gate status changes.
</success_criteria>

## Artifacts-this-phase-produces

- `ReRoomTransactionCore` SwiftPM product and focused test target.
- Frozen `fixtures/transactions/1.0.0/rev-001` corpus with externally pinned manifest and expected traces.
- Exact CON-003/CON-005 typed values and validation adapter shared by reducer, replay, native authority, and later consumers.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-01-SUMMARY.md` when done.
</output>
