---
phase: 03-typed-place-commit-and-offline-restore
plan: "06"
type: execute
wave: 6
depends_on: ["03-04", "03-05"]
files_modified:
  - ios/Packages/ReRoomContracts/Package.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/NormalizedTraceResult.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionTraceExporterTests/TransactionTraceExporterTests.swift
  - tools/javascript/src/transaction.mjs
  - tools/javascript/test/transaction.test.mjs
  - tools/python/reroom_verify/transaction.py
  - tools/python/tests/test_transaction.py
  - tools/verify/compare_transaction_traces.py
  - tools/verify/transaction-trace-result.schema.json
  - tools/verify/tests/test_phase_03_transactions.py
  - scripts/verify-phase-03-transactions
  - evidence/transactions/phase-03/automated-preflight.json
autonomous: true
requirements: [FR-PLACE-001, FR-RESTORE-001, FR-TRANSACTION-001, FR-AGENT-001]
must_haves:
  truths:
    - "Swift, JavaScript, and Python independently consume the same immutable fixture and agree exactly on place/restore projections, fingerprints, revisions, receipts, rejections, and divergence traces."
    - "Every runtime's closed trace includes accepted place and restore proposals, typed nonmutating replace/remove readiness blockers, restore proposal without pre-authorization, exact four-operation order, and injection rejection."
    - "Two isolated runs produce byte-identical normalized results; no runtime output becomes another runtime's oracle and expected bytes remain checked-in read-only inputs."
    - "One fail-closed Phase 3 command runs fixture integrity, focused Swift reducer/store/authority tests, JS/Python parity, injection rejection, deterministic rerun comparison, native model/UI smoke, Release build/surface, secret scan, and diff checks."
    - "Automated evidence may say the deterministic sprint slice passed while full GATE-009/GATE-010 campaigns, physical/device evidence, proxy qualification, and all deferred gates remain PENDING."
    - "The report contains stable IDs, hashes, counts, commands, revisions, and outcomes only—no raw room/user text, machine paths, device IDs, credentials, or fabricated human observations."
  prohibitions:
    - requirement_id: FR-TRANSACTION-001
      category: evidence
      status: unresolved
      verification: null
      statement: "MUST NOT regenerate the fixture oracle from any runtime, compare against dynamic HEAD, or normalize away a branch/revision/digest/rejection mismatch."
      reason: "Flagged-unverified descriptor-less prohibition recall; independent runtime parity must remain reproducible."
    - requirement_id: FR-AGENT-001
      category: claims
      status: unresolved
      verification: null
      statement: "MUST NOT mark GATE-009, GATE-010, GATE-011, physical evidence, or full P0 green from local deterministic automation."
      reason: "The human-approved sprint cut defers those complete campaigns without deleting them."
  artifacts:
    - path: scripts/verify-phase-03-transactions
      provides: "Single quick/full fail-closed Phase 3 verification entry point"
    - path: evidence/transactions/phase-03/automated-preflight.json
      provides: "Sanitized revision-bound automated sprint-slice result with pending-gate truth"
  key_links:
    - from: tools/javascript/src/transaction.mjs
      to: fixtures/transactions/1.0.0/rev-001/expected-traces.json
      via: "Independent local replay computes actual traces before comparison to immutable expected values"
      pattern: "transaction|expected-traces"
    - from: tools/python/reroom_verify/transaction.py
      to: fixtures/transactions/1.0.0/rev-001/expected-traces.json
      via: "Dependency-free independent reducer/validator over the same closed cases"
      pattern: "transaction|expected_traces"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift
      to: tools/verify/compare_transaction_traces.py
      via: "Swift executable emits the same schema-validated normalized trace contract twice before its output joins JavaScript/Python comparison"
      pattern: "ReRoomTransactionTraceExporter|normalized-trace-result"
    - from: scripts/verify-phase-03-transactions
      to: evidence/transactions/phase-03/automated-preflight.json
      via: "Atomic publication only after every declared check and sanitized report validation passes"
      pattern: "automated-preflight|phase-03"
---

<objective>
Prove the deterministic Phase 3 slice across three runtimes and publish one honest, revision-bound automated preflight.

Purpose: Prevent native-only semantic drift and give the sprint one fast fail-closed command without turning local automation into deferred gate or human evidence.
Output: Independent JavaScript/Python runners, strict comparator, verification script/tests, and sanitized automated report.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@fixtures/transactions/1.0.0/rev-001/manifest.json
@tools/javascript/src/replay.ts
@tools/python/reroom_verify/replay.py
@scripts/run-phase-02-replay-agreement
@scripts/verify-phase-02-capture-replay
@.planning/phases/03-typed-place-commit-and-offline-restore/03-04-SUMMARY.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Independently replay Phase 3 traces in JavaScript and Python</name>
  <files>tools/javascript/src/transaction.mjs, tools/javascript/test/transaction.test.mjs, tools/python/reroom_verify/transaction.py, tools/python/tests/test_transaction.py</files>
  <read_first>tools/javascript/src/canonical-json.mjs, tools/javascript/src/replay.ts, tools/python/reroom_verify/canonical_json.py, tools/python/reroom_verify/replay.py, fixtures/transactions/1.0.0/rev-001/manifest.json, docs/canonical/MASTER_TECHNICAL_SPEC.md</read_first>
  <behavior>
    - Both runtimes independently validate fixture integrity and compute exact request fingerprints, projections, touched sets, place/restore/retry/rejection/divergence revision traces.
    - Both traces include accepted place and restore proposals, typed nonmutating replace/remove readiness blockers, a restore proposal with no pre-authorization/commit authority, exact place/replace/remove/restore operation order, and injection rejection.
    - Every edge-case mutation produces the expected stable rejection without importing Swift output or trusting expected values during computation.
    - Two clean runs per runtime emit byte-identical normalized closed results.
  </behavior>
  <action>Write failing Node and Python tests, then implement dependency-free transaction fixture consumers using each runtime's existing strict JSON/JCS/schema loading conventions. Independently reproduce the closed Phase 3 semantic trace algorithm needed by the fixture: accepted context-bound place proposal, typed nonmutating replace/remove readiness blockers, restore proposal without pre-authorized compensation/confirmation/commit, exact place/replace/remove/restore order, fingerprint scope, preview no-op, place projection, same-key equality/conflict, accepted restore and touched-ID rebase/preservation, stale/wrong authority, injection rejection, and divergence freeze. Consult expected traces only after computation and reject missing/extra/out-of-order cases or source-manifest drift. Add no package and do not make either runtime depend on Swift or the other runtime.</action>
  <verify>
    <automated>node --test tools/javascript/test/transaction.test.mjs &amp;&amp; PYTHONPATH=tools/python python3 -m unittest tools.python.tests.test_transaction</automated>
  </verify>
  <done>Node and Python each pass the complete immutable fixture and mutation corpus twice with byte-identical local results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit Swift traces, compare three closed results, and enforce the Phase 3 command</name>
  <files>ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/NormalizedTraceResult.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionTraceExporterTests/TransactionTraceExporterTests.swift, tools/verify/transaction-trace-result.schema.json, tools/verify/compare_transaction_traces.py, tools/verify/tests/test_phase_03_transactions.py, scripts/verify-phase-03-transactions</files>
  <read_first>tools/verify/compare_replay_reports.py, tools/verify/tests/test_replay_agreement.py, scripts/run-phase-02-replay-agreement, scripts/verify-phase-02-capture-replay, ios/Packages/ReRoomContracts/Package.swift</read_first>
  <behavior>
    - A repository Swift executable product consumes the immutable revision and emits one machine-readable normalized result with runtime identity, implementation/fixture revisions, closed ordered case IDs, and exact trace payloads.
    - Swift output includes accepted place/restore proposals, typed nonmutating replace/remove blockers, restore proposal without pre-authorization, exact operation order, and injection rejection; two isolated invocations are byte-identical.
    - Comparator rejects runtime identity/revision/oracle/case-set/order/revision/fingerprint/projection/rejection drift and accepts only exact closed agreement.
    - Quick mode runs fixture integrity, focused transaction tests, JS/Python tests, and parity; full mode additionally runs app model/UI, Debug/Release, release-surface, secret, and whitespace checks.
    - Script failure publishes no passing report and exposes one stable check/rejection class without private paths or payloads.
  </behavior>
  <action>Register the `ReRoomTransactionTraceExporter` executable product/target plus a focused test target in Package.swift; its production source imports ReRoomTransactionCore, validates the immutable fixture revision, computes actual results without consulting expected traces until comparison, and emits the strict `transaction-trace-result.schema.json` contract with closed fields for runtime identity/version, implementation revision, fixture manifest digest/revision, ordered case IDs, and normalized trace values. The Swift trace must explicitly cover accepted place/restore proposals, nonmutating typed replace/remove blockers, restore proposal without pre-authorization, exact operation order, and injection rejection. Write RED exporter/schema tests before implementation. Implement a strict bounded comparator and mutation tests by adapting the independent-first Phase 1/2 pattern. Make the script create fresh exclusive temporary outputs, invoke `swift run --package-path ios/Packages/ReRoomContracts ReRoomTransactionTraceExporter` twice into separate files, require byte identity, then wire the validated Swift output together with two isolated Node and Python outputs into compare_transaction_traces.py. Bind every runner to the exact implementation/source-tree and fixture manifest digest, require complete exact trace agreement, and cleanly separate quick/full checks. Full mode must invoke the existing native scheme and release-surface verifier without rebuilding a substitute Release app. Include tracked-file secret scanning and `git diff --check`; preserve user dirty/untracked build/config/signing files and report their existence without staging them.</action>
  <verify>
    <automated>python3 -m unittest tools.verify.tests.test_phase_03_transactions &amp;&amp; scripts/verify-phase-03-transactions quick</automated>
  </verify>
  <done>The Swift executable/test product emits the schema-valid closed result twice byte-identically; comparator mutation tests kill every drift class; and quick verification passes only when Swift, JavaScript, and Python include every required proposal/blocker/order/injection case and agree exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Publish sanitized automated sprint evidence with gates pending</name>
  <files>evidence/transactions/phase-03/automated-preflight.json, tools/verify/tests/test_phase_03_transactions.py, scripts/verify-phase-03-transactions</files>
  <read_first>.planning/SPRINT-CUT-36H.md, evidence/capture/phase-02/automated-preflight.json, evidence/templates/gate-report.schema.json, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md</read_first>
  <action>After the authoritative full command passes, atomically publish one closed report labeled `automated sprint slice passed`. Record implementation revision, fixture/oracle/source digests, runtime identities, exact declared check set, counts, and sanitized outcomes. Explicitly list GATE-009 and GATE-010 full campaigns plus physical/device/proxy/license evidence as PENDING under the approved sprint cut. Add tests that reject GREEN/full-P0 language, missing digests/checks, raw user/room content, absolute machine paths, device/user identifiers, credentials, or fabricated observations. If any full check fails, leave prior evidence untouched or emit an honest RED/RUNNING report per the schema—never a partial pass.</action>
  <verify>
    <automated>scripts/verify-phase-03-transactions full</automated>
  </verify>
  <done>The report is reproducible, sanitized, bound to the passing full run, and cannot be mistaken for a completed physical/human gate or full P0 decision.</done>
</task>

</tasks>

<verification>
- `scripts/verify-phase-03-transactions quick` passes from clean temporary result roots.
- `scripts/verify-phase-03-transactions full` passes the declared package, three-runtime, app, Release, secret, and whitespace checks.
- Evidence validation confirms deferred gates remain PENDING and no private/raw evidence is representable.
</verification>

<success_criteria>
- Swift/JavaScript/Python exact transaction and restore traces agree independently and repeatably.
- One command fails closed on schema, semantics, injection, durability, UI, release, parity, secret, or source drift.
- Automated evidence is useful for the demo sprint and incapable of overstating GATE-009/GATE-010/full-P0 status.
</success_criteria>

## Artifacts-this-phase-produces

- Independent JavaScript and Python transaction/revision trace runners and tests.
- Closed three-runtime comparator plus quick/full Phase 3 verification command.
- Sanitized revision-bound automated preflight with explicit deferred-gate status.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-06-SUMMARY.md` when done.
</output>
