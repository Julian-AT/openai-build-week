---
phase: 03-typed-place-commit-and-offline-restore
plan: "06"
type: execute
wave: 6
depends_on: ["03-04", "03-05"]
files_modified:
  - ios/Packages/ReRoomContracts/Package.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/NormalizedTraceResult.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionTraceExporterTests/TransactionTraceExporterTests.swift
  - tools/javascript/src/transaction.ts
  - tools/javascript/test/transaction.test.mjs
  - tools/python/reroom_verify/transaction.py
  - tools/python/tests/test_transaction.py
autonomous: true
requirements: [FR-PLACE-001, FR-RESTORE-001, FR-TRANSACTION-001, FR-AGENT-001]
must_haves:
  truths:
    - "Swift, TypeScript, and Python independently consume the same immutable fixture and emit exact normalized place/restore projections, fingerprints, revisions, receipts, rejections, and divergence traces."
    - "Every runtime explicitly emits the accepted place proposal, accepted restore proposal without pre-authorized commit authority, typed nonmutating replace/remove readiness blockers, exact place/replace/remove/restore order, and injection rejection."
    - "Each runtime validates the pinned fixture manifest, binds its exact implementation revision and closed source-tree digest, and emits the same closed ordered case IDs without consulting another runtime's output."
    - "The TypeScript producer follows the existing executable Node TypeScript pattern, rejects any runtime except Node v22.22.3, and adds no package or transpiler dependency."
    - "Two isolated local runs of each producer are byte-identical before any cross-runtime comparison occurs."
  prohibitions:
    - requirement_id: FR-TRANSACTION-001
      category: evidence
      status: unresolved
      verification: null
      statement: "MUST NOT regenerate the fixture oracle from a runtime, compare against dynamic HEAD, or normalize away a branch, revision, digest, rejection, case-set, or order mismatch."
      reason: "Independent runtime parity must remain reproducible and oracle-independent."
    - requirement_id: FR-AGENT-001
      category: authority
      status: unresolved
      verification: null
      statement: "MUST NOT let any runtime trace represent a proposal, blocker, ordering record, or rejected injection as canonical confirmation or commit authority."
      reason: "Typed intent remains nonmutating until deterministic validation and explicit native confirmation authorize the transaction."
  artifacts:
    - path: tools/javascript/src/transaction.ts
      provides: "Executable exact-Node TypeScript transaction trace producer"
    - path: tools/python/reroom_verify/transaction.py
      provides: "Dependency-free Python transaction trace producer"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift
      provides: "Swift transaction-core trace exporter"
  key_links:
    - from: tools/javascript/test/transaction.test.mjs
      to: tools/javascript/src/transaction.ts
      via: "Node v22.22.3 directly imports and executes repository TypeScript like replay.test.mjs imports replay.ts"
      pattern: 'transaction\.ts|EXACT_NODE_VERSION'
    - from: tools/javascript/src/transaction.ts
      to: fixtures/transactions/1.0.0/rev-001/expected-traces.json
      via: "Independent computation precedes comparison to the immutable expected trace corpus"
      pattern: "transaction|expected-traces"
    - from: tools/python/reroom_verify/transaction.py
      to: fixtures/transactions/1.0.0/rev-001/expected-traces.json
      via: "Independent dependency-free reduction precedes oracle comparison"
      pattern: "transaction|expected_traces"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      via: "Exporter exercises the shipping Swift contract/reducer and emits normalized traces"
      pattern: "ReRoomTransactionCore|NativeBranchAuthority|NormalizedTraceResult"
---

<objective>
Produce independent, byte-deterministic Swift, TypeScript, and Python transaction traces for the complete Phase 3 corpus.

Purpose: Satisfy the repository's three-language golden-vector requirement before comparison or evidence orchestration can hide a producer defect.
Output: Three tested normalized trace producers bound to exact runtime, source, fixture, and implementation provenance.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@fixtures/transactions/1.0.0/rev-001/manifest.json
@tools/javascript/src/replay.ts
@tools/javascript/test/replay.test.mjs
@tools/python/reroom_verify/replay.py
@scripts/run-phase-02-replay-agreement
@.planning/phases/03-typed-place-commit-and-offline-restore/03-04-SUMMARY.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit exact TypeScript and Python transaction traces</name>
  <files>tools/javascript/src/transaction.ts, tools/javascript/test/transaction.test.mjs, tools/python/reroom_verify/transaction.py, tools/python/tests/test_transaction.py</files>
  <read_first>tools/javascript/src/replay.ts, tools/javascript/test/replay.test.mjs, tools/javascript/src/canonical-json.mjs, tools/python/reroom_verify/canonical_json.py, tools/python/reroom_verify/replay.py, fixtures/transactions/1.0.0/rev-001/manifest.json, docs/canonical/MASTER_TECHNICAL_SPEC.md</read_first>
  <behavior>
    - Both runtimes independently validate fixture integrity and compute exact request fingerprints, projections, touched sets, place/restore/retry/rejection/divergence revision traces.
    - Both results explicitly contain accepted place and restore proposals, typed nonmutating replace/remove readiness blockers, restore without pre-authorized confirmation/commit authority, exact place/replace/remove/restore order, and injection rejection.
    - Every result carries the pinned fixture manifest digest/revision plus exact implementation revision and closed producer-source digest; missing, dynamic, or mismatched provenance rejects before output publication.
    - TypeScript rejects any runtime except Node v22.22.3 and runs directly as `node tools/javascript/src/transaction.ts`; its `.mjs` test imports `../src/transaction.ts` using the established replay.ts pattern.
    - Two clean runs per runtime emit byte-identical normalized closed results and no runtime imports Swift or the other producer.
  </behavior>
  <action>Write failing Node and Python tests first. Implement `tools/javascript/src/transaction.ts` as an executable TypeScript module following `replay.ts`: export an `EXACT_NODE_VERSION` equal to `v22.22.3`, expose a testable producer function, parse a closed CLI only under the direct-entry guard, use native Node TypeScript execution, and add no package, loader, transpiler, or generated JavaScript copy. Implement the Python producer with the repository's dependency-free module pattern. In both producers, independently reproduce the immutable Phase 3 semantic trace algorithm: context-bound place proposal, typed nonmutating replace/remove blockers, restore proposal without pre-authorization/confirmation/commit authority, exact place/replace/remove/restore order, fingerprint scope, preview no-op, place projection, same-key equality/conflict, accepted restore with touched-ID rebase/preservation, stale/wrong authority, injection rejection, and divergence freeze. Compute actual traces before reading expected values; reject missing/extra/out-of-order cases and bind output to the pinned manifest digest/revision, caller-supplied exact `git:<40-hex>` implementation revision, and a deterministic digest of the declared closed producer source set.</action>
  <verify>
    <automated>node --test tools/javascript/test/transaction.test.mjs &amp;&amp; PYTHONPATH=tools/python python3 -m unittest tools.python.tests.test_transaction</automated>
  </verify>
  <done>Exact Node TypeScript and Python each pass the complete immutable proposal/blocker/order/injection and transaction mutation corpus twice with byte-identical, provenance-bound results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit normalized Swift transaction traces from the shipping core</name>
  <files>ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/NormalizedTraceResult.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionTraceExporter/main.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionTraceExporterTests/TransactionTraceExporterTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift, fixtures/transactions/1.0.0/rev-001/manifest.json</read_first>
  <behavior>
    - A repository Swift executable product imports ReRoomTransactionCore, consumes the immutable fixture revision, and emits one closed normalized result with runtime identity, implementation revision, fixture digest/revision, source digest, ordered case IDs, and exact trace payloads.
    - Swift explicitly emits accepted place and restore proposals, typed nonmutating replace/remove blockers, restore without pre-authorized confirmation/commit authority, exact operation order, and injection rejection.
    - Swift computes actual reducer/store/authority results before consulting expected traces, and mutation tests reject every provenance, order, projection, revision, fingerprint, rejection, and authority drift.
    - Two isolated executable invocations emit byte-identical canonical bytes.
  </behavior>
  <action>Register the `ReRoomTransactionTraceExporter` executable product/target and its focused Swift Testing target in Package.swift without adding a dependency. Write RED exporter tests before implementation. Define a closed `NormalizedTraceResult` value and executable CLI that import ReRoomTransactionCore, validate the pinned fixture manifest, compute the complete actual trace through shipping reducers/authority helpers, and compare to immutable expected values only after computation. Require the same explicit proposal/blocker/order/injection cases as TypeScript and Python. Emit canonical bytes bound to exact runtime identity, exact caller-supplied implementation revision, fixture manifest digest/revision, and a deterministic digest over the declared Swift exporter/core source set; reject a missing/extra/out-of-order case or any binding drift. Invoke the built executable twice in tests and require byte equality without importing TypeScript/Python output.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter TransactionTraceExporterTests</automated>
  </verify>
  <done>The Swift executable/test product independently emits the complete closed proposal/blocker/order/injection and transaction corpus twice byte-identically with exact source, fixture, revision, and runtime provenance.</done>
</task>

</tasks>

<verification>
- `node --test tools/javascript/test/transaction.test.mjs` passes under exact Node v22.22.3 and directly exercises `transaction.ts`.
- `PYTHONPATH=tools/python python3 -m unittest tools.python.tests.test_transaction` passes without external packages.
- `swift test --package-path ios/Packages/ReRoomContracts --filter TransactionTraceExporterTests` passes and runs the executable twice.
- Source checks confirm all three runtimes explicitly cover accepted place/restore proposals, typed replace/remove blockers, exact operation order, and injection rejection with exact provenance binding.
</verification>

<success_criteria>
- Swift, TypeScript, and Python each independently emit the complete immutable Phase 3 transaction trace corpus.
- Every producer is byte-deterministic and bound to exact runtime/source/fixture/implementation provenance.
- No producer depends on another producer's output or introduces a new dependency.
</success_criteria>

## Artifacts-this-phase-produces

- Executable TypeScript transaction/revision trace producer and Node tests.
- Dependency-free Python transaction/revision trace producer and tests.
- Swift executable transaction trace exporter and focused tests.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-06-SUMMARY.md` when done.
</output>
