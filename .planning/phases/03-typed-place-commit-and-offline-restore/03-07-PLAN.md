---
phase: 03-typed-place-commit-and-offline-restore
plan: "07"
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - tools/verify/transaction-trace-result.schema.json
  - tools/verify/compare_transaction_traces.py
  - tools/verify/tests/test_phase_03_transactions.py
  - scripts/verify-phase-03-transactions
  - evidence/transactions/phase-03/automated-preflight.json
autonomous: true
requirements: [FR-PLACE-001, FR-RESTORE-001, FR-TRANSACTION-001, FR-AGENT-001]
must_haves:
  truths:
    - "A closed bounded schema and independent comparator accept only exact Swift/TypeScript/Python agreement over source, fixture, implementation, runtime, case order, projections, fingerprints, revisions, receipts, rejections, and divergence."
    - "The comparator explicitly requires accepted place and restore proposals, typed nonmutating replace/remove readiness blockers, restore without pre-authorized commit authority, exact place/replace/remove/restore order, and injection rejection from every runtime."
    - "The orchestration command pins Node v22.22.3, runs all three producers twice in isolated fresh roots, requires per-runtime byte identity before comparison, and never treats one runtime as another's oracle."
    - "Quick mode runs fixture integrity, focused Swift transaction tests, TypeScript/Python producer tests, two-run producer checks, and parity; full mode additionally runs native model/UI, Debug/Release, release-surface, tracked-file secret scan, and whitespace checks."
    - "Automated evidence may say the deterministic sprint slice passed while full GATE-009/GATE-010 campaigns, physical/device evidence, proxy qualification, and all deferred gates remain PENDING."
    - "The report contains stable IDs, hashes, counts, commands, revisions, and outcomes only—no raw room/user text, machine paths, device IDs, credentials, or fabricated human observations."
  prohibitions:
    - requirement_id: FR-TRANSACTION-001
      category: evidence
      status: unresolved
      verification: null
      statement: "MUST NOT accept dynamic HEAD, regenerate expected bytes, omit source/fixture/revision digest binding, or normalize away a branch, order, projection, fingerprint, revision, rejection, or divergence mismatch."
      reason: "Cross-runtime evidence must fail closed and remain reproducible."
    - requirement_id: FR-AGENT-001
      category: claims
      status: unresolved
      verification: null
      statement: "MUST NOT mark GATE-009, GATE-010, GATE-011, physical evidence, or full P0 green from local deterministic automation."
      reason: "The human-approved sprint cut defers those complete campaigns without deleting them."
  artifacts:
    - path: tools/verify/transaction-trace-result.schema.json
      provides: "Closed normalized three-runtime result contract"
    - path: tools/verify/compare_transaction_traces.py
      provides: "Bounded exact transaction trace comparator"
    - path: scripts/verify-phase-03-transactions
      provides: "Single quick/full fail-closed Phase 3 verification entry point"
    - path: evidence/transactions/phase-03/automated-preflight.json
      provides: "Sanitized revision-bound automated sprint-slice result with pending-gate truth"
  key_links:
    - from: tools/verify/compare_transaction_traces.py
      to: tools/verify/transaction-trace-result.schema.json
      via: "Every producer result validates against the closed schema before semantic normalization or agreement"
      pattern: "transaction-trace-result|schema"
    - from: scripts/verify-phase-03-transactions
      to: tools/javascript/src/transaction.ts
      via: "Exact Node v22.22.3 invokes the executable TypeScript producer twice with pinned provenance"
      pattern: 'node|transaction\.ts|v22\.22\.3'
    - from: scripts/verify-phase-03-transactions
      to: tools/verify/compare_transaction_traces.py
      via: "Two byte-identical outputs per runtime enter one independent exact comparator"
      pattern: "compare_transaction_traces|swift|typescript|python"
    - from: scripts/verify-phase-03-transactions
      to: evidence/transactions/phase-03/automated-preflight.json
      via: "Atomic publication occurs only after every declared full check and sanitized report validation passes"
      pattern: "automated-preflight|phase-03"
---

<objective>
Compare the three independent Phase 3 traces, orchestrate deterministic reruns, and publish one honest revision-bound automated preflight.

Purpose: Detect any runtime or provenance drift and give the sprint one fail-closed verification command without turning host automation into deferred gate or human evidence.
Output: Closed result schema, strict comparator, quick/full verification command, mutation tests, and sanitized automated report.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@fixtures/transactions/1.0.0/rev-001/manifest.json
@tools/verify/compare_replay_reports.py
@tools/verify/tests/test_replay_agreement.py
@scripts/run-phase-02-replay-agreement
@scripts/verify-phase-02-capture-replay
@evidence/capture/phase-02/automated-preflight.json
@.planning/phases/03-typed-place-commit-and-offline-restore/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject every closed-schema and three-runtime semantic drift</name>
  <files>tools/verify/transaction-trace-result.schema.json, tools/verify/compare_transaction_traces.py, tools/verify/tests/test_phase_03_transactions.py</files>
  <read_first>tools/verify/replay-report.schema.json, tools/verify/compare_replay_reports.py, tools/verify/tests/test_replay_agreement.py, fixtures/transactions/1.0.0/rev-001/manifest.json, .planning/phases/03-typed-place-commit-and-offline-restore/03-06-SUMMARY.md</read_first>
  <behavior>
    - Schema rejects unknown/missing fields, oversized input, wrong runtime/version, dynamic or malformed implementation revision, source/fixture digest drift, and incomplete/duplicate/out-of-order case sets.
    - Comparator requires all three independent runtimes and exact equality for projections, fingerprints, revisions, receipts, rejections, divergence, runtime identity, implementation revision, fixture manifest digest/revision, and closed source digest.
    - Comparator explicitly requires accepted place and restore proposals, typed nonmutating replace/remove blockers, restore without pre-authorized confirmation/commit authority, exact place/replace/remove/restore order, and injection rejection in every runtime.
    - Mutation tests kill each schema/provenance/case/order/proposal/blocker/injection/transaction drift independently and prove no runtime result defines the expected value for another.
  </behavior>
  <action>Write RED mutation tests, then define a strict bounded JSON Schema for the normalized producer output and implement `compare_transaction_traces.py` by adapting the repository's independent-first Phase 1/2 comparator pattern. Validate each raw result against the immutable schema before normalization. Require exactly Swift, TypeScript on Node v22.22.3, and Python identities; one exact caller-supplied implementation revision; one pinned fixture revision/manifest digest; each declared closed source-tree digest; one complete ordered case set; and exact normalized trace equality. Add explicit structural assertions for the accepted place proposal, accepted restore proposal without pre-authorized authority, typed nonmutating replace/remove blockers, exact four-operation order, and rejected injection so equal omissions cannot pass. Reject unknown fields, input above bounds, duplicate/missing cases, dynamic HEAD, and every semantic/provenance mutation with a stable sanitized rejection class.</action>
  <verify>
    <automated>python3 -m unittest tools.verify.tests.test_phase_03_transactions</automated>
  </verify>
  <done>Comparator mutation tests kill every schema, binding, proposal, blocker, operation-order, injection, transaction, and runtime disagreement while accepting only the complete exact three-runtime corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate two-run agreement and publish pending-gate evidence</name>
  <files>scripts/verify-phase-03-transactions, tools/verify/tests/test_phase_03_transactions.py, evidence/transactions/phase-03/automated-preflight.json</files>
  <read_first>scripts/run-phase-02-replay-agreement, scripts/verify-phase-02-capture-replay, evidence/capture/phase-02/automated-preflight.json, evidence/templates/gate-report.schema.json, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, .planning/milestones/v1.0/SPRINT-CUT-36H.md</read_first>
  <behavior>
    - Script resolves and rejects any Node version except exact v22.22.3, invokes `node tools/javascript/src/transaction.ts`, the Python module, and the built Swift exporter twice each in fresh exclusive temporary roots, and requires per-runtime byte identity before comparison.
    - Every invocation receives the same exact implementation revision, pinned fixture manifest/revision digest, and declared closed source-tree binding; the comparator receives no dynamic HEAD or unbound output.
    - Quick mode runs fixture integrity, focused Swift reducer/store/authority/exporter tests, TypeScript/Python tests, two-run identity, and three-runtime comparison.
    - Full mode additionally runs RoomEditModel/UI automation, Debug/Release builds, existing release-surface verification, tracked-file secret scan, and `git diff --check` without staging user dirty/untracked files.
    - Only a complete passing full run atomically publishes a sanitized report; GATE-009/GATE-010 campaigns and physical/device/proxy/license evidence remain explicitly PENDING.
  </behavior>
  <action>Write failing orchestration/evidence tests, then implement a quick/full Python or shell entry point consistent with the existing Phase 2 scripts. Resolve the exact Node binary and require `v22.22.3`; build the Swift exporter once, create fresh exclusive temporary locations, invoke each producer twice with the same pinned fixture, `git:<40-lowercase-hex>` implementation revision, and source-set digest contract, and require byte identity before calling the comparator. Quick mode runs immutable fixture checks plus focused Swift transaction/exporter, direct TypeScript, Python, and agreement tests. Full mode additionally invokes the existing native scheme model/UI tests, Debug and Release products, and release-surface verifier, then performs a tracked-file secret scan and `git diff --check`; it must preserve and report user dirty/untracked build/config/signing files without staging them. After the authoritative full command passes, atomically publish a closed `automated sprint slice passed` report recording exact runtime identities, implementation/fixture/oracle/source digests, declared checks, counts, and sanitized outcomes. Tests reject GREEN/full-P0 language, missing bindings/checks, raw user/room content, absolute machine paths, device/user identifiers, credentials, fabricated observations, partial publication, and equal omission of any proposal/blocker/order/injection case.</action>
  <verify>
    <automated>scripts/verify-phase-03-transactions quick &amp;&amp; scripts/verify-phase-03-transactions full</automated>
  </verify>
  <done>Quick/full verification fails closed on any producer, binding, parity, UI, Release, secret, or whitespace failure; a complete full pass publishes only sanitized sprint-slice evidence with every deferred gate still PENDING.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest tools.verify.tests.test_phase_03_transactions` kills all comparator and orchestration mutations.
- `scripts/verify-phase-03-transactions quick` passes from isolated producer roots with exact Node v22.22.3.
- `scripts/verify-phase-03-transactions full` passes the declared package, three-runtime, app, Release, secret, and whitespace checks.
- Evidence validation confirms all proposal/blocker/order/injection cases are present, source/fixture/revision digests are bound, deferred gates remain PENDING, and no private/raw evidence is representable.
</verification>

<success_criteria>
- Swift/TypeScript/Python exact transaction and restore traces agree independently and repeatably across two runs each.
- One command fails closed on schema, semantics, proposal safety, injection, durability, UI, release, parity, secret, or provenance drift.
- Automated evidence is useful for the demo sprint and incapable of overstating GATE-009/GATE-010/full-P0 status.
</success_criteria>

## Artifacts-this-phase-produces

- Closed three-runtime transaction trace schema and fail-closed comparator.
- Quick/full Phase 3 agreement and release verification command.
- Sanitized revision-bound automated preflight with explicit deferred-gate status.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-07-SUMMARY.md` when done.
</output>
