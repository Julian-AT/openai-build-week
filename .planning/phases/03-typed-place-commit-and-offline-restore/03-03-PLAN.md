---
phase: 03-typed-place-commit-and-offline-restore
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/EditProjectionTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/RestoreReducerTests.swift
autonomous: true
requirements: [FR-RESTORE-001, FR-TRANSACTION-001]
must_haves:
  truths:
    - "RR-EDIT-PROJECTION-1 is complete, lexicographically ordered, unique, hash-exact, and excludes revision/tracking/readiness/surface/history/timestamp envelopes."
    - "Restore selects only the latest eligible uncompensated branch edit and verifies its immutable captured-exact inverse, source hashes, ordered touched IDs, branch/world identity, required artifacts, and current touched-value equality."
    - "RR-RESTORE-REBASE-1 changes only verified touched IDs atop the current complete projection; new/unaffected edit state and all excluded live semantic evidence survive byte-for-byte."
    - "Restore creates a fresh compensating transaction and inverse at pending r+1 while leaving the source transaction/history immutable and requiring zero network reads."
    - "No eligible source, corrupt inverse, later edit, drift, dangling support, branch/world mismatch, or artifact-union mismatch rejects non-destructively."
  prohibitions:
    - requirement_id: FR-RESTORE-001
      category: history
      status: unresolved
      verification: null
      statement: "MUST NOT rewind a whole SceneState, change the original transaction state, delete new/unaffected IDs, or copy a historical scene revision/digest."
      reason: "Flagged-unverified descriptor-less prohibition recall; restore is a fresh projection-scoped compensation."
    - requirement_id: FR-TRANSACTION-001
      category: integrity
      status: unresolved
      verification: null
      statement: "MUST NOT apply an inverse whose touched IDs, source operations, current before-values, branch/world identity, hashes, or artifact union do not match exactly."
      reason: "Every inverse mismatch must fail the entire transaction without partial mutation."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
      provides: "Exact projection build/digest/diff/apply and semantic invariant validation"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
      provides: "Pure latest-eligible RR-RESTORE-REBASE-1 compensating reduction"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
      via: "Source inverse diff derives touched sets; current projection supplies preserved values"
      pattern: "RR-RESTORE-REBASE-1|touched"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
      to: docs/contracts/transaction.schema.json
      via: "Exact projection/hash/origin/derivation and artifact-union shape"
      pattern: "RR-EDIT-PROJECTION-1|projection_sha256"
---

<objective>
Implement exact edit projection semantics and offline restore as a fresh compensating transaction.

Purpose: Make undo safe under newer tracking/readiness/edit state and prove it never rewinds immutable history or the whole scene document.
Output: Projection engine, restore reducer, and exhaustive drift/eligibility/preservation tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-RESEARCH.md
@docs/adr/ADR-012-transaction-and-offline-restore.md
@docs/contracts/scene-state.schema.json
@docs/contracts/transaction.schema.json
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/canonical/GLOSSARY_AND_ID_REGISTRY.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and verify complete edit projections and touched-ID diffs</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/EditProjectionTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift, docs/contracts/scene-state.schema.json, docs/contracts/transaction.schema.json, docs/canonical/GLOSSARY_AND_ID_REGISTRY.md, fixtures/transactions/1.0.0/rev-001/expected-traces.json</read_first>
  <behavior>
    - Projection construction includes every object edit state, placed asset with manifest reference, and asset-subject support relation in unique lexicographic stable-ID order.
    - Projection digest covers exactly the projection member; provenance/derivation metadata is excluded while branch/world/edit content is included.
    - Diff returns exact sorted object/asset/support IDs and validates them against the source transaction's ordered operations.
    - Dangling, duplicate, unsorted, incomplete, stale artifact, wrong-world/branch, or unexpected current-value input rejects without output.
  </behavior>
  <action>Write RED parameterized tests from TST-TX-002/TST-DIGEST-001, then implement pure projection build, semantic validation, exact JCS digest, stable-ID map/diff, required-artifact union, and touched-value application helpers. Projection construction must preserve the closed exclusion boundary and validate placed-asset/support referential integrity. Do not use a generic JSON patch, renderer/provider identity, whole-scene equality, or mutable global state.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter EditProjectionTests</automated>
  </verify>
  <done>Projection and touched-set vectors match the fixture exactly; every ordering/reference/hash/branch/world negative rejects before applying any value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce latest-eligible restore with current-complete rebase</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/RestoreReducerTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift, docs/adr/ADR-012-transaction-and-offline-restore.md, docs/canonical/MASTER_TECHNICAL_SPEC.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md</read_first>
  <behavior>
    - Latest eligible source plus valid inverse yields a restore proposal whose before equals current complete projection and whose after is the exact rebase result.
    - A newly tracked object, changed readiness, new unaffected asset/support, surfaces, labels, tracking, and timestamps survive according to their canonical ownership.
    - One touched-value drift, corrupt hash, wrong source, later uncompensated edit, prior compensation, branch/world mismatch, or missing artifact rejects with unchanged input bytes.
    - The source remains committed/immutable; the new restore contains compensates_transaction_id, fresh inverse, appended history candidate, and pending one-step revision.
  </behavior>
  <action>Implement the pure restore reducer from the canonical algorithm in order: choose the latest eligible uncompensated edit; validate source transaction and captured-exact inverse; derive and operation-verify sorted touched sets; require current touched values/presence equal source inverse before-values; apply only source inverse after-values atop the current complete projection; build restore_rebase derivation/source hashes; preserve all excluded SceneState members; and produce one new restore_snapshot inverse for the compensating transaction. Use deterministic local inputs only and assert zero network/provider access. Include a fixture where a new object/readiness update appears between place and restore and one where a touched value drifts.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter RestoreReducerTests</automated>
  </verify>
  <done>Offline restore returns the exact fresh projection/transaction trace and all corruption, drift, eligibility, ordering, and whole-document-rewind attempts reject non-destructively.</done>
</task>

</tasks>

<verification>
- `swift test --package-path ios/Packages/ReRoomContracts --filter EditProjectionTests`
- `swift test --package-path ios/Packages/ReRoomContracts --filter RestoreReducerTests`
- Tests assert source transaction bytes and every unrelated SceneState field remain unchanged.
</verification>

<success_criteria>
- FR-RESTORE-001 exact inverse/latest-eligible/touched rebase/offline/history truths pass.
- FR-TRANSACTION-001 projection ordering, digest, referential integrity, and fail-closed semantics pass.
- No full-document rollback or source-history mutation path is representable.
</success_criteria>

## Artifacts-this-phase-produces

- RR-EDIT-PROJECTION-1 construction, digest, diff, artifact-union, and semantic validation.
- RR-RESTORE-REBASE-1 pure reducer and fresh compensating inverse creation.
- Preservation, drift, eligibility, ordering, corruption, and offline restore regression corpus.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-03-SUMMARY.md` when done.
</output>
