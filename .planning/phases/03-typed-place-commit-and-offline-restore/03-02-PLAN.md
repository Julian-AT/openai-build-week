---
phase: 03-typed-place-commit-and-offline-restore
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionFingerprint.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/IntentBoundaryTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/PlaceReducerTests.swift
autonomous: true
requirements: [FR-PLACE-001, FR-TRANSACTION-001, FR-AGENT-001]
must_haves:
  truths:
    - "Tap and typed ingress produce the same context-bound, nonmutating proposal type in stable operation order and neither can confirm or commit."
    - "Duplicate keys, unknown/empty/oversized fields, stale context, transforms, URLs, confirmation, authority, revision outcomes, target/session substitution, and new tool names reject with an unchanged state digest."
    - "The request fingerprint hashes exactly schema_version, session_id, revision_authority, base_scene_revision, target_context, intent, and proposed_operations; changing any included member changes it and excluded lifecycle/result metadata does not."
    - "Place emits a stable provisional preview at base revision r, requires valid current support and an allowlisted proxy asset, cancels without mutation, and only explicit preview-bound user confirmation can yield the exact create_asset_instance delta."
    - "Replace and remove yield context-bound nonmutating proposals plus typed readiness blockers; restore is proposed without pre-authorizing compensation."
  prohibitions:
    - requirement_id: FR-AGENT-001
      category: authority
      status: unresolved
      verification: null
      statement: "MUST NOT accept transform, target, session, authority, revision, confirmation, commit, URL, or tool-set authority from user/model bytes."
      reason: "Flagged-unverified descriptor-less prohibition recall; explicit adversarial tests make the boundary executable."
    - requirement_id: FR-PLACE-001
      category: mutation
      status: unresolved
      verification: null
      statement: "MUST NOT increment scene_revision, publish canonical state, or persist a preview before explicit user confirmation."
      reason: "Preview and canonical commit are distinct locked lifecycle states."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift
      provides: "One strict local submit_user_intent boundary for supported typed/tap variants"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift
      provides: "Pure deterministic place preview/cancel/confirmed reduction"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift
      via: "Untrusted allowlisted DTO is bound to trusted session/branch/revision/target context only after strict decoding"
      pattern: "submitUserIntent|BoundProposal"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift
      to: docs/contracts/transaction.schema.json
      via: "Exact place operation order, required checks, preview and confirmation shapes"
      pattern: "create_asset_instance|explicit_user_confirmation"
---

<objective>
Implement the strict nonmutating intent seam, exact fingerprint scope, and pure place preview/confirm reducer.

Purpose: Prove the typed/tap golden path cannot smuggle mutation authority and that placement remains deterministic, offline, preview-bound, and revision-exact.
Output: Intent boundary, fingerprint engine, place reducer, and adversarial/reducer tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-RESEARCH.md
@docs/adr/ADR-011-agent-and-deterministic-boundary.md
@docs/adr/ADR-012-transaction-and-offline-restore.md
@docs/contracts/transaction.schema.json
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/canonical/GLOSSARY_AND_ID_REGISTRY.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce one context-bound nonmutating ingress and exact fingerprint</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionFingerprint.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/IntentBoundaryTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CanonicalJSON.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift, docs/adr/ADR-011-agent-and-deterministic-boundary.md, docs/contracts/transaction.schema.json, docs/canonical/TEST_AND_EVALUATION_PLAN.md</read_first>
  <behavior>
    - Tap and typed source variants with equivalent allowlisted operation/design values produce one identical proposal type after trusted local context binding.
    - Every forbidden, duplicate, malformed, empty, unknown, oversized, stale, mismatched, or injected input returns a stable rejection and leaves proposal/store/scene/history digests unchanged.
    - Fingerprint inclusion/exclusion toggles match RR-JCS-SHA256-1 exactly and never hash a generic dictionary or the complete transaction object.
  </behavior>
  <action>Write RED parameterized Swift Testing cases for all FR-AGENT-001 adjacency/empty/ordering/concurrency probes, including supported tap/typed equivalence and proof neither path can construct confirmation/commit. Implement a byte/depth-bounded exact DTO decoder using CanonicalJSON before Codable and canonical round-trip equality after decoding. Accept only the four operation enum plus allowlisted asset/query/typed constraints in stable order; attach captured session, branch, base revision, target/world context from a trusted value after parsing. Reject optional voice locally as unavailable in this phase while keeping any later voice adapter constrained to call the same boundary. Implement the dedicated seven-member fingerprint value and golden included/excluded-member tests.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter IntentBoundaryTests</automated>
  </verify>
  <done>Every supported local ingress variant yields one context-bound proposal; every listed injection class rejects before preview with exact unchanged-state assertions; fingerprint scope matches the frozen oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce stable place preview, cancellation, and explicit confirmation</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/PlaceReducerTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionModels.swift, fixtures/transactions/1.0.0/rev-001/expected-traces.json, docs/canonical/MASTER_TECHNICAL_SPEC.md, docs/contracts/scene-state.schema.json, docs/contracts/transaction.schema.json, .agents/skills/swift-testing-expert/references/parameterized-testing.md</read_first>
  <behavior>
    - Valid support and allowlisted asset produce a byte-stable preview bound to r without changing SceneState/history/store.
    - Missing/stale support, world mismatch, collision/license/integrity failure, stale base, and wrong branch/authority reject without preview or mutation.
    - Cancel changes no revision; a preview-bound explicit user confirmation produces exactly one create_asset_instance operation and complete captured-exact inverse.
    - Replace/remove proposals preserve the stable four-operation surface but return typed blockers and no canonical delta in Phase 3.
  </behavior>
  <action>Implement a pure synchronous reducer over immutable typed values. Derive the Phase 3 proxy transform/support relation only from the trusted deterministic support candidate and allowlisted proxy manifest reference; never accept spatial values from ingress. Materialize the exact place validation checks, provisional preview, cancellation, and confirmation record/event binding. On confirmed reduction, create exactly one placed asset/support relation delta, build the committed and prior complete RR-EDIT-PROJECTION-1 snapshots, required artifact union, and deterministic receipt candidate, but do not allocate the canonical revision or persist here. Assert stable replay traces, missing-support failure, cancellation, interruption-safe recomputation, and nonmutating replace/remove blockers.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter PlaceReducerTests</automated>
  </verify>
  <done>Place preview is stable and noncanonical at r; only exact confirmation yields the correct pending r+1 content and captured inverse; every blocker/rejection leaves all canonical bytes unchanged.</done>
</task>

</tasks>

<verification>
- `swift test --package-path ios/Packages/ReRoomContracts --filter IntentBoundaryTests`
- `swift test --package-path ios/Packages/ReRoomContracts --filter PlaceReducerTests`
- Source scan finds no network/model import or filesystem write in the three pure files.
</verification>

<success_criteria>
- FR-AGENT-001 adversarial corpus rejects 100% with zero mutation and local typed/tap equivalence.
- FR-PLACE-001 preview/support/cancel/confirmation truths match the frozen expected trace.
- FR-TRANSACTION-001 fingerprint and precommit semantics are exact and pure.
</success_criteria>

## Artifacts-this-phase-produces

- Strict `submit_user_intent` implementation and exact RR-JCS request fingerprint engine.
- Pure place preview/confirm reducer with a complete captured-exact projection inverse.
- Parameterized injection, ordering, fingerprint, support, preview, confirmation, and blocker tests.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-02-SUMMARY.md` when done.
</output>
