---
phase: 03-typed-place-commit-and-offline-restore
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift
autonomous: true
requirements: [FR-PLACE-001, FR-RESTORE-001, FR-TRANSACTION-001]
must_haves:
  truths:
    - "One native branch-authority actor serializes submit/preview/confirm/place/restore/CAS/idempotency/divergence decisions without a suspension inside check-reduce-persist-activate-update."
    - "Same idempotency key plus identical fingerprint returns the prior durable result; the same key plus different fingerprint conflicts; stale base and wrong branch/authority reject; every accepted commit changes r to r+1 exactly once."
    - "A generation becomes visible only after schema/JCS/semantic-valid SceneState, transaction, inverse/artifact inventory, receipts, and idempotency record are written and synchronized, then one active pointer is atomically replaced and synchronized last."
    - "Every filesystem fault/restart exposes the complete prior generation or complete new generation; incomplete generations remain inactive and diagnosable, and no visible acknowledgement precedes activation."
    - "Unexpected same-branch divergence preserves both histories, records quarantine, freezes future mutation, and never automatically merges."
    - "Restart recovers the exact active place/restore history and revision trace entirely offline."
  prohibitions:
    - requirement_id: FR-TRANSACTION-001
      category: concurrency
      status: unresolved
      verification: null
      statement: "MUST NOT suspend, call network/model/UI code, or admit a second revision writer between CAS validation and durable activation."
      reason: "Flagged-unverified descriptor-less prohibition recall; actor reentrancy would split canonical authority."
    - requirement_id: FR-TRANSACTION-001
      category: durability
      status: unresolved
      verification: null
      statement: "MUST NOT acknowledge Data.write atomicity, a partial generation, or an unsynchronized activation pointer as a durable commit."
      reason: "Multi-file state requires an immutable bundle and pointer-last durability boundary."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
      provides: "Immutable content-addressed generation activation/recovery with deterministic faults"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      provides: "Sole native actor for CAS, idempotency, commit, restore, recovery, and divergence freeze"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift
      via: "Existing bounded path-safe synchronous write/sync/replace and pre/post observer seam"
      pattern: "CaptureFileSystem|synchronizeDirectory|replace"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift
      via: "Actor supplies active snapshot, validates exact CAS/idempotency, and durably activates pure reducer output"
      pattern: "NativeBranchAuthority|PlaceReducer"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
      via: "Same authority and activation path commits compensating restore offline"
      pattern: "restore|RestoreReducer"
---

<objective>
Make place and restore exactly-once, crash-safe, restartable, and sole-writer authoritative offline.

Purpose: Turn pure reducer candidates into acknowledged canonical revisions without actor reentrancy, partial publication, duplicate mutation, or silent divergence.
Output: Durable generation store, native branch authority actor, crash matrix, and CAS/idempotency/divergence tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-place-commit-and-offline-restore/03-CONTEXT.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-RESEARCH.md
@docs/adr/ADR-012-transaction-and-offline-restore.md
@docs/canonical/RISK_AND_KILL_GATES.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift
@.planning/phases/03-typed-place-commit-and-offline-restore/03-02-SUMMARY.md
@.planning/phases/03-typed-place-commit-and-offline-restore/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish immutable generations with active pointer last</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureCrashMatrixTests.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionContractAdapter.swift, docs/canonical/TEST_AND_EVALUATION_PLAN.md, .agents/skills/swift-concurrency/references/actors.md</read_first>
  <behavior>
    - Every pre/post write, file sync, directory sync, and pointer replacement fault recovers either the exact old active generation or exact complete new one.
    - Scene, transaction, inverse/artifacts, result, idempotency, inventory, and activation identity/hash links are validated both before publication and during recovery.
    - Partial or corrupt bundles are inactive/diagnosable; recovery follows only a valid active pointer and never chooses by timestamp or directory order.
  </behavior>
  <action>Write a RED parameterized TST-PERSIST-001 operation fault matrix over an in-memory CaptureFileSystem fake plus Foundation parity cases. Implement an immutable `generations/<digest>/` bundle containing canonical scene, transaction, inventory/link/hash data, result receipt, and persistent idempotency index. Use the existing synchronous CaptureFileSystem seam: write and sync every member, sync generation inventory/directories, then atomically replace and sync one active-generation record last. Recovery validates exact bytes, frozen schemas, semantic links, hashes, required artifacts, revision trace, and pointer binding before returning an active snapshot. Leave incomplete bundles inactive and expose only sanitized typed corruption diagnostics.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter TransactionStoreCrashTests</automated>
  </verify>
  <done>The complete fault matrix and Foundation/in-memory parity pass; no injected edge exposes mixed revision state or loses a previously acknowledged generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize CAS, idempotency, restore, and divergence in one actor</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift, ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift, docs/adr/ADR-012-transaction-and-offline-restore.md, .agents/skills/swift-concurrency/SKILL.md, .agents/skills/swift-testing-expert/references/parallelization-and-isolation.md</read_first>
  <behavior>
    - Concurrent confirms/restore requests serialize under one authority and allocate unique monotonic revisions; no actor method suspends inside the mutation transaction.
    - Exact same-key/same-fingerprint retry returns the persisted receipt and unchanged revision; changed fingerprint, stale base, and wrong authority/branch reject without writes.
    - Valid place then offline restore survives restart with source history immutable and exact r, r+1, r+2 trace.
    - Injected same-branch divergence quarantines another branch identity, records manual/snapshot reconciliation, freezes mutation, and permits no automatic merge.
  </behavior>
  <action>Implement `NativeBranchAuthority` as the sole mutable owner of active snapshot, revision allocation, idempotency lookup, history, and frozen/quarantine state. Public actor entry may be awaited to cross isolation, but its internal check-fingerprint/reduce/store.activate/update sequence must be synchronous with no suspension. Check persistent idempotency before new mutation, enforce exact base/authority/branch equality, require a preview-bound explicit user confirmation, and acknowledge only after store activation. Keep canonical state separate from local_only/pending_sync. Add concurrent request tests, restart/retry equality, changed-content conflict, stale/wrong authority, offline place/restore, and divergence freeze/quarantine tests with exact pre/post byte digests.</action>
  <verify>
    <automated>swift test --package-path ios/Packages/ReRoomContracts --filter TransactionAuthorityTests</automated>
  </verify>
  <done>CAS/idempotency/concurrency/restart/offline/divergence fixtures match exact expected traces with no duplicate revision, mutation after freeze, partial acknowledgement, or automatic merge.</done>
</task>

</tasks>

<verification>
- `swift test --package-path ios/Packages/ReRoomContracts --filter TransactionStoreCrashTests`
- `swift test --package-path ios/Packages/ReRoomContracts --filter TransactionAuthorityTests`
- Source scan confirms no `await` token occurs in the authority's CAS-to-activation critical function and no network/model import exists in ReRoomTransactionCore.
</verification>

<success_criteria>
- Every commit/restore is one exact durable revision or no mutation, including faults and concurrent calls.
- Idempotency, stale/wrong authority, recovery, and divergence semantics match FR-TRANSACTION-001 exactly.
- Place/restore work offline after restart with immutable history and no visible mixed generation.
</success_criteria>

## Artifacts-this-phase-produces

- Content-addressed transaction generation store and active-pointer-last recovery protocol.
- Sole native branch-authority actor with persistent CAS/idempotency and divergence quarantine.
- Full durability-edge, concurrency, restart, retry, offline restore, and divergence regression suite.

<output>
Create `.planning/phases/03-typed-place-commit-and-offline-restore/03-04-SUMMARY.md` when done.
</output>
