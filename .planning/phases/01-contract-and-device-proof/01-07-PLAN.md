---
phase: 01-contract-and-device-proof
plan: "07"
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - scripts/run-reference-parity
  - tools/verify/tests/test_reference_parity.py
  - tools/javascript/test/parity-mutations.test.mjs
  - tools/python/tests/test_parity_mutations.py
autonomous: true
requirements: [NFR-CONTRACT-001, NFR-COORD-001]
must_haves:
  truths:
    - "JavaScript and Python execute independently against the same immutable inputs and agree exactly."
    - "Mutation gates prove each runtime and the comparator detect contract, digest, coordinate, omission, and oracle-integrity faults."
  artifacts:
    - path: scripts/run-reference-parity
      provides: "Two-runtime parity command"
  key_links:
    - from: scripts/run-reference-parity
      to: tools/verify/compare_results.py
      via: "independent runner outputs"
      pattern: "javascript|python|compare"
  decisions: [D-09, D-10, D-11, D-12]
---
<objective>Prove the two reference implementations and comparator are independently effective.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@tools/verify/compare_results.py
@tools/javascript/src/runner.mjs
@tools/python/reroom_verify/runner.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Execute and compare independent reference runners</name>
  <files>scripts/run-reference-parity, tools/verify/tests/test_reference_parity.py</files>
  <read_first>tools/verify/compare_results.py, tools/javascript/src/runner.mjs, tools/python/reroom_verify/runner.py, fixtures/runner-result.schema.json</read_first>
  <behavior>Command runs both actual runtimes, validates both envelopes and oracle integrity, then requires complete exact agreement.</behavior>
  <action>Add an isolated temporary-output command and integration test. Reject stale outputs, wrong runner identity/revision, missing executable/environment, and any case mismatch.</action>
  <acceptance_criteria>One command produces fresh results from both runtimes and the common comparator accepts only exact complete parity.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_reference_parity -v</automated></verify>
  <done>The reference pair is genuinely executed and compared.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add cross-runtime mutation gates</name>
  <files>tools/javascript/test/parity-mutations.test.mjs, tools/python/tests/test_parity_mutations.py, tools/verify/tests/test_reference_parity.py</files>
  <read_first>scripts/run-reference-parity, tools/verify/tests/test_compare_results.py, fixtures/contracts/1.0.0/rev-001/manifest.json, fixtures/policies/RR-JCS-SHA256-1/rev-001/manifest.json, fixtures/policies/RR-COORD-1/rev-001/manifest.json</read_first>
  <behavior>Temporary mutations to schema boundary, JCS bytes, RRFP magic/version/flags/length/sequence/payload SHA/truncation/trailing bytes, archive path, coordinate matrix/orientation/correction, result completeness, and fixture hash each cause failure.</behavior>
  <action>Mutate copies only. Require a targeted failure from each independent runtime where applicable and from the comparator; include a control that immutable repository fixtures remain unchanged. Do not introduce or test a wire trailer.</action>
  <acceptance_criteria>Every mutation is killed and post-test oracle hashes equal their pre-test values.</acceptance_criteria>
  <verify><automated>node --test tools/javascript/test/parity-mutations.test.mjs &amp;&amp; .venv/bin/python -m unittest tools.python.tests.test_parity_mutations tools.verify.tests.test_reference_parity -v</automated></verify>
  <done>Green parity cannot be explained by shared omissions or an ineffective comparator.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Tampering | parity harness | high | mitigate | Fresh isolated outputs, mutation gates, pre/post fixture hashes. |
</threat_model>
## Artifacts this phase produces
- Reference parity command, integration tests, and mutation gates.
<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>Two independent references agree and demonstrably reject representative faults.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-07-SUMMARY.md`.</output>
