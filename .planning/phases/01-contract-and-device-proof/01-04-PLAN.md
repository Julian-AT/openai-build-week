---
phase: 01-contract-and-device-proof
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/javascript/package.json
  - tools/javascript/package-lock.json
  - tools/python/requirements.in
  - tools/python/requirements.lock
  - ios/Packages/ReRoomContracts/Package.swift
  - ios/Packages/ReRoomContracts/Package.resolved
  - evidence/dependencies/phase-01-package-audit.json
  - tools/verify/verify_phase_01_dependencies.py
autonomous: false
requirements: [NFR-CONTRACT-001]
must_haves:
  truths:
    - "Every third-party package has an exact version, compatible license/artifact evidence, current official documentation, and a tested fallback before installation."
    - "The candidate set is exactly ajv, ajv-formats, canonicalize, jsonschema, rfc8785, and swift-json-schema; SUS is only an audit verdict/signal and is never a package."
    - "No lock generation or package install occurs until a human records approve, reject, or fallback for every candidate; every generated lock must match the approved exact artifact."
  artifacts:
    - path: evidence/dependencies/phase-01-package-audit.json
      provides: "Dependency approval record"
  key_links:
    - from: evidence/dependencies/phase-01-package-audit.json
      to: tools/javascript/package-lock.json
      via: "approved exact package version and artifact digest"
      pattern: "package|version|sha256|license|fallback"
  decisions: [D-09]
---
<objective>Resolve the exact six-candidate dependency gate before any lock generation or installation.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-contract-and-device-proof/01-RESEARCH.md
@docs/canonical/RESEARCH_LEDGER.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Prepare exact dependency audit and fallback manifests</name>
  <files>tools/javascript/package.json, tools/python/requirements.in, evidence/dependencies/phase-01-package-audit.json</files>
  <read_first>AGENTS.md, .planning/phases/01-contract-and-device-proof/01-RESEARCH.md, docs/canonical/RESEARCH_LEDGER.md</read_first>
  <action>Using current official package/release/license sources, create one pending audit entry for each and only each candidate: `ajv`, `ajv-formats`, `canonicalize`, `jsonschema`, `rfc8785`, and `swift-json-schema`. Record exact proposed version, artifact URL/hash or registry integrity, license compatibility, current API documentation, role, and tested stdlib/manual fallback. Record `SUS` only as a verdict/signal on the affected candidate audit entry—never as a package name. Prepare proposed non-lock manifests only; do not generate package locks, resolve Swift packages, create environments, populate caches, or install packages before Task 2 human decisions.</action>
  <acceptance_criteria>The audit candidate keys equal the six-name allowlist, every entry is pending with complete exact artifact/license/docs/fallback evidence, SUS appears only as an audit signal, no lock exists, and no root/nested install or build directory exists.</acceptance_criteria>
  <verify><automated>python3 -c "import json; d=json.load(open('evidence/dependencies/phase-01-package-audit.json')); e={'ajv','ajv-formats','canonicalize','jsonschema','rfc8785','swift-json-schema'}; assert set(d['candidates'])==e; assert all(d['candidates'][n]['decision']=='pending' for n in e); assert 'SUS' not in e" &amp;&amp; test ! -f tools/javascript/package-lock.json &amp;&amp; test ! -f tools/python/requirements.lock &amp;&amp; test ! -f ios/Packages/ReRoomContracts/Package.resolved &amp;&amp; test ! -d node_modules &amp;&amp; test ! -d .venv &amp;&amp; test ! -d tools/javascript/node_modules &amp;&amp; test ! -d tools/python/.venv &amp;&amp; test ! -d ios/Packages/ReRoomContracts/.build</automated></verify>
  <done>Exact candidate dependencies and fallbacks are reviewable before mutation.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve package audit before install</name>
  <files>evidence/dependencies/phase-01-package-audit.json, tools/javascript/package.json, tools/javascript/package-lock.json, tools/python/requirements.in, tools/python/requirements.lock, ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Package.resolved, tools/verify/verify_phase_01_dependencies.py</files>
  <read_first>evidence/dependencies/phase-01-package-audit.json, tools/javascript/package.json, tools/python/requirements.in</read_first>
  <action>Present all six named candidate entries to the human and require an explicit `approved`, `rejected`, or `fallback_selected` decision for each before generating a manifest lock or installing anything. Then make each ecosystem's direct manifest set equal exactly its approved subset: the union of JavaScript `dependencies` and `devDependencies` equals the approved subset of `ajv`, `ajv-formats`, and `canonicalize`, with no duplicate direct key; Python `requirements.in` direct requirement names equal the approved subset of `jsonschema` and `rfc8785`; Swift `Package.swift` has the `swift-json-schema` external package only when approved, at the audited exact version/revision, otherwise selects the named frozen in-repo validator with that external package absent. Generate locks from those manifests. Implement `verify_phase_01_dependencies.py` as a bounded offline verifier that reads only the six-candidate audit plus the three manifests/locks and fails on: pending/unknown decision; any extra, duplicate, or missing direct dependency; package-lock root direct-set drift; direct version/integrity mismatch; an unreachable/extraneous npm resolved node outside the dependency closure of approved direct roots; requirements.in set/version drift; requirements.lock version/hash drift or any resolved Python package lacking a bounded provenance chain to an approved direct root; Package.swift external URL/requirement drift; Package.resolved pin drift; or external Swift presence under rejected/fallback. Rejected/unapproved candidates must be absent everywhere. SUS remains an audit signal, never a dependency. Resolved transitives are permitted only when the verifier proves reachability from an approved direct root; any later install consumes only these verified locks.</action>
  <acceptance_criteria>All six candidates have individual human decisions; JavaScript, Python, and Swift direct manifest sets equal exactly their approved ecosystem subsets with no unaudited extras; rejected/fallback candidates are absent; approved direct locks match exact audited version and integrity/hash/revision; every resolved transitive has a bounded provenance path to an approved root; and the named frozen Swift fallback contains no external swift-json-schema package or pin.</acceptance_criteria>
  <verify><automated>python3 tools/verify/verify_phase_01_dependencies.py --audit evidence/dependencies/phase-01-package-audit.json --package-json tools/javascript/package.json --package-lock tools/javascript/package-lock.json --requirements-in tools/python/requirements.in --requirements-lock tools/python/requirements.lock --swift-package ios/Packages/ReRoomContracts/Package.swift --swift-resolved ios/Packages/ReRoomContracts/Package.resolved</automated></verify>
  <done>Dependency use is explicitly authorized or replaced by a verified fallback.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Supply chain | package toolchains | high | mitigate | Exact artifacts, licenses, official docs, human approval, tested fallbacks. |
</threat_model>
## Artifacts this phase produces
- Exact lock/manifests and the Phase 1 dependency approval record.
<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>No unapproved or unknown-license dependency enters the Phase 1 toolchains.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-04-SUMMARY.md`.</output>
