---
phase: 01-contract-and-device-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/manifest.schema.json
  - fixtures/runner-result.schema.json
  - fixtures/contracts/1.0.0/rev-001/
  - fixtures/policies/RR-JCS-SHA256-1/rev-001/
  - fixtures/policies/RR-COORD-1/rev-001/
autonomous: true
requirements: [NFR-CONTRACT-001, NFR-COORD-001]
must_haves:
  truths:
    - "Exact 1.0 and exact inclusive/exclusive schema boundaries follow the frozen schemas; unknown versions/fields and invalid boundary values reject without coercion or defaults."
    - "Null and empty inputs follow the exact schema type/required/minItems/minProperties rules; no runtime invents a default."
    - "Contract array order remains normative, object members are normalized only by RR-JCS-SHA256-1, and fixture/result rows use stable lexicographic case_id order."
    - "Checked-in inputs, expected bytes/digests, rejection classes, and schema hashes are immutable oracle data."
  prohibitions:
    - "MUST NOT alter expected bytes, digests, rejection classes, thresholds, or a frozen fixture revision merely to turn a failure into a pass."
  artifacts:
    - path: fixtures/manifest.schema.json
      provides: "Closed revision/case manifest"
    - path: fixtures/runner-result.schema.json
      provides: "Normalized three-runtime result envelope"
    - path: fixtures/contracts/1.0.0/rev-001/
      provides: "FX-CONTRACT-001 corpus"
  key_links:
    - from: fixtures/contracts/1.0.0/rev-001/manifest.json
      to: docs/contracts/
      via: "SHA-256 for each canonical schema"
      pattern: "schema_sha256"
  decisions: [D-09, D-10, D-11, D-12]
---

<objective>Freeze the language-neutral CON-001–CON-005, RR-JCS-SHA256-1, and RR-COORD-1 oracle before runtime consumers exist.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-contract-and-device-proof/01-CONTEXT.md
@.planning/phases/01-contract-and-device-proof/01-RESEARCH.md
@docs/contracts/README.md
@docs/contracts/frame-packet.schema.json
@docs/contracts/rrcap-manifest.schema.json
@docs/contracts/scene-state.schema.json
@docs/contracts/edit-artifacts.schema.json
@docs/contracts/transaction.schema.json
@docs/canonical/GLOSSARY_AND_ID_REGISTRY.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define closed fixture and runner-result manifests</name>
  <files>fixtures/manifest.schema.json, fixtures/runner-result.schema.json</files>
  <read_first>docs/contracts/README.md, docs/contracts/frame-packet.schema.json, docs/contracts/rrcap-manifest.schema.json, docs/contracts/scene-state.schema.json, docs/contracts/edit-artifacts.schema.json, docs/contracts/transaction.schema.json, docs/canonical/GLOSSARY_AND_ID_REGISTRY.md</read_first>
  <behavior>Closed test-only manifests require revision, schema hashes, stable case ID, input path, expected verdict, rejection class/output bytes/digest, and immutable status; runner envelopes require runner/revision and unique case results.</behavior>
  <action>Define `FixtureManifestV1`, `FixtureCaseV1`, and `RunnerResultV1` without minting product IDs. Bound path, byte, depth, and case counts. Require lexicographic case ordering and exact expected artifact hashes; generators are proposal tools and never an oracle.</action>
  <acceptance_criteria>Both files parse as Draft 2020-12 JSON Schemas, are closed, and contain all named required fields and bounds.</acceptance_criteria>
  <verify><automated>python3 -m json.tool fixtures/manifest.schema.json >/dev/null &amp;&amp; python3 -m json.tool fixtures/runner-result.schema.json >/dev/null</automated></verify>
  <done>Every runtime has one exact test-only input/output envelope.</done>
</task>
<task type="auto">
  <name>Task 2: Author immutable rev-001 oracle cases</name>
  <files>fixtures/contracts/1.0.0/rev-001/, fixtures/policies/RR-JCS-SHA256-1/rev-001/, fixtures/policies/RR-COORD-1/rev-001/</files>
  <read_first>fixtures/manifest.schema.json, docs/canonical/TEST_AND_EVALUATION_PLAN.md, docs/canonical/MASTER_TECHNICAL_SPEC.md, docs/canonical/GLOSSARY_AND_ID_REGISTRY.md, docs/contracts/frame-packet.schema.json, docs/contracts/rrcap-manifest.schema.json, docs/contracts/scene-state.schema.json, docs/contracts/edit-artifacts.schema.json, docs/contracts/transaction.schema.json</read_first>
  <action>Populate FX-CONTRACT-001, FX-JCS-001, and FX-COORD-001 with valid/invalid exact inputs, `.raw` parser-differential bytes, authoritative `.jcs` bytes, lowercase digests, stable case IDs, and closed rejection classes per D-09, D-10, D-11, and D-12. Cover all canonical compatibility, path, ID, lifecycle, digest-scope, RR-FLOAT, projection, crop/orientation, and correction cases. For RRFP-WIRE-1, fixture the exact CON-001 envelope: 24-byte big-endian header, RRFP magic, version 1.0, zero flags, bounded JCS-header and payload lengths, duplicated payload-length/capture-sequence agreement, exact payload SHA-256, truncation/tamper/mismatch rejection, and rejection of every trailing byte; there is no trailer. Record all five schema hashes and never edit canonical schemas.</action>
  <acceptance_criteria>All five schema IDs have positive/negative cases; RRFP cases cover every exact CON-001 fixed-header/duplicate/hash/truncation/trailing-byte rule without a trailer; every manifest input/expected path exists and hashes match; duplicate-name/invalid-Unicode cases remain raw bytes; ordinary verification cannot regenerate expected files.</acceptance_criteria>
  <verify><automated>python3 -c "import hashlib,json,pathlib; roots=list(pathlib.Path('fixtures').glob('**/rev-001/manifest.json')); assert len(roots)==3; [json.load(open(p)) for p in roots]"</automated></verify>
  <done>The immutable corpus fully expresses canonical contract and coordinate/digest behavior.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | fixture oracle | high | mitigate | Schema hashes, exact expected-file hashes, append-only revision, no generator in verification. |
| T-01-02 | Denial of Service | fixture loaders | medium | mitigate | Manifest byte/depth/count/path bounds. |
</threat_model>

## Spec-less Probe Mapping
| Probe row | Resolution |
|---|---|
| NFR-CONTRACT-001 adjacency | Truth 1: exact version and inclusive/exclusive schema boundaries. |
| NFR-CONTRACT-001 empty | Truth 2: exact null/empty schema behavior, no defaults. |
| NFR-CONTRACT-001 ordering | Truth 3: normative arrays, JCS objects, stable case order. |

## Artifacts this phase produces
- Files/directories listed in `files_modified`.
- Test-only symbols: `FixtureManifestV1`, `FixtureCaseV1`, `RunnerResultV1`.
<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>The checked-in oracle is complete, closed, immutable, and consumable without runtime-specific expectations.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-01-SUMMARY.md`.</output>
