---
phase: 01-contract-and-device-proof
plan: "03"
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - tools/verify/compare_results.py
  - tools/verify/tests/test_compare_results.py
  - scripts/verify-phase-01-contracts
autonomous: true
requirements: [NFR-CONTRACT-001, NFR-COORD-001, OPS-DEVICE-001]
must_haves:
  truths:
    - "One fail-closed comparator verifies oracle integrity and exact per-case agreement before reporting success."
    - "Full verification is the complete automated preflight and can pass before physical evidence exists; after all required automated runtime, mutation, build, schema, and release-surface checks pass, it emits the sanitized automated-preflight artifact."
    - "Gate verification is the only mode that requires signed physical reports/checklists; schema-valid RED is retained as D-16 failure evidence, but gate exits nonzero unless both GATE-002 and GATE-013 are exactly GREEN."
  artifacts:
    - path: tools/verify/compare_results.py
      provides: "Bounded normalized-result and evidence comparator"
    - path: scripts/verify-phase-01-contracts
      provides: "Stable Phase 1 verification entry point"
  key_links:
    - from: scripts/verify-phase-01-contracts
      to: tools/verify/compare_results.py
      via: "explicit verification modes"
      pattern: "fixture-integrity|contracts|coords|references|gate"
  decisions: [D-09, D-12, D-16]
---
<objective>Build the common fail-closed comparator and verification command before language runners depend on it.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-contract-and-device-proof/01-CONTEXT.md
@fixtures/manifest.schema.json
@fixtures/runner-result.schema.json
@evidence/templates/gate-report.schema.json</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement bounded oracle and result comparison</name>
  <files>tools/verify/compare_results.py, tools/verify/tests/test_compare_results.py</files>
  <read_first>fixtures/manifest.schema.json, fixtures/runner-result.schema.json, evidence/templates/gate-report.schema.json, .planning/phases/01-contract-and-device-proof/01-CONTEXT.md</read_first>
  <behavior>Comparator verifies manifest/file/schema hashes, rejects duplicate/missing/extra/out-of-order cases, and requires exact verdict, rejection class, canonical bytes, digest, and normalized numeric agreement.</behavior>
  <action>Write tests first, then implement bounded reads and closed result comparison. Never regenerate expectations. Make fixture integrity an unavoidable prerequisite for result comparison and render deterministic diagnostics without private input bytes.</action>
  <acceptance_criteria>Mutation tests detect changed oracle bytes/hash, duplicate/omitted/extra cases, and every result-field mismatch.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_compare_results -v</automated></verify>
  <done>The shared comparator fails closed on oracle tampering and runtime disagreement.</done>
</task>
<task type="auto">
  <name>Task 2: Add stable quick and full verification modes</name>
  <files>scripts/verify-phase-01-contracts</files>
  <read_first>tools/verify/compare_results.py, docs/canonical/TEST_AND_EVALUATION_PLAN.md, docs/canonical/RISK_AND_KILL_GATES.md</read_first>
  <action>Add explicit `fixture-integrity`, `evidence`, `contracts`, `coords`, `references`, `quick`, `full`, and `gate` modes. Quick runs available deterministic checks. `full` is the complete automated preflight: it runs all three runtimes, mutation gates, package/schema checks, native unit/UI/build checks, release-surface inspection, and secret/diff checks, then atomically emits a valid sanitized automated-preflight artifact only after success; it never reads or requires physical reports or signatures. `gate` first requires a successful `full` artifact, then schema-valid signed GATE-013 and GATE-002 report/checklist pairs, and finally requires both report states to equal `GREEN`. Preserve schema-valid `RED` reports as D-16 evidence but exit nonzero; likewise fail for `UNRUN`, `RUNNING`, missing, invalid, or ordinary `WAIVED_BY_HUMAN` evidence.</action>
  <acceptance_criteria>Unknown mode fails; `full` can create and validate automated preflight while physical gates remain UNRUN/RUNNING; individual RED evidence validates, but `gate` exits zero only when both signed physical reports are exactly GREEN and exits nonzero for every other state combination.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_compare_results -v &amp;&amp; scripts/verify-phase-01-contracts fixture-integrity</automated></verify>
  <done>CI can complete automated preflight before device work, while Phase 1 gate completion succeeds only for two signed GREEN reports.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Tampering | verifier | high | mitigate | Verify immutable inputs before results; exact closed comparison. |
| T-01-06 | Information Disclosure | diagnostics | medium | mitigate | Case IDs and mismatch classes only; no private bytes. |
</threat_model>
## Artifacts this phase produces
- Files listed in `files_modified` and comparator mutation tests.
<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>No runner or evidence source can be omitted or disagreed with, and physical completion cannot pass unless both GATE-002 and GATE-013 are GREEN.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-03-SUMMARY.md`.</output>
