---
phase: 01-contract-and-device-proof
plan: "15"
type: execute
wave: 12
depends_on: ["01-14"]
files_modified:
  - scripts/run-three-runtime-agreement
  - evidence/compatibility/contract-agreement.json
  - evidence/compatibility/jcs-agreement.json
  - evidence/compatibility/coordinate-agreement.json
autonomous: true
gap_closure: true
requirements: [NFR-CONTRACT-001, NFR-COORD-001]
must_haves:
  truths:
    - "The ordinary checked-in three-runtime command accepts the finalized bound contract source tree and publishes current MEASURED evidence without an in-memory or verifier-only rebind."
    - "Contract, JCS, and coordinate reports bind one exact post-review implementation revision, one identical source-tree digest, current immutable oracle/schema/evaluator hashes, all three fresh raw-result digests, and zero disagreements."
    - "A second ordinary execution reproduces all three compatibility report files byte-for-byte."
    - "The signed device candidate, automated preflight, GATE-013/GATE-002 reports and checklists, external attestations, and opaque physical evidence remain unchanged."
  artifacts:
    - path: scripts/run-three-runtime-agreement
      provides: "Non-self-referential binding to the finalized contract-source revision and deterministic evidence revision IDs"
      contains: "a5bff6896188dcac9397c48ce1a6820a7196011a"
    - path: evidence/compatibility/contract-agreement.json
      provides: "Current CON-001 through CON-005 three-runtime agreement evidence"
      contains: "git:a5bff6896188dcac9397c48ce1a6820a7196011a"
    - path: evidence/compatibility/jcs-agreement.json
      provides: "Current RR-JCS-SHA256-1 three-runtime agreement evidence"
      contains: "git:a5bff6896188dcac9397c48ce1a6820a7196011a"
    - path: evidence/compatibility/coordinate-agreement.json
      provides: "Current RR-COORD-1 and RRFP-WIRE-1 three-runtime agreement evidence"
      contains: "git:a5bff6896188dcac9397c48ce1a6820a7196011a"
  key_links:
    - from: scripts/run-three-runtime-agreement
      to: a5bff6896188dcac9397c48ce1a6820a7196011a
      via: "BOUND_REVISION plus git diff/ls-tree over BOUND_SOURCE_SCOPES"
      pattern: "BOUND_REVISION.*a5bff689"
    - from: scripts/run-three-runtime-agreement
      to: tools/verify/compare_results.py
      via: "fresh Swift, JavaScript, and Python outputs required before atomic report replacement"
      pattern: "require-runtime.*swift"
    - from: evidence/compatibility/contract-agreement.json
      to: evidence/compatibility/coordinate-agreement.json
      via: "shared implementation revision and source_tree_sha256"
      pattern: "source_tree_sha256"
decisions: [D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
---

<objective>
Refresh the stale three-runtime agreement provenance for the finalized post-review contract sources.

Purpose: Close P10.2 from `01-VERIFICATION.md` by making the ordinary production command reproducibly publish current contract, JCS, and coordinate evidence. This is an evidence-freshness repair, not a contract, oracle, runtime-policy, or physical-candidate change.
Output: One exact non-circular source binding and three byte-stable MEASURED compatibility reports.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-and-device-proof/01-CONTEXT.md
@.planning/phases/01-contract-and-device-proof/01-VERIFICATION.md
@.planning/phases/01-contract-and-device-proof/01-10-SUMMARY.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@docs/contracts/README.md
@scripts/run-three-runtime-agreement
@scripts/verify-phase-01-contracts
@tools/verify/compare_results.py
@evidence/compatibility/contract-agreement.json
@evidence/compatibility/jcs-agreement.json
@evidence/compatibility/coordinate-agreement.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebind and regenerate the three compatibility reports</name>
  <files>scripts/run-three-runtime-agreement, evidence/compatibility/contract-agreement.json, evidence/compatibility/jcs-agreement.json, evidence/compatibility/coordinate-agreement.json</files>
  <read_first>scripts/run-three-runtime-agreement, .planning/phases/01-contract-and-device-proof/01-VERIFICATION.md, .planning/phases/01-contract-and-device-proof/01-10-SUMMARY.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, evidence/compatibility/contract-agreement.json, evidence/compatibility/jcs-agreement.json, evidence/compatibility/coordinate-agreement.json</read_first>
  <action>
Before editing, prove that `a5bff6896188dcac9397c48ce1a6820a7196011a` is the latest commit that changed any path in the existing `BOUND_SOURCE_SCOPES`, and prove that those scoped bytes are identical between that commit and the current HEAD. Stop on either mismatch; do not select a newer documentation/evidence commit merely because it is HEAD.

Update only the harness binding/evidence-run identity: set `BOUND_REVISION` to `a5bff6896188dcac9397c48ce1a6820a7196011a`, the last finalized contract-source commit containing the serialized-validator and frozen-schema/exporter-profile changes. Keep `BOUND_SOURCE_SCOPES` unchanged so the harness remains non-self-referential: the script and generated reports stay outside the source tree whose historical bytes are compared. Advance the three deterministic evidence IDs from their prior `_001` run identity to `_002`, because the implementation revision changed and the report itself says a source change requires a new evidence revision. Do not alter fixture revisions, expected bytes/digests, rejection classes, thresholds, runtime identities, comparator policy, report metrics, or source-scope membership, per D-09 through D-12.

Run the ordinary checked-in `scripts/run-three-runtime-agreement` command with no monkeypatch, environment override, or publishing bypass. Let that command exclusively create fresh Swift/JavaScript/Python outputs, compare all three corpora, verify the unchanged oracle before and after execution, and atomically replace all three reports. Assert that every report records `git:a5bff6896188dcac9397c48ce1a6820a7196011a`, the `_002` evidence ID, one common nonempty `source_tree_sha256`, three raw results at the same implementation revision, `agreement.verdict=pass`, and zero missing, extra, verdict, rejection-class, artifact, and oracle disagreements.

Do not run `scripts/verify-phase-01-contracts full`, regenerate `automated-preflight.json`, rebuild/re-sign/reinstall the candidate, or access the externally retained raw physical evidence. Do not edit anything under `evidence/device/phase-01/` or the external attestation path; D-13 through D-16 keep physical/human evidence outside this repair. Preserve the pre-existing scheme, `.swiftpm`, Xcode workspace, and `xcuserdata` worktree paths without staging or editing them.
  </action>
  <acceptance_criteria>The checked-in command completes normally for all three fixture IDs; all three reports carry the exact finalized source revision, distinct `_002` evidence IDs, the same current source-tree digest, complete three-runtime raw-result bindings, and a zero-disagreement pass without any oracle, contract, runtime-policy, or physical-evidence change.</acceptance_criteria>
  <verify>
    <automated>test "$(git log -1 --format=%H -- fixtures docs/contracts ios/Packages/ReRoomContracts/Package.swift ios/Packages/ReRoomContracts/Package.resolved ios/Packages/ReRoomContracts/Sources/ReRoomContracts ios/Packages/ReRoomContracts/Sources/ReRoomContractRunner tools/javascript/package.json tools/javascript/package-lock.json tools/javascript/src tools/python/requirements.in tools/python/requirements.lock tools/python/reroom_verify tools/verify/compare_results.py tools/verify/fixtures)" = a5bff6896188dcac9397c48ce1a6820a7196011a &amp;&amp; git diff --quiet a5bff6896188dcac9397c48ce1a6820a7196011a HEAD -- fixtures docs/contracts ios/Packages/ReRoomContracts/Package.swift ios/Packages/ReRoomContracts/Package.resolved ios/Packages/ReRoomContracts/Sources/ReRoomContracts ios/Packages/ReRoomContracts/Sources/ReRoomContractRunner tools/javascript/package.json tools/javascript/package-lock.json tools/javascript/src tools/python/requirements.in tools/python/requirements.lock tools/python/reroom_verify tools/verify/compare_results.py tools/verify/fixtures &amp;&amp; scripts/run-three-runtime-agreement &amp;&amp; python3 -c 'import json,pathlib; paths=[pathlib.Path("evidence/compatibility")/name for name in ("contract-agreement.json","jcs-agreement.json","coordinate-agreement.json")]; reports=[json.loads(path.read_text()) for path in paths]; revision="git:a5bff6896188dcac9397c48ce1a6820a7196011a"; assert all(r["implementation"]["revision"]==revision and r["evidence_id"].endswith("_002") and r["agreement"]["verdict"]=="pass" and all(r["agreement"][k]==0 for k in ("missing_cases","extra_cases","verdict_disagreements","rejection_class_disagreements","artifact_disagreements","oracle_mismatches")) and all(v["implementation_revision"]==revision for v in r["raw_results"].values()) for r in reports); assert len({r["implementation"]["source_tree_sha256"] for r in reports})==1'</automated>
  </verify>
  <done>The production harness and three durable MEASURED reports are current for the finalized bound contract-source tree.</done>
</task>

<task type="auto">
  <name>Task 2: Prove byte stability, focused regressions, and physical-evidence non-interference</name>
  <files>evidence/compatibility/contract-agreement.json, evidence/compatibility/jcs-agreement.json, evidence/compatibility/coordinate-agreement.json</files>
  <read_first>scripts/verify-phase-01-contracts, scripts/run-reference-parity, tools/verify/tests/test_reference_parity.py, tools/javascript/test/parity-mutations.test.mjs, tools/python/tests/test_parity_mutations.py, ios/Packages/ReRoomContracts/Tests/ReRoomContractsTests/ContractValidationTests.swift, evidence/device/phase-01/automated-preflight.json, evidence/device/phase-01/gate-013-report.json, evidence/device/phase-01/gate-013-operator-checklist.json, evidence/device/phase-01/gate-002-report.json, evidence/device/phase-01/gate-002-operator-checklist.json</read_first>
  <action>
Record SHA-256 values for the three Task 1 reports, execute the same ordinary `scripts/run-three-runtime-agreement` command a second time, and require the post-run SHA-256 values to match exactly. Then run the checked-in `references` mode, immutable fixture-integrity comparator, independent JavaScript/Python reference parity and mutation suites, the complete Swift package tests (including the 1,024-request shared-validator concurrency regression), and the exact offline dependency audit. These focused checks must all use the existing locked environments and add no dependency.

Prove the five tracked files under `evidence/device/phase-01/` are byte-identical to `a5bff6896188dcac9397c48ce1a6820a7196011a` and have no worktree diff. This anchors the unchanged signed candidate/preflight/GATE-013/GATE-002 chain without reading raw room evidence or repeating a human observation. Also require the three fixture/oracle manifests to remain unchanged, run the tracked-secret scan and `git diff --check`, and inspect `git status --short` to ensure only this plan's four declared paths plus pre-existing user/Xcode and `01-VERIFICATION.md` state are present. Never stage the protected uncommitted paths.

Run the installed GSD Core roadmap consistency validator with `node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" roadmap validate` and require an empty warning list. Inspect planning health with `node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" query validate.health`; fail on every error and on every warning except the one already-known non-phase `W004` warning whose message identifies `model_profile "adaptive"` as invalid for this installed GSD Core. Treat that warning as documented configuration/tooling debt only: do not repair or rewrite `.planning/config.json` in this gap plan. Informational `I001` for the currently executing 01-15 plan having no summary is expected until the executor writes `01-15-SUMMARY.md`; no other error or warning is allowlisted.
  </action>
  <acceptance_criteria>A second ordinary run is byte-stable; references, fixture integrity, runtime mutations, Swift contract/concurrency, and dependency checks pass; all physical evidence/preflight bytes match the protected baseline; roadmap consistency has zero warnings; GSD health has no errors and no warning beyond the documented `adaptive` model-profile W004; no secret, whitespace error, raw evidence, or undeclared product change is introduced.</acceptance_criteria>
  <verify>
    <automated>before=$(shasum -a 256 evidence/compatibility/contract-agreement.json evidence/compatibility/jcs-agreement.json evidence/compatibility/coordinate-agreement.json) &amp;&amp; scripts/run-three-runtime-agreement &amp;&amp; after=$(shasum -a 256 evidence/compatibility/contract-agreement.json evidence/compatibility/jcs-agreement.json evidence/compatibility/coordinate-agreement.json) &amp;&amp; test "$before" = "$after" &amp;&amp; scripts/verify-phase-01-contracts references &amp;&amp; python3 tools/verify/compare_results.py fixture-integrity --manifest fixtures/contracts/1.0.0/rev-001/manifest.json --manifest fixtures/policies/RR-JCS-SHA256-1/rev-001/manifest.json --manifest fixtures/policies/RR-COORD-1/rev-001/manifest.json &amp;&amp; scripts/run-reference-parity &amp;&amp; node --test tools/javascript/test/parity-mutations.test.mjs &amp;&amp; .venv/bin/python -m unittest tools.python.tests.test_parity_mutations tools.verify.tests.test_reference_parity -v &amp;&amp; swift test --package-path ios/Packages/ReRoomContracts &amp;&amp; python3 tools/verify/verify_phase_01_dependencies.py --audit evidence/dependencies/phase-01-package-audit.json --package-json tools/javascript/package.json --package-lock tools/javascript/package-lock.json --requirements-in tools/python/requirements.in --requirements-lock tools/python/requirements.lock --swift-package ios/Packages/ReRoomContracts/Package.swift --swift-resolved ios/Packages/ReRoomContracts/Package.resolved &amp;&amp; git diff --quiet a5bff6896188dcac9397c48ce1a6820a7196011a HEAD -- evidence/device/phase-01 &amp;&amp; git diff --quiet -- evidence/device/phase-01 &amp;&amp; roadmap_json=$(node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" roadmap validate) &amp;&amp; health_json=$(node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" query validate.health) &amp;&amp; ROADMAP_JSON="$roadmap_json" HEALTH_JSON="$health_json" python3 -c 'import json,os; roadmap=json.loads(os.environ["ROADMAP_JSON"]); health=json.loads(os.environ["HEALTH_JSON"]); assert roadmap.get("warnings")==[]; assert health.get("errors")==[]; warnings=health.get("warnings",[]); assert len(warnings)&lt;=1 and all(w.get("code")=="W004" and "invalid model_profile \"adaptive\"" in w.get("message","") for w in warnings)' &amp;&amp; ! git grep -nE '(AKIA[0-9A-Z]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|xox[baprs]-[A-Za-z0-9-]{10,})' -- . ':!evidence/fixtures/**' &amp;&amp; git diff --check</automated>
  </verify>
  <done>P10.2 has reproducible current acceptance evidence, while every signed physical artifact and unrelated user path remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git revision to working source tree | Historical source identity must match the exact current bytes in the closed bound scope before evidence can be published. |
| Fresh runtime output to compatibility report | Ephemeral Swift, JavaScript, and Python result files are untrusted until closed-schema validation and three-way comparison complete. |
| Compatibility maintenance to signed device evidence | An automated host-only evidence refresh must not mutate, reinterpret, or regenerate the separately authorized human/device chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15-01 | Tampering | `BOUND_REVISION` and `BOUND_SOURCE_SCOPES` | high | mitigate | Pin the last source-changing commit, require scoped byte equality to HEAD, keep script/reports outside the scope, and fail before execution on drift. |
| T-01-15-02 | Repudiation | Three compatibility reports | high | mitigate | Create a new deterministic evidence-run identity and bind fixture, implementation, source tree, evaluator, environment, raw-result, and metric digests. |
| T-01-15-03 | Tampering | Signed device evidence | high | mitigate | Prohibit physical/preflight commands and require the complete tracked device-evidence directory to remain byte-identical to its protected baseline. |
| T-01-15-04 | Denial of Service | Runtime/evaluator subprocesses | medium | mitigate | Retain existing bounded reads, exclusive temporary outputs, command timeouts, and atomic publish-after-all-pass behavior. |
</threat_model>

## Artifacts this phase produces

- One finalized non-circular three-runtime source binding.
- Three refreshed, current, byte-reproducible compatibility evidence reports.

<verification>
Run both task commands. The final verification must show the ordinary publisher succeeds without a bypass, two consecutive report sets are byte-identical, all focused contract/reference/mutation/dependency checks pass, and `evidence/device/phase-01/` remains unchanged.
</verification>

<success_criteria>
- `scripts/run-three-runtime-agreement` succeeds from the checked-in state against the exact finalized contract-source revision.
- All three reports bind revision `git:a5bff6896188dcac9397c48ce1a6820a7196011a`, one common source-tree digest, three fresh runtimes, and zero disagreements.
- A second ordinary run changes none of the three report bytes.
- No fixture/oracle, contract source, candidate, preflight, signed physical report/checklist, attestation, raw evidence, dependency, or unrelated Xcode/user file changes.
</success_criteria>

<output>
Create `.planning/phases/01-contract-and-device-proof/01-15-SUMMARY.md` when done.
</output>
