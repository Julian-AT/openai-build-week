---
phase: 01-contract-and-device-proof
plan: "12"
type: execute
wave: 9
depends_on: ["01-11"]
files_modified:
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/OrientationGate.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/WorldEpochController.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureAttemptMachine.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/FramePacketBuilder.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureAttemptTests.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/WorldEpochTests.swift
autonomous: true
requirements: [OPS-DEVICE-001, NFR-COORD-001, NFR-CONTRACT-001]
must_haves:
  truths:
    - "Rotation away from portrait rejects the in-flight capture, preserves ARSession, and coaches retry."
    - "World epoch ownership advances world_frame_version whenever reset/relocalization changes coordinate meaning."
    - "A validated directed correction binds base/target versions; if correction is absent, ambiguous, invalid, or reverse-directed, affected data is quarantined and capture remains unavailable."
    - "Atomic rename advances only the internal image_and_metadata_durable state; the packet remains non-visible and non-network-eligible until the authoritative journal record is appended and synced, after which network_eligible exposure may occur."
  prohibitions:
    - "MUST NOT infer rear-LiDAR capability, silently relabel world-frame versions, or publish a partially durable FramePacket."
  artifacts:
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/WorldEpochController.swift
      provides: "Explicit epoch advancement and correction/quarantine owner"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift
      provides: "Atomic capture visibility boundary"
  key_links:
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/WorldEpochController.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CoordinateMath.swift
      via: "validated directed correction"
      pattern: "baseWorldFrameVersion|targetWorldFrameVersion"
  decisions: [D-01, D-02, D-03, D-04, D-06]
---
<objective>Implement explicit orientation, world-epoch, quarantine, and atomic minimal FramePacket capture semantics.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/canonical/DEVELOPMENT_STRATEGY.md
@docs/contracts/frame-packet.schema.json
@docs/contracts/rrcap-manifest.schema.json
@ios/Packages/ReRoomContracts/</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Own orientation attempts and world-epoch correction or quarantine</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/OrientationGate.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/WorldEpochController.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureAttemptMachine.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureAttemptTests.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/WorldEpochTests.swift</files>
  <read_first>docs/canonical/MASTER_TECHNICAL_SPEC.md, docs/adr/ADR-003-arkit-authority-and-coordinates.md, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CoordinateMath.swift, docs/contracts/frame-packet.schema.json</read_first>
  <behavior>Attempt snapshots portrait and epoch; rotation rejects attempt without stopping session. Reset/relocalization always advances version. Only a finite rigid correction with matching directed base/target versions releases quarantined data; otherwise quarantine persists.</behavior>
  <action>Per D-01, D-02, D-03, D-04, and D-06, implement one `WorldEpochController` as sole version owner and a capture state machine. Make correction validation explicit and direction-sensitive; emit correction evidence only after validation. On unknown alignment, wrong version/direction, invalid transform, or missing correction, quarantine old/new affected observations and disable capture; orientation rejection preserves the healthy session and coaches retry.</action>
  <acceptance_criteria>Tests prove epoch increment, valid correction release, absent/ambiguous/invalid/reverse correction quarantine, landscape mid-attempt rejection, session preservation, and retry coaching.</acceptance_criteria>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/CaptureAttemptTests -only-testing:ReRoomDeviceProofTests/WorldEpochTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>Epoch changes have one owner and always result in validated correction or explicit quarantine.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build schema-valid FramePackets with atomic journal visibility</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/FramePacketBuilder.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureAttemptTests.swift</files>
  <read_first>docs/contracts/frame-packet.schema.json, docs/contracts/rrcap-manifest.schema.json, docs/canonical/MASTER_TECHNICAL_SPEC.md, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/ContractValidation.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/WorldEpochController.swift</read_first>
  <behavior>Minimal packet contains synchronized camera/intrinsics/pose/orientation/crop/version/hash fields and follows selected → internal image_and_metadata_durable → journaled → network_eligible. Invalid/quarantined attempts reject. Crashes before rename expose nothing; a crash after rename but before authoritative journal sync leaves durable internal bytes that remain non-visible and non-network-eligible.</behavior>
  <action>Build and schema-validate bounded packet bytes, write temp image and metadata, fsync required files/directories, and atomically rename only to establish CON-001 internal `image_and_metadata_durable`. Under CON-002 schema authority, append/sync journal entries with exactly `journal_sequence`, `monotonic_timestamp_ns`, `entry_type`, `reference_id`, and `content_sha256`; use `entry_type=frame` for the FramePacket and its RR-JCS-SHA256-1 canonical packet digest, and schema-owned event records/names `frame_selected`, `frame_image_and_metadata_durable`, `frame_journaled`, and `frame_network_eligible` for the Phase 1 lifecycle (do not emit `frame_server_acknowledged`). Enforce one journal authority contiguous from zero; event records use exactly `event_id`, `event_sequence`, `durable_journal_sequence`, `monotonic_timestamp_ns`, `type`, `payload_sha256`, `payload_path`, `record_sha256_algorithm`, `record_sha256_scope`, and `record_sha256`, including the schema-owned self-omitting record digest. Only after the authoritative frame/journaled event records are synced may CON-001 transition to `journaled` and expose `network_eligible`. Make `accepted_frame_order` and `events` exact journal-order projections with matching reference, durable sequence, per-type contiguous sequence, and content hash. Inject crash points before rename, after rename/before journal, during journal append/sync, and after journal sync. Recovery accepts only the hash-valid contiguous prefix, sets `finalization.state=recovered_prefix`, requires `last_durable_journal_sequence` to equal the final accepted sequence, verifies both projections exactly, and recomputes the schema-owned replay digest from the JCS array of `[journal_sequence,entry_type,reference_id,content_sha256]` tuples. Do not invent fields or collapse rename into visibility.</action>
  <acceptance_criteria>Tests cover CON-001 schema/hash validity, invalid attempt rejection, and all crash points; rename-before-journal remains internally durable but absent from visible/network-eligible enumeration. CON-002 tests reject gaps/reordering, wrong event names/fields, bad record or tuple digest, projection mismatch, wrong last durable sequence, and an invalid recovered prefix; post-journal-sync exposes exactly one verified packet with exact frame/event projections.</acceptance_criteria>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/CaptureAttemptTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>Minimal capture is hash-valid, durable, atomic, and recoverable.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Tampering | world epoch/correction | high | mitigate | Single owner, monotonic version, directed validation, fail-closed quarantine. |
| T-01-16 | Tampering/Repudiation | capture durability | high | mitigate | Schema/hash before fsync+rename; authoritative journal append+sync before any network-eligible visibility; crash-prefix tests. |
</threat_model>
## Artifacts this phase produces
- Explicit epoch/correction/quarantine owner, atomic capture journal, and crash tests.
<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>Every world reset is corrected or quarantined, and no partial capture becomes authoritative.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-12-SUMMARY.md`.</output>
