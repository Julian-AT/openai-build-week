---
phase: 01-contract-and-device-proof
plan: "14"
type: execute
wave: 11
depends_on: ["01-13"]
files_modified:
  - evidence/device/phase-01/automated-preflight.json
  - evidence/device/phase-01/gate-013-report.json
  - evidence/device/phase-01/gate-013-operator-checklist.json
  - evidence/device/phase-01/gate-002-report.json
  - evidence/device/phase-01/gate-002-operator-checklist.json
autonomous: false
requirements: [OPS-DEVICE-001, NFR-COORD-001, NFR-CONTRACT-001]
must_haves:
  truths:
    - "Physical evidence comes from the declared signed build on the base iPhone path and never from simulator, synthetic, or fabricated observations."
    - "GATE-013 verifies install/launch, camera and microphone grant/deny authorization evidence, ARKit tracking, horizontal/vertical planes, recovery of a minimal hash-valid journaled FramePacket capture, signing, declared iOS 26.0 proof-baseline compatibility, and no rear-LiDAR requirement; it records no audio."
    - "GATE-002 verifies portrait geometry, landscape rejection/coaching, and one explicit physical world reset whose world_frame_version advances and is bound to either a valid directed correction or explicit quarantine; synthetic fixtures do not replace physical proof."
    - "Each gate becomes GREEN only after automated checks pass, external raw evidence resolves by opaque ID/digest, and an accountable human signs the bound checklist."
    - "Schema-valid RED remains exact D-16 failure evidence, keeps the candidate unpromoted, and blocks dependent mobile work; Phase 1 completion and gate mode fail unless both physical gates are exactly GREEN, while UNRUN/RUNNING remain incomplete."
    - "Under D-05, the candidate device-proof seed is promoted to the Mode A production seed only when GATE-013 is GREEN; RED, RUNNING, UNRUN, or waiver leaves it unpromoted and blocks dependent mobile work."
  prohibitions:
    - "MUST NOT mark GATE-002 or GATE-013 GREEN from automation alone, a simulator, missing external raw evidence, an unsigned checklist, or fabricated physical evidence."
  artifacts:
    - path: evidence/device/phase-01/gate-013-report.json
      provides: "Sanitized signed-device gate evidence"
    - path: evidence/device/phase-01/gate-002-report.json
      provides: "Sanitized physical coordinate gate evidence"
  key_links:
    - from: evidence/device/phase-01/gate-013-operator-checklist.json
      to: evidence/device/phase-01/gate-013-report.json
      via: "report digest and human signature digest"
      pattern: "report_sha256"
  decisions: [D-04, D-05, D-13, D-14, D-15, D-16]
---
<objective>Run automated preflight and obtain honest physical/human GATE-013 and GATE-002 decisions.</objective>
<execution_context>@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md</execution_context>
<context>@docs/canonical/RISK_AND_KILL_GATES.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@evidence/templates/gate-report.schema.json
@evidence/templates/operator-checklist.schema.json</context>
<tasks>
<task type="auto">
  <name>Task 1: Produce automated preflight bound to the candidate signed build</name>
  <files>evidence/device/phase-01/automated-preflight.json</files>
  <read_first>docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, evidence/templates/gate-report.schema.json, scripts/verify-phase-01-contracts, scripts/verify-reroom-release-surface</read_first>
  <action>Run `full` as the complete automated preflight: three-runtime, mutation, schema, unit/UI (including camera/microphone authorization state tests and rename-before-journal crash test), release-surface, secret, diff, archive, signing-entitlement, and evidence-template checks. On success, `full` atomically emits the sanitized preflight with candidate build digest and environment; validate that output immediately. Leave both physical gates UNRUN/RUNNING; `full` neither reads nor requires their reports/signatures and cannot approve/waive them.</action>
  <acceptance_criteria>Preflight is valid, all automated checks are green, signing/archive metadata is bound, and no gate is GREEN/WAIVED.</acceptance_criteria>
  <verify><automated>scripts/verify-phase-01-contracts full &amp;&amp; python3 tools/verify/verify_evidence.py --file evidence/device/phase-01/automated-preflight.json</automated></verify>
  <done>A reproducible automated baseline is bound to the physical-test candidate.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run and sign GATE-013 on the declared base iPhone</name>
  <files>evidence/device/phase-01/gate-013-report.json, evidence/device/phase-01/gate-013-operator-checklist.json</files>
  <read_first>evidence/device/phase-01/automated-preflight.json, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, evidence/templates/operator-checklist.schema.json</read_first>
  <action>Per D-05, D-13, D-14, D-15, and D-16, the human operator installs/launches the exact signed candidate on the declared base iPhone path and executes separate camera grant/deny and microphone grant/deny authorization cases, including proof that camera denial blocks visual capture while microphone denial disables only optional microphone readiness and leaves camera/AR/minimal visual FramePacket/typed-tap paths available. Continue with tracking, horizontal/vertical plane callbacks, minimal hash-valid journaled FramePacket capture and recovery, signing, declared iOS 26.0 proof-baseline, capability, external raw-store resolution, and no-rear-LiDAR checks. Capture authorization state only—do not record audio or add audio capture scope. Record sanitized model/OS/Xcode/build/capabilities and opaque raw IDs/digests; sign the report-bound checklist. Promote the candidate to Mode A production seed only if the validated signed report is GREEN. RED or any pending/non-GREEN state leaves it explicitly unpromoted and invokes D-16 dependent-mobile stop routing.</action>
  <acceptance_criteria>Report/checklist validate as evidence in either GREEN or RED, bind exact preflight/build/raw digests, include separate physical camera and microphone grant/deny outcomes without audio recording, prove recovery of the hash-valid journaled short capture, verify the declared proof baseline, and contain an accountable human signature digest. RED is retained but cannot satisfy gate/phase completion. Only GREEN authorizes D-05 promotion; every other state remains unpromoted.</acceptance_criteria>
  <verify><automated>python3 tools/verify/verify_evidence.py --file evidence/device/phase-01/gate-013-report.json --checklist evidence/device/phase-01/gate-013-operator-checklist.json</automated></verify>
  <done>GATE-013 has real signed-device and human evidence; GREEN conditionally promotes under D-05, while valid RED evidence leaves the phase incomplete, candidate unpromoted, and dependent mobile work blocked.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run and sign GATE-002 portrait/landscape physical proof</name>
  <files>evidence/device/phase-01/gate-002-report.json, evidence/device/phase-01/gate-002-operator-checklist.json</files>
  <read_first>evidence/device/phase-01/automated-preflight.json, evidence/device/phase-01/gate-013-report.json, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, fixtures/policies/RR-COORD-1/rev-001/manifest.json</read_first>
  <action>Per D-04, D-13, D-14, D-15, and D-16, the human operator runs the versioned checkerboard/geometry portrait procedure and landscape capture-rejection/coaching negative case on the same bound device/build. Then perform one explicit physical AR world reset/relocalization that changes coordinate meaning: prove `world_frame_version` increments, and bind the signed evidence either to a finite rigid directed correction with matching from/to versions or to explicit quarantine with capture/network eligibility disabled. Bind sanitized measured results and opaque raw IDs/digests, verify external retention, then sign the report-bound checklist. Record RED for failed thresholds, missing epoch advance, invalid/ambiguous correction, or missing required quarantine; never substitute synthetic cases for physical evidence.</action>
  <acceptance_criteria>Report/checklist are schema-valid as GREEN or RED evidence; every physical metric, landscape negative, and reset case is present; reset evidence shows monotonic world_frame_version advancement plus either a valid directed correction or explicit quarantine; and raw references resolve/digest-match. The final `gate` command and task/phase completion succeed only when this report and GATE-013 are both GREEN; RED remains recorded D-16 evidence but fails completion.</acceptance_criteria>
  <verify><automated>python3 tools/verify/verify_evidence.py --file evidence/device/phase-01/gate-002-report.json --checklist evidence/device/phase-01/gate-002-operator-checklist.json &amp;&amp; scripts/verify-phase-01-contracts gate</automated></verify>
  <done>GATE-002 has real physical/human evidence; only two GREEN gates complete Phase 1, while any RED is retained and blocks completion/dependent mobile work.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-19 | Spoofing/Repudiation | physical gates | critical | mitigate | Exact build/device binding, raw digests, accountable human signatures, no automation approval. |
</threat_model>
## Spec-less Probe Mapping
| Probe row | Resolution |
|---|---|
| OPS-DEVICE-001 unclassified | ASSUMPTION: the canonical base-iPhone test device supports the recorded iOS 26 deployment baseline; Task 2 must verify the OS/device/build facts and record RED if false. |

## Artifacts this phase produces
- Sanitized automated preflight and signed GATE-013/GATE-002 report/checklist pairs.
<verification>Run every task command; require real external raw evidence and human signatures before any GREEN claim.</verification>
<success_criteria>Phase 1 completes only when both physical gates are honestly GREEN with bound automated and human evidence; RED remains schema-valid D-16 evidence but forces nonzero gate status, no promotion, and dependent-mobile stop routing.</success_criteria>
<output>Create `.planning/phases/01-contract-and-device-proof/01-14-SUMMARY.md`.</output>
