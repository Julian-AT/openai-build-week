---
phase: 05-curated-replacement-vertical
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/ReplaceReducer.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/ReplaceReducerTests.swift
autonomous: true
requirements: [FR-REPLACE-001]
must_haves:
  truths:
    - "A valid replace preview emits exactly set_object_visibility then create_asset_instance and changes no revision."
    - "Target, visibility, readiness, view fixture, support, asset policy, world, revision, and integrity failures reject before mutation."
    - "Confirmation replays the preview against current state, commits one r+1 scene, and captures one exact restore_snapshot inverse."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/ReplaceReducer.swift
      provides: "Pure replace preview/cancel/confirm reducer"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/ReplaceReducerTests.swift
      provides: "Exact order, rejection, inverse, and nonmutation tests"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/ReplaceReducer.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
      via: "projection diff verification and captured-exact inverse"
---

<objective>
Implement the contract-first replace reducer for the no-reveal sprint fallback.

Purpose: Make the signature edit deterministic before durability or UI wiring.
Output: Tested pure reducer with exact CON-005 semantics.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/05-curated-replacement-vertical/05-CONTEXT.md
@.planning/phases/05-curated-replacement-vertical/05-RESEARCH.md
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/PlaceReducer.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
</context>

<feature>
  <name>Exact no-reveal replace reduction</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/ReplaceReducer.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/ReplaceReducerTests.swift</files>
  <behavior>
    - RED first for the exact two-operation order, visible tracked target, stable selected target ID, degraded local fallback capability, bounded supported-view fixture, current support/world/revision, allowlisted local asset, collision, provenance/license, and artifact integrity.
    - Intent bytes may select only the allowlisted asset ID; target, transforms, support, readiness, and policy verdicts come from deterministic candidate values.
    - Preview and cancel keep `scene_revision` unchanged; confirm requires matching explicit confirmation and replays the whole reduction against current state.
    - Pending state hides only the selected object and creates the replacement asset/support last. Any mismatch leaves the input scene unchanged.
    - The inverse is one RR-EDIT-PROJECTION-1 `restore_snapshot` from committed projection to the exact prior projection with the exact required artifact union.
  </behavior>
  <implementation>Mirror the proven PlaceReducer lifecycle but define replace-specific candidate/readiness/view fields and rejection codes. Reuse existing ProxyAssetCandidate, DeterministicSupportCandidate, TransactionOperation, EditProjectionEngine, validation, preview, and confirmation types where their meaning is exact. Emit canonical validation checks including scene_revision, artifact_integrity, target_exists, capability_ready, view_envelope, support, collision_proxy, and asset_license. Do not emit a reveal operation or claim measured geometry.</implementation>
</feature>

<verification>
<automated>swift test --package-path ios/Packages/ReRoomContracts --filter ReplaceReducerTests</automated>
</verification>

<success_criteria>
- Reducer tests prove exact order, atomic state, captured inverse, and every fail-closed boundary.
- No dependency, schema, provider, cloud, or Xcode project change is introduced.
</success_criteria>

<output>Create `.planning/phases/05-curated-replacement-vertical/05-01-SUMMARY.md` when done.</output>
