---
phase: 05-curated-replacement-vertical
plan: "02"
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
autonomous: true
requirements: [FR-REPLACE-001]
must_haves:
  truths:
    - "NativeBranchAuthority is the sole replace commit owner; same-key/same-fingerprint retry returns one receipt and one revision."
    - "Changed fingerprint, stale base, wrong authority/world, divergence, invalid confirmation, or durability fault publishes no replacement."
    - "A committed replace survives recovery/replay and a later restore compensates it without deleting new unrelated state."
    - "The Phase 5 canonical scene contains the same stable controlled object as the manual target; incompatible old empty-object stores fail closed for replace."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      provides: "Replace preview/commit through sole durable branch authority"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift
      provides: "Idempotency, recovery, replay, and restore coverage"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      provides: "Versioned controlled-object bootstrap and replace candidate binding"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
      via: "pointer-last generation activation before visible acknowledgement"
---

<objective>
Make replace durable, idempotent, restart-safe, and restorable through the sole native authority.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-curated-replacement-vertical/05-01-SUMMARY.md
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable authority replace lifecycle</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift</files>
  <action>RED first, then add `previewReplace` and `commitReplace` using the same fingerprint/idempotency/divergence/undo-token/confirmation/activation invariants as place. Same-key retry must resolve before CAS; changed fingerprint must conflict. Commit only after durable generation activation. Cover restart recovery, injected activation failures, exact journal replay, immutable history, and restore compensation including preservation of a newly tracked unrelated object.</action>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter 'TransactionAuthorityTests|TransactionStoreCrashTests|RestoreReducerTests'</automated></verify>
  <done>Replace commits exactly once, survives restart, fails closed on faults, and restores through a new revision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the stable target into canonical bootstrap</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift</files>
  <action>Version the Phase 5 room-edit store/bootstrap so a fresh scene contains one controlled visible tracked object with the exact `RoomEditIdentity.targetObjectID`, current branch/world identity, and honest independent readiness. Selection still changes no revision. Detect a recovered empty-object generation and keep replace unavailable with an explicit migration/restart reason rather than silently inventing canonical state. Bind typed/tap replace proposals and deterministic local candidates without weakening place/restore behavior.</action>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/RoomEditModelTests</automated></verify>
  <done>The app and transaction authority agree on one target identity and old incompatible state cannot authorize replace.</done>
</task>

</tasks>

<verification>
- Focused package authority/store/restore tests pass.
- Focused app model tests pass with place/restore regressions retained.
</verification>

<output>Create `.planning/phases/05-curated-replacement-vertical/05-02-SUMMARY.md` when done.</output>
