---
phase: 06-controlled-multi-surface-removal
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift
autonomous: true
requirements: [FR-REMOVE-001]
must_haves:
  truths:
    - "NativeBranchAuthority is the sole remove commit owner; an exact retry returns the original receipt without a second revision."
    - "Changed fingerprint, stale state, divergence, invalid confirmation, missing local reveal bytes, or activation fault publishes no hidden object."
    - "A committed demo remove survives recovery and exact replay, then restore reactivates the original reveal/visibility state through a new revision."
    - "The source remove record stays immutable and unrelated/new object state survives compensation."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      provides: "Sole-writer remove preview/commit and durable activation"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift
      provides: "Retry, recovery, replay, and restore behavior"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift
      provides: "Pointer-last remove-generation fault coverage"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
      via: "complete generation activation before in-memory publication or receipt"
---

<objective>
Make the exact remove transaction idempotent, durable, restart-safe, replayable, and restorable.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-controlled-multi-surface-removal/06-CONTEXT.md
@.planning/phases/06-controlled-multi-surface-removal/06-01-SUMMARY.md
@.planning/phases/05-curated-replacement-vertical/05-02-PLAN.md
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionStore.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the sole-authority remove lifecycle and exactly-once retry</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/TransactionAuthority.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift</files>
  <behavior>
    - `previewRemove` reduces only against the active scene and `commitRemove` performs no suspension inside fingerprint, retry, CAS, reduction, generation construction, activation, and publication.
    - Same-key/same-fingerprint lookup occurs before current-revision replay and returns the original receipt; changed content conflicts.
    - Missing local reveal artifact, authority freeze, stale preview, wrong confirmation/undo token, or reduction mismatch leaves active scene/history/receipts byte-equivalent.
    - Successful commit persists one immutable remove record with exact operations/inverse and publishes r+1 only after store activation.
  </behavior>
  <action>RED first, then extend `NativeBranchAuthority` through the same synchronous actor-isolated critical path used by place/replace. Reuse narrow transaction construction and activation helpers without changing fingerprint scope or restore semantics. Require the demo reveal reference in the local artifact inventory. Do not add a renderer-side or view-side revision path.</action>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter TransactionAuthorityTests</automated></verify>
  <done>Authority tests prove one durable revision, exact retry, changed-body conflict, and fail-closed rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove crash recovery, replay, and captured-exact restore</name>
  <files>ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionAuthorityTests.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/TransactionStoreCrashTests.swift</files>
  <behavior>
    - Every injected generation-write/rename/sync/pointer boundary recovers either the prior visible object or the complete committed remove, never a partial hidden state.
    - Restarted authority returns the prior retry receipt and exposes the same ordered remove record/projection digest.
    - Offline restore makes a new r+1 compensation over current state, clears the demo reveal, restores visibility, retains unrelated additions, and never modifies the source transaction.
  </behavior>
  <action>Extend isolated in-memory/fault fixtures with a locally available deterministic reveal reference. Exercise the existing `TransactionStore` boundaries rather than creating a remove-specific store. Add remove-to-restore assertions through `RestoreReducer` and the authority, including exact required-artifact union and immutable source bytes.</action>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter 'TransactionAuthorityTests|TransactionStoreCrashTests|RestoreReducerTests'</automated></verify>
  <done>Crash/restart/replay/restore tests pass with no partial state or duplicate mutation.</done>
</task>

</tasks>

<verification>
- Focused authority, store-crash, and restore suites pass.
- No dependency, schema, network, provider, or Xcode project change is introduced.
</verification>

<output>Create `.planning/phases/06-controlled-multi-surface-removal/06-02-SUMMARY.md` when done.</output>
