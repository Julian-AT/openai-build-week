---
phase: 06-controlled-multi-surface-removal
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RemoveReducer.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/RemoveReducerTests.swift
autonomous: true
trace_requirements: [FR-REMOVE-001]
must_haves:
  truths:
    - "A demo-fixture remove preview emits exactly set_reveal_bundle then set_object_visibility and changes no revision."
    - "Target, world, revision, capability classification, pose-envelope, and reveal-artifact failures reject without mutation."
    - "Confirmation creates one atomic r+1 pending scene and one captured-exact restore_snapshot inverse."
    - "The validator identifies degraded_demo_fixture and never represents the fixture as ready, measured, observed, or gate-passing."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RemoveReducer.swift
      provides: "Pure reveal-first remove preview/cancel/confirm reducer"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/RemoveReducerTests.swift
      provides: "Exact order, rejection, artifact-union, and inverse tests"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RemoveReducer.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/EditProjection.swift
      via: "canonical ordered projection diff and captured-exact inverse"
---

<objective>
Implement the exact reveal-first remove reducer for the explicitly degraded sprint fixture.

Purpose: Prove canonical transaction behavior without manufacturing the physical evidence required by GATE-006.
Output: Tested pure reducer with a closed demo-fixture authorization boundary.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/SPRINT-CUT-36H.md
@.planning/phases/06-controlled-multi-surface-removal/06-CONTEXT.md
@.planning/phases/06-controlled-multi-surface-removal/06-RESEARCH.md
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/adr/ADR-009-multi-surface-reveal.md
@docs/adr/ADR-012-transaction-and-offline-restore.md
@docs/contracts/transaction.schema.json
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/ReplaceReducer.swift
@ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RestoreReducer.swift
</context>

<feature>
  <name>Exact degraded-fixture remove reduction</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/RemoveReducer.swift, ios/Packages/ReRoomContracts/Tests/ReRoomTransactionCoreTests/RemoveReducerTests.swift</files>
  <behavior>
    - RED first: only a typed/tap remove proposal bound to one selected tracked visible object, the current native branch/world/revision, one current `envelope_*` fixture, and one locally qualified `reveal_bundle` reference can preview.
    - RED first: the fixture authorization is deterministic policy output labeled `degraded_demo_fixture`; intent bytes cannot provide the target, pose, envelope, artifact verdict, readiness, confirmation, or transforms.
    - RED first: operations are exactly `set_reveal_bundle(nil -> fixture artifact)` followed by `set_object_visibility(true -> false)` for the same object; operation reversal, omission, addition, stale before-state, or artifact-union mismatch rejects.
    - RED first: preview/cancel preserve the input scene and revision; confirm replays the complete preview, requires explicit native confirmation, applies both deltas atomically at r+1, and creates one RR-EDIT-PROJECTION-1 captured-exact inverse.
    - RED first: validation uses scene_revision, target_exists, capability_ready, view_envelope, and artifact_integrity with validator `RR-DEMO-REMOVE-VALIDATOR-1`; it records no coverage, votes, seam, foreground, observed-atlas, provider, or physical result.
  </behavior>
  <implementation>Mirror the proven ReplaceReducer value/replay lifecycle while defining remove-specific candidate, seed, reduction, and typed rejection values. Reuse `TransactionOperation.setRevealBundle`, `setObjectVisibility`, `EditProjectionEngine`, existing explicit confirmation/request types where exact, and `restoreSnapshot`. Require the reveal reference in both the reveal delta and required artifact union. Keep normal production readiness outside this reducer entry path unavailable; the accepted policy is explicitly the launch-gated degraded fixture and cannot be interpreted as GATE-006 evidence.</implementation>
</feature>

<verification>
<automated>swift test --package-path ios/Packages/ReRoomContracts --filter RemoveReducerTests</automated>
</verification>

<success_criteria>
- Exact reducer/inverse semantics pass focused Swift Testing coverage.
- FR-REMOVE-001 and GATE-006 remain pending because no physical reveal evidence or human vote was produced.
</success_criteria>

<output>Create `.planning/phases/06-controlled-multi-surface-removal/06-01-SUMMARY.md` when done.</output>
