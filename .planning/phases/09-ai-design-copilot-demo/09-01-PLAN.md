---
phase: 09-ai-design-copilot-demo
plan: "01"
type: execute
wave: 1
depends_on: ["03-07", "04-04", "05-04"]
files_modified:
  - docs/contracts/semantic-proposal.schema.json
  - fixtures/semantic-proposals/1.0.0/rev-001/cases.json
  - gateway/
  - ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilot.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilotView.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift
autonomous: true
requirements: [FR-AGENT-001, SEC-AGENT-001, SEC-CREDENTIAL-001, STR-VOICE-001]
must_haves:
  truths:
    - "GPT-5.6 Sol can visibly propose one typed catalog edit from text or one explicitly consented frame, but cannot authorize target, geometry, confirmation, revision, commit, or restore execution."
    - "Optional Realtime push-to-talk yields only a bounded transcript that re-enters the same Sol/CON-006 path through a short-lived credential."
    - "A proposal is rebound to exact native session/branch/revision/world/selection context and creates at most a revision-neutral preview before separate explicit confirmation."
    - "Typed/tap place, replace, remove, restore, preview, commit, and restore remain complete with the gateway/model/network disabled."
    - "Secrets and room media remain out of source/logs; a camera frame is encoded and sent only after per-send consent."
  artifacts:
    - path: docs/contracts/semantic-proposal.schema.json
      provides: "Closed CON-006 nonmutating semantic proposal envelope"
    - path: fixtures/semantic-proposals/1.0.0/rev-001/cases.json
      provides: "Ten immutable accept/reject semantic vectors bound to the exact schema digest"
    - path: gateway/src/proposal-service.ts
      provides: "Independent model-output validation and trusted-context envelope binding"
    - path: gateway/src/realtime-client-secret.ts
      provides: "Bounded server-owned Realtime session and ephemeral credential subset"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilot.swift
      provides: "Strict native decoder, gateway/Keychain boundary, frame consent, Realtime ingress, and proposal application"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilotView.swift
      provides: "Visible optional AI panel, consent controls, voice control, proposal state, and local catalog"
  key_links:
    - from: gateway/src/openai-responses-client.ts
      to: docs/contracts/semantic-proposal.schema.json
      via: "Strict model-output schema followed by independent runtime validation and server-owned context binding"
      pattern: "json_schema|parseInitialModelOutput|request_context"
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilot.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift
      via: "Strict CON-006 decode/bind then frozen CON-005 submission with model provenance"
      pattern: "decodeStrict|bind|submitUserIntent"
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilotView.swift
      to: ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift
      via: "Create deterministic preview remains separate from Confirm/Cancel"
      pattern: "applyProposal|previewSemanticProposal|Confirm|Cancel"
---

<objective>
Ship a visible, useful, and safely bounded AI design-copilot slice for the
hackathon hero journey without changing ReRoom's deterministic authority.

Purpose: Make Sol vision/semantic reasoning and optional Realtime voice obvious
to judges while preserving the offline typed product and every canonical
transaction/replay invariant.

Output: CON-006 plus executable vectors, a local credential-isolating gateway,
native proposal/voice/UI integration, three digest-bound local assets, and an
honest verification/handoff surface.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ai-design-copilot-demo/09-CONTEXT.md
@.planning/phases/09-ai-design-copilot-demo/09-AI-SPEC.md
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/canonical/PRD.md
@docs/adr/ADR-011-agent-and-deterministic-boundary.md
@docs/contracts/README.md
@docs/contracts/transaction.schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close and implement the Sol/Realtime gateway boundary</name>
  <files>docs/contracts/semantic-proposal.schema.json, fixtures/semantic-proposals/1.0.0/rev-001/cases.json, gateway/</files>
  <behavior>
    - "A strict Sol result over the server-owned three-item catalog becomes one context-bound CON-006 envelope."
    - "Unknown fields/assets, transforms, authority, URLs, unsafe copy, status/nullability contradictions, duplicate/noncanonical constraints, oversized input, weak shapes, and upstream failures reject generically."
    - "The standard API key stays server-side; protected routes require a constant-time bearer comparison and emit sanitized logs only."
    - "Realtime returns only a validated 600-second credential/session subset for the fixed model/configuration."
  </behavior>
  <action>Write gateway and immutable-vector tests first. Use the exact OpenAI SDK and strict Structured Outputs, then independently parse the untrusted result. Bind trusted native context in code after the model call. Keep the gateway stateless, bounded, timed out, no-CORS, credential-redacted, and dependency-minimal. Do not add an agent framework, persistence, model-selected URL, or model mutation tool.</action>
  <verify><automated>cd gateway &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; npm audit --omit=dev</automated></verify>
  <done>Gateway paths are exact-version, fail closed, executable from immutable vectors, and incapable of expanding model authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind strict proposals and optional voice into the native deterministic journey</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomTransactionCore/IntentBoundary.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilot.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DesignCopilotView.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/RoomEditView.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/RoomEditModelTests.swift</files>
  <behavior>
    - "Native decoding rejects unknown/unsafe/stale CON-006 and exact-context mismatch before preview."
    - "Vision maps to frozen CON-005 typed ingress with explicit semantic-model provenance; voice requires provenance."
    - "Ask encodes at most one bounded upright JPEG after explicit per-send consent and never from the render loop."
    - "Realtime audio is bounded/cancellable and only a completed safe transcript reaches Sol; failure retains typed/tap."
    - "Apply creates a preview only; Confirm/Cancel remain separate native controls and revisions remain unchanged until confirmation."
  </behavior>
  <action>Write native regression tests first. Add a strict Codable/shape/semantic decoder, exact native context comparison, Keychain-backed bearer token, HTTPS-or-local-root gateway restriction, bounded one-frame encoding, bounded push-to-talk transcript path, and a prominent accessible AI panel. Reuse the existing IntentBoundary/reducers/authority; do not create a second mutation path.</action>
  <verify><automated>Run the clean-source Xcode `RoomEditModelTests` suite and the full `ReRoomContracts` Swift package suite.</automated></verify>
  <done>Sol/voice can populate only the same deterministic preview boundary, and all offline transaction paths remain independent.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize authority, evaluation, archive coverage, and 24-hour GSD handoff</name>
  <files>docs/canonical/, docs/adr/ADR-011-agent-and-deterministic-boundary.md, docs/contracts/README.md, docs/audit/ARCHIVE_MASTER_PLAN_COVERAGE.md, .planning/</files>
  <action>Record CON-006, the bounded native WebSocket hackathon amendment, official OpenAI evidence, requirements/test/eval traceability, immutable vectors, archive-plan coverage, optimized adaptive GSD config, kill rules, exact run commands, and pending live/device/human claims. Preserve stable IDs and do not edit archive/source.</action>
  <verify><automated>Run GSD health, JSON/plist validation, archive digest comparison, secret scan, link/path checks, and git diff --check.</automated></verify>
  <done>The next session has one authoritative AI contract, an executable plan, honest evidence state, and an unambiguous 24-hour route.</done>
</task>

</tasks>

<verification>
Automated completion requires all gateway tests/typecheck/build/audit, full Swift
package tests, clean-source simulator tests, schema/plist/catalog validation,
GSD health, archive preservation, secret scan, and whitespace checks. Live Sol,
Realtime, physical camera/microphone, design usefulness, latency, and public
demo claims remain pending until separately recorded.
</verification>

<success_criteria>
The AI is visible and useful in the hero UI, but every model/network component
can be removed or fail without changing deterministic P0. No model result can
authorize canonical mutation, and the repository tells the next operator
exactly how to run, test, rehearse, kill, or continue the slice.
</success_criteria>

<output>
Create `.planning/phases/09-ai-design-copilot-demo/09-01-SUMMARY.md` when the
automated implementation slice passes, plus `09-VERIFICATION.md` with live and
human evidence left pending.
</output>
