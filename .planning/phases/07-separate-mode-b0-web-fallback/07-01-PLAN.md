---
phase: 07-separate-mode-b0-web-fallback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/tsconfig.json
  - web/next.config.ts
  - web/next-env.d.ts
  - web/src/lib/replay/types.ts
  - web/src/lib/replay/project-verified-view.ts
  - web/src/lib/replay/load-golden-capture.server.ts
  - web/test/golden-replay.test.mjs
autonomous: true
requirements:
  - FR-WEB-001
must_haves:
  truths:
    - "The web server exposes capture data only after the existing exact Phase 2 Node replay accepts the fixed golden archive."
    - "The verified result contains a serializable, stable-ID timeline, accepted frame preview, manifest/privacy facts, and explicit absent scene/transaction state."
    - "A verifier, report, fixture, runtime, or projection mismatch returns one sanitized rejection and no trusted replay payload."
  artifacts:
    - path: "web/src/lib/replay/types.ts"
      provides: "Closed verified/rejected server result and serializable replay-view contracts"
      exports: ["GoldenReplayResult", "VerifiedReplayView"]
    - path: "web/src/lib/replay/load-golden-capture.server.ts"
      provides: "Server-only fixed-fixture orchestration through the exact Phase 2 replay CLI"
      exports: ["loadGoldenCapture"]
    - path: "web/src/lib/replay/project-verified-view.ts"
      provides: "Pure accepted-report and archive-to-view projection"
      exports: ["projectVerifiedReplay"]
  key_links:
    - from: "web/src/lib/replay/load-golden-capture.server.ts"
      to: "tools/javascript/src/replay.ts"
      via: "fixed-argument exact-Node execFile invocation of the exported runner CLI"
      pattern: "execFile.*replay\.ts"
    - from: "web/src/lib/replay/load-golden-capture.server.ts"
      to: "web/src/lib/replay/project-verified-view.ts"
      via: "projection only after archive.finalized-one-frame accept report validation"
      pattern: "projectVerifiedReplay"
---

<objective>
Create the exact server verification boundary and the serializable recorded-replay contract for D-02 and D-05.

Purpose: Make the existing provider-independent Phase 2 verifier the only route from repository capture bytes to browser-presentable data, with no second verifier or partial-trust path.
Output: A separately locked Next.js package, closed replay-view types, pure projection, server-only fixed-fixture loader, and fail-closed tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-CONTEXT.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-05-SUMMARY.md
@tools/javascript/package.json
@tools/javascript/src/replay.ts
@tools/javascript/test/replay.test.mjs
@fixtures/capture/1.0.0/rev-001/manifest.json
@fixtures/capture/1.0.0/rev-001/archives/finalized-one-frame.rrcap/manifest.json

<interfaces>
From tools/javascript/src/replay.ts:
- `EXACT_NODE_VERSION = "v22.22.3"`.
- CLI requires exactly `--manifest`, `--output-root`, `--repo-root`, and `--implementation-revision`.
- `runReplay` requires a previously nonexistent output root and publishes exactly sixteen canonical reports after complete corpus verification.

Create in web/src/lib/replay/types.ts:
- `GoldenReplayResult = { status: "verified"; replay: VerifiedReplayView } | { status: "rejected"; error: { code: "verification_failed"; message: string } }`.
- `VerifiedReplayView` is composed only of JSON-serializable primitives/plain arrays/objects and includes mode labels, verification/archive identity and digests, ordered events, accepted frames, privacy state, and capability states.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the separate exact-version web package and replay-view contract</name>
  <files>web/package.json, web/package-lock.json, web/tsconfig.json, web/next.config.ts, web/next-env.d.ts</files>
  <action>Per D-05, create a private `web` package pinned to Node 22.22.3, `next@16.2.9`, `react@19.2.7`, `react-dom@19.2.7`, `typescript@6.0.2`, and the exact audited React/Node type packages from 07-RESEARCH. Use scripts for `dev`, `build`, `start`, `typecheck`, and Node's native `test`; create the lock with npm using scripts disabled, then use `npm ci --ignore-scripts` for reproducibility. Configure strict TypeScript and Next App Router without a workspace, external service, telemetry dependency, UI kit, 3D engine, persistence library, or test framework. The package must not modify or absorb `tools/javascript`; that exact package remains the verifier owner.</action>
  <verify>
    <automated>npm ci --prefix tools/javascript --ignore-scripts && npm ci --prefix web --ignore-scripts && npm --prefix web run typecheck</automated>
  </verify>
  <done>The separate web package installs from its exact lock under Node 22.22.3, strict typecheck passes, and its direct dependency set is exactly the audited Next/React runtime plus TypeScript/type development packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project an exact accepted replay through a fail-closed server-only loader</name>
  <files>web/src/lib/replay/types.ts, web/src/lib/replay/project-verified-view.ts, web/src/lib/replay/load-golden-capture.server.ts, web/test/golden-replay.test.mjs</files>
  <behavior>
    - "Exact Phase 2 replay acceptance plus the expected fixture/report/archive identities produces one verified DTO with authoritative event order, one accepted frame, digest/privacy facts, and scene/transactions marked not_present."
    - "Wrong verdict, rejection data, fixture ID/revision/hash, archive name/finalization, count, path, or digest rejects before a DTO exists."
    - "Runner failure, missing report, malformed JSON, missing manifest member, or cleanup failure never returns partial timeline or raw failure details."
    - "The fixed PNG preview is embedded only after its manifest-bound bytes have passed Phase 2 verification."
  </behavior>
  <action>Write the failing Node tests first. Define the closed types, then implement `projectVerifiedReplay` as a pure allowlisted mapping that preserves stable IDs, decimal timestamps as strings, exact sequence values, event payload inspection, frame metadata, the tiny verified PNG data URL, manifest privacy state, and explicit capability states. Implement `loadGoldenCapture` in a module beginning with `import 'server-only'`: use constants for the repository fixture, report case, and pinned Phase 2 implementation revision `git:0d371bc1de9a057cbf61b70142729f6cbe620eec`; create an exclusive temporary parent; invoke the existing `tools/javascript/src/replay.ts` CLI with `execFile(process.execPath, fixedArgs)`, never a shell; require the accepted finalized-one-frame report before reading view members; and remove the temporary parent in `finally`. Keep the public loader argument-free and factor its process/filesystem effects through a module-private test seam so runner, read, and cleanup failures are deterministic without opening a request-controlled production input. Per D-02, no request/search/path value may select a fixture, and no JCS, schema, digest, journal, recovery, or archive validation rule may be reimplemented. Per D-03, catch failures into one non-sensitive rejected result and never include paths, temporary names, archive bytes, or stack text.</action>
  <verify>
    <automated>node --test tools/javascript/test/replay.test.mjs web/test/golden-replay.test.mjs && npm --prefix web run typecheck</automated>
  </verify>
  <done>The exact existing runner is demonstrably invoked, a verified serializable DTO exists only after its accept report, every tested mismatch yields only the closed rejected result, and temporary replay output is removed on success and failure.</done>
</task>

</tasks>

<verification>
- `node --test tools/javascript/test/replay.test.mjs web/test/golden-replay.test.mjs`
- `npm --prefix web run typecheck`
- `git diff --check -- web tools/javascript/src/replay.ts tools/javascript/test/replay.test.mjs`
</verification>

<success_criteria>
- Phase 2 remains the sole replay verifier and its existing tests stay green.
- The only production fixture is the fixed repository golden capture and no client-controlled path reaches filesystem or process arguments.
- Verification rejection exposes zero timeline/frame/manifest payload.
- The DTO is serializable and honestly records absent scene/transaction data and unavailable capabilities.
</success_criteria>

<output>
Create `.planning/phases/07-separate-mode-b0-web-fallback/07-01-SUMMARY.md` when done.
</output>
