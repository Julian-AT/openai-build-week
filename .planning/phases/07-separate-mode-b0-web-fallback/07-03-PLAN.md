---
phase: 07-separate-mode-b0-web-fallback
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - tools/verify/verify_phase_07_b0.py
  - tools/verify/tests/test_phase_07_b0_gate.py
  - scripts/verify-phase-07-b0
  - evidence/web/phase-07/automated-preflight.json
autonomous: true
requirements:
  - FR-WEB-001
  - SEC-RETENTION-001
must_haves:
  truths:
    - "One command independently proves exact Phase 2 replay, web projection/timeline tests, typecheck, production build, source closure, and a local HTTP replay response."
    - "The evidence records only sanitized stable IDs, hashes, exact tool/package versions, named check outcomes, and honest pending states."
    - "The evidence cannot promote an HTTP smoke to browser evidence or promote the sprint slice to FR-WEB-001, SEC-RETENTION-001, or GATE-008 completion."
  artifacts:
    - path: "scripts/verify-phase-07-b0"
      provides: "Fail-closed autonomous Phase 7 check orchestrator"
    - path: "tools/verify/verify_phase_07_b0.py"
      provides: "Independent package/source/evidence/claim verifier"
    - path: "evidence/web/phase-07/automated-preflight.json"
      provides: "Sanitized revision-bound automated sprint-slice evidence"
  key_links:
    - from: "scripts/verify-phase-07-b0"
      to: "tools/verify/verify_phase_07_b0.py"
      via: "generate then independently verify the closed evidence"
      pattern: "verify_phase_07_b0"
    - from: "scripts/verify-phase-07-b0"
      to: "web/src/app/page.tsx"
      via: "built local server HTTP response contains exact honest labels and verified fixture identity"
      pattern: "(next start|npm.*start)"
---

<objective>
Close the automated sprint evidence loop for D-04 without converting automation or HTTP output into browser, retention, or full GATE-008 evidence.

Purpose: Make the local B0 replay slice repeatable and independently auditable while keeping every deferred canonical acceptance item explicitly pending.
Output: A fail-closed verifier with mutation tests, one check command, and sanitized byte-stable preflight evidence.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-CONTEXT.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-RESEARCH.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-02-SUMMARY.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@docs/canonical/RISK_AND_KILL_GATES.md
@scripts/verify-phase-02-capture-replay
@tools/verify/verify_phase_02_gate.py

<interfaces>
The evidence producer must emit a closed JSON object containing:
- source revision and closed source digest;
- exact Node/npm/Next/React versions plus fixture ID/revision/manifest hash and archive/report identities;
- named automated checks for Phase 2 replay, web projection/timeline, typecheck, production build, source closure, and local HTTP smoke;
- `browser_smoke: PENDING` unless a separate real browser artifact is supplied and independently validated;
- `FR-WEB-001`, `SEC-RETENTION-001`, and `GATE-008` each exactly `PENDING`;
- a deferred register matching 07-CONTEXT, with no raw page body, room imagery, temporary path, process ID, port, stack, credential, or user/device identifier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and independently validate the closed Phase 7 evidence boundary</name>
  <files>tools/verify/verify_phase_07_b0.py, tools/verify/tests/test_phase_07_b0_gate.py</files>
  <behavior>
    - "A complete evidence object with exact identity/version/hash fields, all declared automated checks PASS, and canonical pending claims accepts."
    - "Missing/extra keys, wrong package/fixture/report identity, non-PASS automated check, promoted browser/gate/requirement state, unsafe detail, or raw/private field rejects."
    - "The tracked web package has only the audited exact direct dependencies and the product source has no upload/session/share/auth/cloud/provider/browser-persistence/live-network implementation surface."
    - "A browser VERIFIED state without a separately bound real-browser artifact rejects."
  </behavior>
  <action>Write mutation tests first using temporary evidence/source copies. Implement a dependency-free Python verifier with a closed allowlist and stable rejection classes. Bind the exact package versions and fixture/report identities from research and Phase 2; compute source and evidence digests independently; inspect the tracked `web` source/lock for the sprint's closed capability surface; allow honest unavailable/not-present status copy while rejecting behavioral surfaces through unexpected dependencies, API/route files, network/process calls outside the server verifier, browser persistence globals, or action controls; require every automated check named in the interface; and reject any wording/state that promotes browser coverage, the three pending IDs, sharing/retention server lifecycle, provider output, scene/transactions, deployment, or Mode A parity. Keep diagnostics to stable file/check/rejection identifiers and never echo response bodies, JSON payloads, temporary paths, or secrets.</action>
  <verify>
    <automated>python3 -m unittest tools.verify.tests.test_phase_07_b0_gate -v</automated>
  </verify>
  <done>The independent verifier accepts only the closed honest preflight shape and all identity, completeness, source-scope, privacy, capability-promotion, and pending-state mutations fail with stable sanitized classifications.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate exact checks, local HTTP smoke, and byte-stable preflight publication</name>
  <files>scripts/verify-phase-07-b0, evidence/web/phase-07/automated-preflight.json</files>
  <action>Create one fail-fast executable that uses isolated temporary files and cleanup traps, disables Next telemetry, installs both exact locks with package scripts disabled, runs the existing Phase 2 Node replay tests, web projection/timeline tests, strict typecheck, and production build, then launches the built app on loopback only and polls it with bounded retries. Treat this as `local_http_smoke`, assert the response contains the four locked labels, verified fixture/archive identity, initial timeline data, local-only/privacy and absent scene/transaction copy, and stop the process cleanly. Run the independent verifier/source-closure checks before atomically publishing a canonical, sanitized evidence file, then independently verify the published file. Per D-04, record `browser_smoke` and the complete canonical browser/fault/ordinary-video/session/share/retention matrix as PENDING because no browser tool or those systems were exercised; record zero learned-provider dependency/call paths without claiming the full gate. Run the complete command twice from the same committed product revision and require identical evidence bytes.</action>
  <verify>
    <automated>scripts/verify-phase-07-b0 full && cp evidence/web/phase-07/automated-preflight.json /tmp/reroom-phase-07-preflight-first.json && scripts/verify-phase-07-b0 full && cmp /tmp/reroom-phase-07-preflight-first.json evidence/web/phase-07/automated-preflight.json && scripts/verify-phase-07-b0 --verify-evidence</automated>
  </verify>
  <done>One command passes every declared automated check and local HTTP smoke twice, publishes identical sanitized evidence, independently re-verifies it, and leaves browser smoke, full fault matrix, FR-WEB-001, SEC-RETENTION-001, and GATE-008 exactly pending.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest tools.verify.tests.test_phase_07_b0_gate -v`
- `scripts/verify-phase-07-b0 full` twice with byte comparison
- `scripts/verify-phase-07-b0 --verify-evidence`
- `git diff --check`
- Scan the four Phase 7 changed-file sets for credentials/private room data before commit.
</verification>

<success_criteria>
- Automated tests, production build, exact replay reuse, source closure, and local HTTP replay response are reproducible from one command.
- Evidence is stable, sanitized, revision-bound, and independently checked.
- No browser, device, cloud, auth, upload, sharing, retention backend, provider, ordinary-video, or full gate evidence is fabricated.
- The only permitted phase claim is that the local fixed-golden-capture B0 replay/inspection sprint slice passed its declared automated checks and HTTP smoke.
</success_criteria>

<output>
Create `.planning/phases/07-separate-mode-b0-web-fallback/07-03-SUMMARY.md` when done.
</output>
