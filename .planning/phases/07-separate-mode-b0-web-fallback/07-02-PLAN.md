---
phase: 07-separate-mode-b0-web-fallback
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - web/src/lib/replay/timeline.ts
  - web/src/components/replay-explorer.tsx
  - web/test/timeline.test.mjs
  - web/src/app/layout.tsx
  - web/src/app/page.tsx
  - web/src/app/error.tsx
  - web/src/app/globals.css
autonomous: true
requirements:
  - FR-WEB-001
  - SEC-RETENTION-001
must_haves:
  truths:
    - "A user sees an unmistakable recorded B0, provider-independent, local-fixture, GATE-008-pending experience rather than a Mode A claim."
    - "A user can scrub the verified authoritative event timeline by range input and keyboard controls, inspect the selected event/frame, and never mutate canonical state."
    - "Privacy and capability panels expose the exact local-only/share/delete manifest facts and say scene/transactions are not present while unsupported features remain unavailable."
    - "A verification failure shows a sanitized closed error surface with no trusted timeline or frame preview."
  artifacts:
    - path: "web/src/components/replay-explorer.tsx"
      provides: "Interactive in-memory timeline, frame, manifest, and capability inspector"
      exports: ["ReplayExplorer"]
    - path: "web/src/app/page.tsx"
      provides: "Server-rendered entry route wired to loadGoldenCapture"
    - path: "web/src/app/globals.css"
      provides: "Responsive accessible recorded-replay visual system without remote assets"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "web/src/lib/replay/load-golden-capture.server.ts"
      via: "direct Server Component call with verified/rejected branching"
      pattern: "loadGoldenCapture"
    - from: "web/src/app/page.tsx"
      to: "web/src/components/replay-explorer.tsx"
      via: "verified replay prop only"
      pattern: "<ReplayExplorer.*replay="
    - from: "web/src/components/replay-explorer.tsx"
      to: "web/src/lib/replay/timeline.ts"
      via: "bounded selected-index transitions"
      pattern: "(selectTimelineIndex|moveTimelineIndex)"
---

<objective>
Deliver the local recorded-replay experience described by D-01, D-03, and D-04 over the verified DTO from Plan 07-01.

Purpose: Let a user replay, scrub, and inspect the golden capture in a separate honest B0 interface without providers, live phone, network, persistence, or mutation authority.
Output: A responsive App Router page, bounded in-memory replay explorer, explicit privacy/capability states, and production build coverage.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestones/v1.0/SPRINT-CUT-36H.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-CONTEXT.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-RESEARCH.md
@.planning/phases/07-separate-mode-b0-web-fallback/07-01-SUMMARY.md
@docs/canonical/PRD.md
@docs/canonical/RISK_AND_KILL_GATES.md
@fixtures/capture/1.0.0/rev-001/archives/finalized-one-frame.rrcap/manifest.json

<interfaces>
Consume from Plan 07-01:
- `loadGoldenCapture(): Promise<GoldenReplayResult>`.
- `GoldenReplayResult` is a closed verified/rejected union.
- `ReplayExplorer` must receive `VerifiedReplayView`, never archive paths/bytes or a rejected/partial object.
- Timeline entries already preserve authoritative event/global-journal sequence and stable IDs; the client selects but does not reorder or reinterpret them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded in-memory timeline interaction and inspection</name>
  <files>web/src/lib/replay/timeline.ts, web/src/components/replay-explorer.tsx, web/test/timeline.test.mjs</files>
  <behavior>
    - "Selection starts at event zero, clamps to the verified event range, and previous/next transitions never produce an invalid index."
    - "Changing the native range input changes the selected stable event detail without changing the verified array."
    - "Zero-event input has a noninteractive empty state rather than an invalid range."
    - "Selected frame/event payloads render as escaped text, and capability/absence values come from the verified DTO."
  </behavior>
  <action>Write the pure timeline transition tests first, then implement `selectTimelineIndex` and `moveTimelineIndex` without mutation, timers, playback inference, or persistence. Build one focused `'use client'` ReplayExplorer that holds only selected index in React state; use a labeled native range input plus previous/next buttons, an `aria-live` selected-event summary, exact sequence/type/timestamp/stable-ID fields, escaped JSON detail, the accepted synthetic frame preview, verification/digest facts, manifest privacy state, and the capability ledger. Per D-01 and D-03, scene and transactions must read `not present in this capture`; share, typed proposals, ordinary video, providers, and live phone are status rows rather than controls. Add no fetch, route call, browser storage, service worker, model, renderer, or mutation command.</action>
  <verify>
    <automated>node --test web/test/golden-replay.test.mjs web/test/timeline.test.mjs && npm --prefix web run typecheck</automated>
  </verify>
  <done>Timeline selection is bounded and keyboard-accessible, selected details follow the verified order, empty and absent states are explicit, and the client component owns no filesystem, network, persistence, verification, or canonical edit behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the verified server result into the honest recorded-replay page</name>
  <files>web/src/app/layout.tsx, web/src/app/page.tsx, web/src/app/error.tsx, web/src/app/globals.css</files>
  <action>Create the App Router root with local metadata and no remote font/assets. Make `page.tsx` a dynamic Server Component that calls `loadGoldenCapture` directly: render the ReplayExplorer only for `status: verified`, otherwise render a sanitized verification-failed panel with zero replay children. The verified page must persistently show `MODE B0 — RECORDED REPLAY`, `PROVIDER-INDEPENDENT`, `LOCAL DEMO FIXTURE`, and `GATE-008 PENDING`; distinguish automated replay acceptance from the still-pending full gate; and explain that closing the tab discards UI selection. Use a dark, high-contrast forensic replay layout with a compact status rail, responsive preview/timeline/inspector grid, clear focus indicators, reduced-motion-safe transitions, and readable long IDs/digests. Per D-03, show exact local-only/share/delete manifest values without claiming server TTL, share invalidation, account, deletion queue, or audit lifecycle. `error.tsx` catches unexpected rendering faults with the same closed, non-sensitive posture.</action>
  <verify>
    <automated>NEXT_TELEMETRY_DISABLED=1 npm --prefix web run build</automated>
  </verify>
  <done>The production build succeeds; verified requests render the complete recorded-replay interface and rejected requests render no trusted data; all locked labels, local-only facts, pending-gate copy, absent scene/transaction states, and unavailable capability states are visible at desktop and narrow layouts.</done>
</task>

</tasks>

<verification>
- `node --test web/test/golden-replay.test.mjs web/test/timeline.test.mjs`
- `npm --prefix web run typecheck`
- `NEXT_TELEMETRY_DISABLED=1 npm --prefix web run build`
- Inspect the built client chunks/source map inventory to confirm the server loader, fixture paths, `child_process`, and Phase 2 runner are absent from client code.
</verification>

<success_criteria>
- The separate root route provides verified replay, scrub, preview, event/frame/digest/privacy inspection with no provider or live connection.
- The experience never claims Mode A parity, scene/transaction content absent from the fixture, or completion of FR-WEB-001, SEC-RETENTION-001, or GATE-008.
- Client state is memory-only and no unavailable capability appears as an actionable control.
- Verification failure and unexpected render failure remain sanitized and fail closed.
</success_criteria>

<output>
Create `.planning/phases/07-separate-mode-b0-web-fallback/07-02-SUMMARY.md` when done.
</output>
