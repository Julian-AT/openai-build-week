---
phase: 02-atomic-capture-and-exact-replay
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureRecovery.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayCore.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayReport.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureRecoveryTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/ReplayCoreTests.swift
autonomous: true
requirements: [FR-B0-001, FR-CAPTURE-001]
must_haves:
  truths:
    - "Launch recovery verifies one contiguous global-journal prefix, quarantines/discards the first invalid entry and all suffix bytes, publishes recovered_prefix over accepted bytes only, and never resumes recording into that archive."
    - "FR-B0-001 adjacency is exact: corrupt suffix recovers only to the immediately preceding valid sequence, while a gap/reorder/interior corruption or unsafe inventory reference fails closed rather than skipping ahead."
    - "FR-B0-001 empty behavior distinguishes a valid finalized session with zero accepted frames from an empty/missing journal or inventory, which returns a precise stable rejection class."
    - "FR-B0-001 ordering comes only from contiguous global journal tuples; timestamp, directory order, manifest array order, per-type sequence, queue completion, and acknowledgement arrival never break ties or repair order."
    - "FR-B0-001 concurrency requires two sequential or concurrent replays over identical finalized or recovered-prefix input to emit byte-identical canonical ReplayReportV1 bytes and expose only hash-verified timeline records."
    - "Unknown version/codec/digest/scope, invalid raw or manifest hash, projection mismatch, and non-prefix corruption fail before any scrub/timeline record is exposed."
  prohibitions:
    - requirement_id: FR-B0-001
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT display, scrub, or derive replay state from a record beyond the last hash-valid contiguous global-journal prefix."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
    - requirement_id: FR-B0-001
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT silently repair a gap/reorder using timestamps, projection arrays, directory order, or provider output."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureRecovery.swift
      provides: "Bounded launch discovery, valid-prefix recovery, and quarantine"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayCore.swift
      provides: "Pure provider/network-independent archive verification and replay"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayReport.swift
      provides: "Closed canonical machine-comparable replay evidence"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayCore.swift
      to: docs/contracts/rrcap-manifest.schema.json
      via: "Closed manifest/inventory validation followed by journal-derived projections"
      pattern: "global_journal_sequence|accepted_frame_order|events"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayReport.swift
      to: fixtures/replay-report.schema.json
      via: "JCS report bytes with only report_sha256 omitted from its self-digest"
      pattern: "report_sha256|ReplayReportV1"
---

<objective>
Implement bounded recovered-prefix publication and pure exact replay over untrusted `.rrcap` input.

Purpose: Make deterministic replay, corruption diagnosis, and scrub authorization available before any provider or live network integration.
Output: Recovery/quarantine module, pure replay verifier/timeline, canonical replay report, and mutation/concurrency proof.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-01-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-02-SUMMARY.md
@docs/contracts/rrcap-manifest.schema.json
@docs/contracts/README.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@fixtures/capture/1.0.0/rev-001/manifest.json

<interfaces>
Consume `CaptureFinalization`, `RecoveredArchive`, `ReplayVerdict`, `ReplayTimelineEntry`, and `ReplayReportV1` from Plan 02-01. `CaptureRecovery.inspect(root:)` returns either a finalized archive, a newly published immutable recovered-prefix archive, or a stable failure diagnostic. `ReplayCore.replay(root:)` is pure with respect to archive bytes and returns only already-verified value snapshots. `ReplayReport.encode(_:)` returns canonical bytes whose digest omits only its own digest field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover and quarantine only the contiguous durable prefix</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureRecovery.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureRecoveryTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift, docs/contracts/rrcap-manifest.schema.json, fixtures/capture/1.0.0/rev-001/manifest.json</read_first>
  <behavior>
    - RED: launch discovery, first-invalid, corrupt-tail, interior-gap, empty-journal, and already-finalized cases fail before recovery exists.
    - Complete finalized/recovered-prefix archives are read-only and never reopened for recording.
    - A torn final JSONL record or corrupt suffix is bounded, copied to diagnostic quarantine with first-invalid sequence and exact SHA-256 outside accepted inventory, then excluded from the atomically published recovered-prefix manifest/journal.
    - Interior gap/reorder, unsafe path, hash mismatch in an accepted record, unknown contract value, or missing required inventory fails closed without rewriting accepted history.
    - Re-running recovery is idempotent and yields identical accepted bytes/status.
  </behavior>
  <action>Write parameterized tests first and prove RED. Then implement bounded archive discovery and verification using `ArchivePath`, exact file/record hashes, closed contract validation, event self-digests, frame/event projection equality, final sequence, and journal tuple input digest. Under the agent's quarantine discretion, retain the invalid suffix as bounded diagnostic bytes plus exact SHA-256 and first-invalid sequence outside the accepted `.rrcap` inventory; atomically publish only the verified prefix with `finalization.state=recovered_prefix`. Never relabel it finalized, silently resume it, skip over corruption, or mutate a complete archive. Keep quarantine content out of Git/evidence output.</action>
  <acceptance_criteria>Tests cover finalized, recovered, zero-frame, torn-final-line, corrupt suffix, first-record corruption, interior gap/reorder, unsafe path, wrong raw/event/packet/manifest hash, unsupported version/codec/digest, repeated recovery, and failure-in-publication rollback.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter CaptureRecoveryTests</automated></verify>
  <done>Launch recovery exposes one immutable verified prefix or one precise failure; no suffix byte enters accepted replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay verified archives into byte-identical reports and timelines</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayCore.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayReport.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/ReplayCoreTests.swift</files>
  <read_first>fixtures/replay-report.schema.json, fixtures/capture/1.0.0/rev-001/manifest.json, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/CanonicalJSON.swift, ios/Packages/ReRoomContracts/Sources/ReRoomContracts/ContractValidation.swift, docs/canonical/GLOSSARY_AND_ID_REGISTRY.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md</read_first>
  <behavior>
    - RED: finalized/recovered replay, concurrent repeatability, exact projection, empty-valid session, and mutation tests fail before the core exists.
    - Replay validates all inventory bytes and the manifest self-hash before walking journal sequence 0...n exactly once.
    - Accepted frames/events and the displayed timeline are derived solely from verified journal entries and match complete manifest projections exactly.
    - Two runs and two concurrent runs produce identical report bytes, report digest, journal/projection/trace digests, verdict, and rejection class.
    - No provider/network import or call exists; provider_lock is empty for Phase 2 fixtures and no learned output enters comparison.
  </behavior>
  <action>Create deterministic tests first and record RED, then implement a pure reader/verifier/report pipeline. Validate bounds, safe paths, raw hashes, packet/image binding, exact event record digest, contiguous global/per-type sequences, exact projections, final sequence, replay input digest, and manifest self-hash before constructing any public timeline item. Encode a closed ReplayReportV1 with exact fixture/implementation/evaluator identity and self-omitting report digest. Mutation tests must alter one independent property at a time, including adjacency, empty/missing data, order, simultaneous replay, digest scope, unknown values, and suffix versus interior corruption. Do not use timing, provider tolerance, directory order, or a live endpoint.</action>
  <acceptance_criteria>Both positive fixture kinds replay twice and concurrently to byte-identical expected reports; the finalized empty-frame fixture succeeds with session events; every declared mutation rejects or recovers at the documented exact prefix and exposes zero unverified timeline entries.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter ReplayCoreTests</automated></verify>
  <done>Exact replay is a pure verified read whose canonical report and timeline are independent of live completion, model, network, and wall-clock order.</done>
</task>

</tasks>

## Artifacts this phase produces

- `CaptureRecovery` launch-time verifier and bounded quarantine publisher.
- `ReplayCore` pure archive reader plus verified authoritative timeline.
- `ReplayReport` canonical evidence encoder and complete mutation/concurrency suites.

<verification>
Run both task commands and the full Swift package suite; run source scans confirming `ReRoomCaptureCore` imports no ARKit, SwiftUI, URLSession, provider, or model module; run `git diff --check`.
</verification>

<success_criteria>
Finalized and recovered-prefix archives replay exactly; corrupt input never leaks past the verified prefix; repeated/concurrent runs are byte-identical with no learned provider or live network.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-03-SUMMARY.md` when done.
</output>
