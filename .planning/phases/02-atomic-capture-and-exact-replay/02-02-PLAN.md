---
phase: 02-atomic-capture-and-exact-replay
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FramePacketEncoder.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureLifecycleTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureCrashMatrixTests.swift
autonomous: true
requirements: [FR-CAPTURE-001, SEC-CONSENT-001]
must_haves:
  truths:
    - "SEC-CONSENT-001 concurrency requires every new session to receive its own explicit authorization before archive creation; concurrent starts cannot reuse, race, or transfer consent across session IDs."
    - "FR-CAPTURE-001 adjacency and ordering are exact: each selected frame advances selected → image_and_metadata_durable → journaled → network_eligible → server_acknowledged, and duplicate, skipped, adjacent inversion, or out-of-session acknowledgement is rejected."
    - "FR-CAPTURE-001 concurrency has one actor allocate all frame/event/global-journal sequences and perform each filesystem transition synchronously without an await/reentrancy seam."
    - "FR-CAPTURE-001 empty behavior finalizes a consented session with no selected frame as a valid empty-frame archive; a denied or malformed consent path writes no directory, journal, event, manifest, or image byte."
    - "Crash/fault injection at every write, sync, rename, append, manifest replacement, and lifecycle edge recovers either no frame or the last complete hash-valid contiguous prefix without corrupting an earlier record."
    - "Explicit stop publishes finalized; interruption publication is never mislabeled finalized, and local replay remains independent of server acknowledgement."
  prohibitions:
    - requirement_id: FR-CAPTURE-001
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT expose, enqueue, or acknowledge a FramePacket whose image/metadata and authoritative journal reference are not both durable and hash-valid."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
    - requirement_id: SEC-CONSENT-001
      category: privacy
      status: unresolved
      verification: null
      statement: "MUST NOT create room-capture bytes or silently reuse consent before the current session ID receives explicit authorization."
      reason: "Product-specific consent prohibition retained; generic GDPR/security canon remains referred to security review."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift
      provides: "Sole actor archive mutation and lifecycle authority"
    - path: ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureCrashMatrixTests.swift
      provides: "Operation-level durability fault and concurrency proof"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift
      via: "One synchronous actor-isolated transaction performs write/sync/rename/append/replace"
      pattern: "actor CaptureArchiveStore|CaptureFileSystem"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FramePacketEncoder.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomContracts/WireFrame.swift
      via: "RRFP-WIRE-1 and exact payload digest validation"
      pattern: "WireFrame|payloadSHA256"
---

<objective>
Implement the consent-bound, five-state, crash-safe capture writer and explicit finalization transaction.

Purpose: Establish the one durable ordering authority before any queue, replay consumer, native adapter, or live provider can observe a frame.
Output: Actor-owned archive writer, complete lifecycle including typed server acknowledgement, finalized manifest publication, and exhaustive fault tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-01-SUMMARY.md
@docs/adr/ADR-004-atomic-capture-and-record-first-replay.md
@docs/contracts/frame-packet.schema.json
@docs/contracts/rrcap-manifest.schema.json
@ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/FramePacketBuilder.swift

<interfaces>
Consume `CaptureSessionAuthorization`, `CaptureSessionDescriptor`, `SelectedFrameCandidate`, `NetworkEligibleReceipt`, `GatewayAcknowledgement`, and `CaptureFinalization` from Plan 02-01. `CaptureArchiveStore` is the only mutable writer. Its public methods are `startSession(authorization:)`, `publishSelectedFrame(_:)`, `recordAcknowledgement(_:)`, `finalizeExplicitly()`, and `snapshot()`. Each method may be invoked with `await` from outside, but its isolated body contains no suspension point while inspecting or mutating archive state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive consented frames through the exact five-state transaction</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FramePacketEncoder.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureLifecycleTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift, docs/contracts/frame-packet.schema.json, docs/contracts/rrcap-manifest.schema.json, docs/canonical/MASTER_TECHNICAL_SPEC.md, .agents/skills/swift-concurrency/references/actors.md</read_first>
  <behavior>
    - RED: exact-state, adjacency, ordering, empty-session, consent-concurrency, and acknowledgement tests fail before the actor exists.
    - Authorization is session-bound and local_only_until_share; denied, stale, mismatched, duplicate, and concurrent cross-session attempts create no capture bytes.
    - A selected candidate appends the five schema-owned event names in order around one CON-001 frame journal record; accepted frame/event projections are exact journal projections.
    - NetworkEligibleReceipt is emitted only after image/packet and journal/event sync; GatewayAcknowledgement must match session, frame, idempotency key, packet digest, and accepted sequence before the fifth event is appended.
    - Explicit finalization appends session_finalized and atomically publishes a self-digested finalized manifest, including a valid zero-frame session.
  </behavior>
  <action>Write behavior tests first and capture the RED failure. Then deepen the Phase 1 journal seed into `CaptureArchiveStore` rather than retaining a second writer. Construct CON-001 bytes with existing `CanonicalJSON`, `ContractValidator`, coordinate policy, and `WireFrame`; atomically publish the image/packet generation, append and sync the sole global journal, and only then return an immutable receipt. Validate typed acknowledgements exactly and append `frame_server_acknowledged` without changing local visibility/durability. Make explicit finalization use the complete verified prefix, exact accepted-frame/event projections, exact journal-tuple digest, `last_durable_journal_sequence`, and manifest hash omitting only `finalization.manifest_sha256`. Keep all state inspection/allocation/filesystem mutation in synchronous actor-isolated code; do not call an async adapter inside the transaction.</action>
  <acceptance_criteria>Parameterized tests prove all five positive transitions, every adjacent invalid transition, duplicate/skip/reorder rejection, zero-frame finalized archive, exact ack matching, wrong-session/ID/digest/sequence rejection, per-session consent under concurrent starts, and byte-valid CON-001/CON-002 output.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter CaptureLifecycleTests</automated></verify>
  <done>A frame is observable outside the writer only as a verified durable receipt, and explicit stop yields an exact immutable finalized archive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every durability edge and earlier-record invariant</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureFileSystem.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureCrashMatrixTests.swift</files>
  <read_first>ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticJournal.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureAttemptTests.swift, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, .agents/skills/swift-testing-expert/references/parameterized-testing.md, .agents/skills/swift-testing-expert/references/parallelization-and-isolation.md</read_first>
  <behavior>
    - RED: parameterized operation-level faults expose missing write/sync/rename/append/manifest/ack coverage.
    - Every injected termination leaves either no accepted frame or one complete hash-valid journaled frame, never a partial visible frame.
    - Earlier durable records remain byte-identical after a later frame fails at any operation.
    - Concurrent publishers receive monotonic unique sequences in actor order; identity/idempotency collisions reject without mutation.
    - A fault after internal rename but before journal eligibility leaves orphan/internal bytes non-visible and recoverable without network reference.
  </behavior>
  <action>Extend the per-test filesystem operation log/fault plan so each write, file sync, directory sync, rename, append, replace, acknowledgement append, and final manifest publication boundary is independently injectable; make production filesystem integration exercise the same observable contract. Parameterize tests with explicit case/expected literals and fresh roots rather than sleeps, shared globals, or broad `.serialized`. Reopen after each fault, prove the contiguous prefix and earlier byte hashes, and assert zero upload/ack reference can exist without its journal-eligible receipt. Keep injected failures and synthetic observations labeled automated; do not create physical or MEASURED evidence.</action>
  <acceptance_criteria>The full fault matrix passes for an empty archive, first frame, later frame, acknowledgement, and finalization; concurrency tests prove unique monotonic journal/event/frame sequences and fail-closed collisions; production filesystem round trips match the memory fake.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter CaptureCrashMatrixTests</automated></verify>
  <done>No operation-level fault corrupts a prior record or makes a non-journaled frame externally visible/network-referenced.</done>
</task>

</tasks>

## Artifacts this phase produces

- `CaptureArchiveStore` actor with exact five-state, acknowledgement, and explicit-finalization APIs.
- `FramePacketEncoder` adapter over existing CON-001/JCS/RRFP authorities.
- Parallel-safe lifecycle, consent-concurrency, identity-collision, and operation-level crash suites.

<verification>
Run both task commands, then `swift test --package-path ios/Packages/ReRoomContracts`; inspect the actor for suspension points inside archive transactions; confirm `Package.resolved` and external dependency locks are unchanged; run `git diff --check`.
</verification>

<success_criteria>
No room byte exists before current-session consent, no frame becomes eligible before durable journal authority, all five states are exact, and every fault leaves a verified immutable prefix.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-02-SUMMARY.md` when done.
</output>
