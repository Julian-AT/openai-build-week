---
phase: 02-atomic-capture-and-exact-replay
plan: "07"
type: execute
wave: 5
depends_on: ["02-05", "02-06"]
files_modified:
  - scripts/verify-phase-02-capture-replay
  - tools/verify/verify_phase_02_gate.py
  - tools/verify/tests/test_phase_02_gate.py
  - evidence/templates/gate-001-physical-observations.schema.json
  - evidence/templates/gate-001-operator-procedure.md
  - evidence/capture/phase-02/automated-preflight.json
  - evidence/capture/phase-02/gate-001-report.json
  - evidence/capture/phase-02/gate-001-checklist.json
autonomous: false
requirements: [FR-CAPTURE-001, FR-B0-001, NFR-REPLAY-001, SEC-CONSENT-001]
must_haves:
  truths:
    - "One automated preflight runs synthetic short/long-duration workload matrices, the five-state crash matrix, exact replay/mutations, queue stress/reordering, consent denial, native simulator flow, and three-runtime agreement without consuming or claiming the physical FX-RRCAP-010S/FX-RRCAP-060S identities."
    - "GATE-001 evidence keeps TARGET/HYPOTHESIS/MEASURED labels exact and can become GREEN only from a human decision bound to real base-device 10-second and 60-second observations plus immutable implementation/fixture/evaluator/environment/raw-artifact digests."
    - "GREEN requires a closed physical crash-observation document containing exactly one bound observation after each canonical state—selected, image_and_metadata_durable, journaled, network_eligible, and server_acknowledged; omission of any state leaves the gate pending or RED."
    - "Physical capture evidence records zero upload references to non-journaled frames, zero earlier-record corruption, exact recovered prefix, two matching replays, bounded queue metrics, and explicit consent/local/upload state—or records RED with the exact miss."
    - "Checked-in evidence is reconstructed from an allowlist and stores sanitized facts plus opaque external artifact IDs/digests only; raw room bytes, private traces, device UUIDs, accounts, signing material, and machine paths stay outside Git."
    - "If physical evidence is absent or incomplete, GATE-001 remains pending/RED and live provider integration remains blocked; simulator/synthetic output never substitutes for human/device proof."
  prohibitions:
    - requirement_id: FR-CAPTURE-001
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT label synthetic, simulator, replay-fixture, or inferred results as physical MEASURED GATE-001 evidence."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
    - requirement_id: SEC-CONSENT-001
      category: privacy
      status: unresolved
      verification: null
      statement: "MUST NOT commit raw room imagery, private traces, device identifiers, signing material, or account data while recording gate evidence."
      reason: "Product-specific evidence boundary retained; generic credential/privacy canon remains referred to security review."
  artifacts:
    - path: scripts/verify-phase-02-capture-replay
      provides: "Deterministic quick/full/gate verification entry point"
    - path: tools/verify/verify_phase_02_gate.py
      provides: "Independent sanitized evidence and human-authority verifier"
    - path: evidence/templates/gate-001-operator-procedure.md
      provides: "Exact physical 10s/60s operator/evaluator procedure"
    - path: evidence/templates/gate-001-physical-observations.schema.json
      provides: "Closed five-state physical termination observation shape required for GATE-001 GREEN"
  key_links:
    - from: scripts/verify-phase-02-capture-replay
      to: evidence/capture/phase-02/automated-preflight.json
      via: "Automation emits only UNRUN/RUNNING/RED sanitized facts and binds exact test outputs"
      pattern: "automated-preflight|RUNNING|RED"
    - from: tools/verify/verify_phase_02_gate.py
      to: evidence/capture/phase-02/gate-001-checklist.json
      via: "GREEN requires human actor, exact decision/checklist digests, and opaque physical evidence bindings"
      pattern: "GATE-001|operator_attestation|GREEN"
    - from: tools/verify/verify_phase_02_gate.py
      to: evidence/templates/gate-001-physical-observations.schema.json
      via: "GREEN validates the bound physical observation document and requires the exact complete five-state set"
      pattern: "selected|image_and_metadata_durable|journaled|network_eligible|server_acknowledged"
---

<objective>
Close automated Phase 2 verification and obtain—or truthfully leave pending—the real human/device GATE-001 evidence required before live integration.

Purpose: Keep deterministic automation, physical measurement, human authority, privacy, and gate state distinct while enforcing the canonical kill rule.
Output: Quick/full/gate verifier, closed five-state physical-observation schema, sanitized evidence pipeline, operator procedure, automated preflight, and human-bound GATE-001 report/checklist.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-05-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-06-SUMMARY.md
@docs/canonical/RISK_AND_KILL_GATES.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md
@evidence/templates/gate-report.schema.json
@evidence/templates/operator-checklist.schema.json
@scripts/verify-phase-01-contracts
@tools/verify/verify_evidence.py

<interfaces>
`scripts/verify-phase-02-capture-replay quick` runs host/package checks; `full` adds simulator/UI and fresh three-runtime publication but never reads physical reports. `gate` reads the completed automated preflight plus GATE-001 report/checklist and, for an attempted human decision, the sanitized external observation document supplied only at runtime through `REROOM_GATE_001_OBSERVATIONS_PATH`; it validates that document against `gate-001-physical-observations.schema.json`, requires its exact digest in the report/checklist/attestation chain, and succeeds only for a bound human GREEN decision with the complete five-state set. Missing runtime observation input stays pending rather than being inferred. Automation can emit UNRUN, RUNNING, or RED only. Physical raw artifacts remain external and are referenced by opaque ID plus lowercase SHA-256.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the fail-closed Phase 2 verifier and evidence boundary</name>
  <files>scripts/verify-phase-02-capture-replay, tools/verify/verify_phase_02_gate.py, tools/verify/tests/test_phase_02_gate.py, evidence/templates/gate-001-physical-observations.schema.json, evidence/templates/gate-001-operator-procedure.md</files>
  <read_first>scripts/verify-phase-01-contracts, tools/verify/verify_evidence.py, tools/verify/tests/test_evidence_templates.py, evidence/templates/gate-report.schema.json, evidence/templates/operator-checklist.schema.json, docs/canonical/RISK_AND_KILL_GATES.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md</read_first>
  <behavior>
    - RED: quick/full/gate routing, evidence sanitization, actor/state authority, report/checklist digest binding, and mutation tests fail before the verifier exists.
    - quick/full reject any failed/missing declared deterministic check and emit no GREEN/WAIVED decision.
    - gate rejects automation-authored GREEN, unsigned/stale/mixed checklist/report, wrong fixture/build/evaluator/environment/raw digest, private/unknown field, and absent 10s/60s physical facts.
    - A closed physical observation record required for GREEN contains exactly the canonical states `selected`, `image_and_metadata_durable`, `journaled`, `network_eligible`, and `server_acknowledged`, each bound to the declared build/device/fixture/external artifact digest and recovered-prefix result.
    - Parameterized omission of any one of the five states, duplicate/unknown state, synthetic/simulator actor, or mismatched observation digest makes GREEN invalid; UNRUN remains pending and an attempted incomplete physical evaluation records RED.
    - RED evidence remains valid failure evidence and the command exits non-success; missing physical evidence remains visibly pending rather than inferred.
  </behavior>
  <action>Write fail-closed unit/mutation tests first. Reuse the exact GateReportV2/operator-checklist human-authority and non-circular digest patterns from Phase 1, restricted to `GATE-001` facts and decisions. Add a closed `gate-001-physical-observations.schema.json` that requires exactly five unique observations and uses an enum containing only `selected`, `image_and_metadata_durable`, `journaled`, `network_eligible`, and `server_acknowledged`; each observation binds the declared physical fixture/run, build revision, sanitized environment, external raw-artifact opaque ID/SHA-256, exact packet/image/journal/recovery facts, and evaluator identity without representing raw room bytes or device UUIDs. The gate verifier must validate that document, recompute its digest, bind the digest through the report/checklist/attestation chain, and require the complete five-state set before accepting human GREEN. Parameterize omission of each state plus duplicate, unknown, reordered binding, stale digest, and simulator/synthetic substitutions; missing physical evidence remains pending, while an attempted incomplete physical decision is RED and exits non-success. Build quick/full/gate modes with explicit allowlisted checks and no error-suppressing fallback comparisons. The operator procedure must name the base-device build, explicit consent/deny/start/stop/background/upload-pause states, physical TARGET 10s/60s runs, mandatory physical termination after all five exact canonical lifecycle states through the instrumented build, queue/blackhole observations, two independent replay/evaluator runs, raw artifact retention outside Git, and RED decision rules. Do not weaken or reinterpret the gate to fit automation.</action>
  <acceptance_criteria>Unit tests prove every human/automation authority branch and digest mutation, including five parameterized missing-state mutations that each reject GREEN; the closed observation schema and procedure require all five canonical physical terminations plus all GATE-001 measurements/threshold/fallback/kill semantics; quick/full/gate modes have deterministic exit behavior and sanitize before any write.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_phase_02_gate -v</automated></verify>
  <done>Phase 2 has one independent verifier that cannot self-approve, fabricate physical evidence, leak raw data, or silently pass an incomplete gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run deterministic preflight and publish sanitized non-physical evidence</name>
  <files>evidence/capture/phase-02/automated-preflight.json, evidence/capture/phase-02/gate-001-report.json, evidence/capture/phase-02/gate-001-checklist.json</files>
  <read_first>scripts/verify-phase-02-capture-replay, evidence/templates/gate-report.schema.json, evidence/templates/operator-checklist.schema.json, evidence/templates/gate-001-operator-procedure.md, evidence/compatibility/replay-agreement.json</read_first>
  <behavior>
    - RED: exporter mutation tests prove a missing/failed check, unknown/private fact, stale binding, or atomic-publication fault prevents preflight publication.
    - full mode freshly executes contract/package, lifecycle/crash, recovery/replay, queue, simulator/UI, release-surface, and cross-runtime checks.
    - Preflight records exact implementation/fixture/evaluator/environment and sanitized result digests with automation actor and RUNNING/RED state only.
    - Initial gate report/checklist remain UNRUN/RUNNING or RED until the physical task supplies real evidence and human attestation.
  </behavior>
  <action>Add exporter/publication mutations to the Task 1 suite, then run `full` from a clean declared toolchain and atomically publish only after all deterministic checks pass. Reconstruct the exact allowlisted evidence shape instead of forwarding arbitrary runner output. Record synthetic queue/cadence/pressure metrics with their HYPOTHESIS/TARGET labels; never promote them to MEASURED. Initialize the physical gate pair truthfully and bind it to the successful preflight without supplying a human decision or physical values.</action>
  <acceptance_criteria>Fresh full preflight passes and publishes one schema-valid deterministic record; privacy/secret scans pass; physical fields remain absent/pending; gate mode does not return success before Task 3.</acceptance_criteria>
  <verify><automated>scripts/verify-phase-02-capture-replay full</automated></verify>
  <done>All automatable Phase 2 invariants are freshly proven and durably recorded without claiming physical GATE-001 completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run and attest the physical GATE-001 10s/60s procedure</name>
  <files>evidence/capture/phase-02/gate-001-report.json, evidence/capture/phase-02/gate-001-checklist.json</files>
  <read_first>evidence/templates/gate-001-operator-procedure.md, evidence/templates/gate-001-physical-observations.schema.json, evidence/capture/phase-02/automated-preflight.json, evidence/templates/gate-report.schema.json, evidence/templates/operator-checklist.schema.json</read_first>
  <action>After automation is complete, use the exact signed base-iPhone candidate and the operator procedure to collect the real 10-second and 60-second capture observations. Physically terminate the instrumented capture after each exact canonical lifecycle state in turn—`selected`, `image_and_metadata_durable`, `journaled`, `network_eligible`, and `server_acknowledged`—and for every run verify packet/image binding, visible upload eligibility, earlier-record integrity, and the exact recovered contiguous prefix. Retain raw room/device evidence outside Git; enter the five sanitized schema-bound observation records with opaque artifact IDs/digests; run the two independent replays/evaluators; and record one accountable human GREEN or RED decision. GREEN is permitted only when all five state records, both duration observations, every canonical threshold, and every privacy/consent subcheck pass. Any missing state or failed check remains pending if unrun or records exact RED after an attempted evaluation, and stops live integration.</action>
  <what-built>The instrumented native capture/replay path, deterministic verifier, external-evidence procedure, and sanitized pending GATE-001 report/checklist.</what-built>
  <how-to-verify>Follow every numbered procedure step on the declared physical device; inspect explicit consent/local/upload/recovery UI; run both capture durations plus blackhole/pressure exercises; then perform and record separate physical terminations after `selected`, `image_and_metadata_durable`, `journaled`, `network_eligible`, and `server_acknowledged`. For each state verify the expected recovered prefix and zero earlier-record corruption, retain raw artifacts externally, enter only the schema-allowed sanitized measurements/opaque IDs/digests, validate that the observation document contains the exact five-state set, set `REROOM_GATE_001_OBSERVATIONS_PATH` to that external sanitized document without persisting the path, sign the checklist decision payload including its digest, and run the automated gate command.</how-to-verify>
  <acceptance_criteria>A real human-authored report/checklist pair binds the exact successful preflight, build, fixture, evaluator, environment, external raw-artifact digests, 10s/60s measurements, the schema-valid complete five-state physical termination document, and either GREEN with every threshold met or RED with the exact miss; omission of any one state cannot produce GREEN.</acceptance_criteria>
  <verify><automated>scripts/verify-phase-02-capture-replay gate</automated></verify>
  <done>GATE-001 is human-signed GREEN from real evidence, or remains exact RED/pending and correctly blocks live integration; no synthetic substitute is accepted.</done>
  <resume-signal>Type `approved` only after the gate command passes for a human-signed GREEN pair; otherwise provide the RED/pending result and evidence gap.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `verify-phase-02-capture-replay` quick/full/gate entry point and independent gate verifier.
- Closed five-state physical-observation schema, versioned operator/evaluator procedure, and sanitized automated preflight.
- Human-authoritative GATE-001 report/checklist or an explicit RED/pending blocker with exact evidence gap.

<verification>
Run Task 1 tests, full mode, and—only after real human/device evidence—gate mode; verify all evidence schemas/digests independently; run dependency/source integrity, release-surface, tracked-secret, privacy-path, GSD consistency, and `git diff --check` checks.
</verification>

<success_criteria>
Automation proves every host/simulator invariant, and the project neither advances live integration nor claims Phase 2 complete until real GATE-001 device evidence is human-signed GREEN.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-07-SUMMARY.md` when done; record the human checkpoint outcome without copying raw evidence into Git.
</output>
