---
phase: 02-atomic-capture-and-exact-replay
plan: "05"
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - tools/javascript/src/replay.ts
  - tools/javascript/test/replay.test.mjs
  - tools/python/reroom_verify/replay.py
  - tools/python/tests/test_replay.py
  - tools/verify/compare_replay_reports.py
  - tools/verify/tests/test_replay_agreement.py
  - scripts/run-phase-02-replay-agreement
  - evidence/compatibility/replay-agreement.json
autonomous: true
requirements: [FR-B0-001, NFR-REPLAY-001, FR-CAPTURE-001]
must_haves:
  truths:
    - "Swift, TypeScript-on-exact-Node-22.22.3, and Python independently parse the immutable capture corpus and emit closed ReplayReportV1 bytes from their own computations."
    - "All three runtimes verify raw inventory, event and packet digests, exact projections, final sequence, manifest self-hash, and journal-tuple input digest; no runner reads another runtime's result as authority."
    - "Two runs per runtime and the agreement publisher are byte-identical, complete, sorted by stable case ID, and bound to one exact implementation revision/source-tree digest plus immutable fixture/schema hashes."
    - "Every adjacency, empty, ordering, precision, concurrency, corruption, and completion-order case has the same verdict/rejection class/report artifact in all runtimes."
    - "Mutation gates independently kill omitted case, stale result, source/fixture drift, wrong runtime identity, wrong report digest, mixed generation, semantic corruption, and oracle-integrity corruption."
  prohibitions:
    - requirement_id: FR-B0-001
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT obtain cross-runtime agreement by copying expected artifacts, normalizing away a mismatch, or consuming another runner's output as replay authority."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
  artifacts:
    - path: tools/javascript/src/replay.ts
      provides: "Independent dependency-free TypeScript replay runner"
    - path: tools/python/reroom_verify/replay.py
      provides: "Independent Python replay runner"
    - path: evidence/compatibility/replay-agreement.json
      provides: "Atomic three-runtime exact replay agreement evidence"
  key_links:
    - from: scripts/run-phase-02-replay-agreement
      to: ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift
      via: "Build and invoke the named ReRoomReplayRunner product with the frozen manifest and a fresh exclusive Swift output root"
      pattern: "ReRoomReplayRunner|--manifest|--output-root"
    - from: scripts/run-phase-02-replay-agreement
      to: tools/verify/compare_replay_reports.py
      via: "Fresh isolated runtime outputs and closed complete comparator"
      pattern: "replay-agreement|compare_replay_reports"
    - from: tools/javascript/src/replay.ts
      to: fixtures/capture/1.0.0/rev-001/manifest.json
      via: "Exact Node runtime independently verifies immutable fixture bytes"
      pattern: "22.22.3|fixture"
---

<objective>
Prove exact replay and queue-order invariants independently in Swift, TypeScript, and Python and publish one atomic agreement report.

Purpose: Prevent a single-runtime implementation bug from defining the expected replay result or digest semantics.
Output: Independent JavaScript/Python replay runners, a fail-closed comparator/publisher, mutation gates, and revision-bound agreement evidence.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-01-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-03-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-04-SUMMARY.md
@fixtures/capture/1.0.0/rev-001/manifest.json
@fixtures/replay-report.schema.json
@tools/javascript/src/canonical-json.mjs
@tools/python/reroom_verify/canonical_json.py
@scripts/run-three-runtime-agreement
@evidence/compatibility/three-runtime-agreement-generation.json

<interfaces>
Each runner accepts the fixture manifest path plus an exclusive nonexistent output root and emits exactly one `<case_id>.replay-report.json` JCS `ReplayReportV1` per complete sorted manifest case. The Swift entry point is the Plan 02-03 package product `ReRoomReplayRunner`; the publisher builds that exact selected manifest/product, resolves its binary from the package build output, and invokes `ReRoomReplayRunner --manifest <manifest> --output-root <fresh-root>/swift --repo-root <root> --implementation-revision git:<40-lowercase-hex>`. `compare_replay_reports.py` accepts three fresh report directories and the immutable manifest, verifies each report independently, and returns one normalized case comparison with stable disagreement classes. The publisher rejects any untracked/ignored/symlinked/execution-eligible source and publishes evidence through a durable all-files generation transaction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement independent exact replay under pinned Node</name>
  <files>tools/javascript/src/replay.ts, tools/javascript/test/replay.test.mjs</files>
  <read_first>tools/javascript/package.json, tools/javascript/src/canonical-json.mjs, tools/javascript/src/loader.mjs, tools/javascript/src/wire-frame.mjs, fixtures/capture/1.0.0/rev-001/manifest.json, fixtures/replay-report.schema.json, docs/contracts/rrcap-manifest.schema.json</read_first>
  <behavior>
    - RED: direct Node replay tests fail because the runner does not exist.
    - Exact Node v22.22.3 runs erasable TypeScript directly with no transpiler/package and rejects any other runtime identity.
    - Runner performs bounded duplicate-aware Unicode-strict JSON parsing, safe archive paths, exact raw/JCS hashes, contiguous journal/projections, finalization, and report self-digest independently.
    - Positive, empty-valid, recovered-prefix, concurrent-repeat, order, adjacency, precision, and mutation cases match the immutable expected artifacts exactly.
  </behavior>
  <action>Write `node:test` cases first and prove RED. Implement erasable TypeScript compatible with exact Node 22.22.3 built-in stripping, importing only existing audited local helpers and Node standard modules. Parse untrusted JSON before object materialization with existing bounded duplicate/Unicode rules, verify every file/record/projection/digest independently, and emit sorted JCS ReplayReportV1 bytes to a newly created output. Repeated/concurrent tests must use isolated roots and compare bytes. Do not update package manifests/locks, install a compiler, call Swift/Python, or derive expected output from fixture expected files except for final equality.</action>
  <acceptance_criteria>Exact Node tests pass every frozen case and mutation, two isolated runs are byte-identical, wrong Node fails before output creation, and `package.json`/lockfiles remain unchanged.</acceptance_criteria>
  <verify><automated>node --test tools/javascript/test/replay.test.mjs</automated></verify>
  <done>Node independently reproduces the canonical replay verdict/report corpus under the exact approved runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement independent exact replay in Python</name>
  <files>tools/python/reroom_verify/replay.py, tools/python/tests/test_replay.py</files>
  <read_first>tools/python/reroom_verify/canonical_json.py, tools/python/reroom_verify/loader.py, tools/python/reroom_verify/wire_frame.py, tools/python/requirements.lock, fixtures/capture/1.0.0/rev-001/manifest.json, fixtures/replay-report.schema.json, docs/contracts/rrcap-manifest.schema.json</read_first>
  <behavior>
    - RED: Python replay tests fail before the module exists.
    - Strict UTF-8/duplicate-name parsing, safe paths, raw/JCS hashes, journal/projection/finalization validation, and report encoding are computed independently.
    - Positive, empty-valid, recovered-prefix, concurrency/order/adjacency/precision, and mutation cases match immutable artifacts and stable rejection classes.
    - Fresh outputs never consume Node/Swift results and never overwrite pre-existing paths.
  </behavior>
  <action>Add failing `unittest` cases, then implement the runner with the existing exact locked Python environment and standard/local helpers. Preserve bounded reads, duplicate/lone-surrogate rejection, exact RR-JCS member scopes, journal-only order, complete projections, and closed report bytes. Keep actual output metadata content-derived; use expected artifacts only as read-only equality oracles after independent computation. Add concurrent isolated executions without sleeps or shared output state.</action>
  <acceptance_criteria>Python passes all frozen positive/negative cases, two sequential/concurrent runs are byte-identical, stale/pre-existing output rejects, and requirements locks remain byte-unchanged.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.python.tests.test_replay -v</automated></verify>
  <done>Python independently reproduces the canonical replay verdict/report corpus without another runtime's output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish complete three-runtime replay agreement atomically</name>
  <files>tools/verify/compare_replay_reports.py, tools/verify/tests/test_replay_agreement.py, scripts/run-phase-02-replay-agreement, evidence/compatibility/replay-agreement.json</files>
  <read_first>scripts/run-three-runtime-agreement, tools/verify/compare_results.py, tools/verify/tests/test_three_runtime_agreement.py, evidence/compatibility/three-runtime-agreement-generation.json, fixtures/replay-report.schema.json, ios/Packages/ReRoomContracts/Package.swift, ios/Packages/ReRoomContracts/Sources/ReRoomReplayRunner/main.swift, ios/Packages/ReRoomContracts/Tests/ReRoomReplayRunnerTests/ReplayRunnerTests.swift</read_first>
  <behavior>
    - RED: comparator/publisher tests fail before replay agreement tooling exists.
    - Publisher runs actual Swift, Node, and Python replay entry points into exclusive fresh roots and requires exact complete case-set/order/verdict/rejection/artifact agreement.
    - Evidence binds one implementation revision/source-tree digest, exact selected Swift manifest, exact Node/Python/Swift identities, fixture/schema/evaluator/publisher hashes, raw-result digests, and zero disagreement metrics.
    - Faults at every publication boundary and restart recovery expose either the complete old generation or complete new generation, never a mixed accepted set.
  </behavior>
  <action>Write comparator, source-drift, runtime-drift, omission, semantic mutation, fixture-integrity, publication-fault, and restart tests first. Build the publisher using the hardened Phase 1 source-closure/runtime/generation transaction patterns: close every execution-eligible Swift manifest/source and Node/Python source, verify bindings before and after runtime work, build the exact `ReRoomReplayRunner` product from the selected `ios/Packages/ReRoomContracts/Package.swift`, resolve its package build binary, and invoke the exact interface-block CLI into a newly exclusive Swift root beside fresh Node/Python roots. Require all three directories to contain exactly the manifest's sorted `<case_id>.replay-report.json` set before comparison, then compare each closed report and durably publish one agreement generation. Tests must prove a missing Swift target/binary, mismatched manifest, pre-existing Swift root, omitted/extra Swift report, or runner nonzero exit prevents evidence replacement. Evidence may record only sanitized facts and raw-output digests, not room bytes, temp paths, device IDs, or private traces.</action>
  <acceptance_criteria>Publisher and focused mutations pass; two ordinary publications preserve exact evidence bytes; each semantic/integrity/runtime/source/omission/mixed-generation fault fails closed without replacing prior evidence; all cases report zero disagreement.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.verify.tests.test_replay_agreement -v</automated></verify>
  <done>One reproducible atomic evidence file proves complete independent Swift/Node/Python agreement for every Phase 2 replay case.</done>
</task>

</tasks>

## Artifacts this phase produces

- Dependency-free exact-Node `replay.ts` and independent Python `replay.py` runners.
- `compare_replay_reports.py` complete-case comparator and hardened publisher.
- `evidence/compatibility/replay-agreement.json` revision-bound zero-disagreement evidence.

<verification>
Run all three task commands, `scripts/run-phase-02-replay-agreement` twice, verify evidence byte stability and source/fixture integrity, confirm all dependency manifests/locks are unchanged, run tracked-secret scanning and `git diff --check`.
</verification>

<success_criteria>
All three runtimes independently agree on every exact replay case and publish one atomic, complete, reproducible, non-private evidence generation.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-05-SUMMARY.md` when done.
</output>
