---
phase: 02-atomic-capture-and-exact-replay
plan: "04"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FrameSelectionPolicy.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureAdmission.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/BoundedLatestQueue.swift
  - ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureTransport.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/FrameSelectionTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureAdmissionTests.swift
  - ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/BoundedQueueTests.swift
autonomous: true
requirements: [NFR-REPLAY-001, FR-CAPTURE-001]
must_haves:
  truths:
    - "The deterministic selector always accepts an initial explicit user-event candidate and otherwise uses injected cadence, view-change, and quality inputs with stable reasons; no model controls eligibility, and a selection decision does not start the selected lifecycle until bounded admission succeeds."
    - "A synchronous nonblocking pre-durability gate admits at most the injected ordinary capacity plus one reserved user-event candidate across queued and writer-in-flight work; rejected offers create no frame_selected event, archive byte, actor call, or task."
    - "The one reserved user-event lane is single-flight: an admitted explicit candidate is never replaced, a repeat offer rejects as user_event_busy until terminal completion, and ordinary work can neither consume nor evict that lane."
    - "Only immutable NetworkEligibleReceipt values enter transmission or optional-compute queues; selection/durability is complete before queue admission."
    - "Every queue has a configured positive capacity, never exceeds it under concurrency/stress, favors newest useful work within explicit priority rules, and records stable depth/drop/pause/completion metrics."
    - "Optional compute drops first, upload pauses second, and cadence/quality reduction is requested third; none of those decisions deletes, rewrites, or reorders a durable archive record."
    - "Reversed, duplicated, delayed, cancelled, or blackholed completion changes live metrics only; durable journal sequence and replay order remain identical."
    - statement: "[FLAGGED ASSUMPTION — NFR-REPLAY-001 unclassified edge] Queue capacities, cadence, view-change/quality cutoffs, storage warning levels, and pressure thresholds are injected HYPOTHESIS/TARGET values until GATE-001 records reproducible physical measurements; no numeric default is treated as MEASURED."
      verification: backstop
  prohibitions:
    - requirement_id: NFR-REPLAY-001
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT let newest-useful dropping, cancellation, upload pause, or completion order delete an accepted durable packet or redefine authoritative replay order."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
  artifacts:
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FrameSelectionPolicy.swift
      provides: "Deterministic measured-input frame selection policy"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureAdmission.swift
      provides: "Synchronous bounded pre-durability admission and one-consumer handoff"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/BoundedLatestQueue.swift
      provides: "Capacity-bounded newest-useful queue and metrics"
    - path: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureTransport.swift
      provides: "Typed echo/ack and pressure-policy adapter with no live network"
  key_links:
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FrameSelectionPolicy.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureAdmission.swift
      via: "A deterministic useful-frame decision produces a pre-lifecycle CaptureAdmissionCandidate; only an admitted candidate may become SelectedFrameCandidate"
      pattern: "CaptureAdmissionCandidate|CaptureAdmissionDisposition|admitted"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureAdmission.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift
      via: "One consumer emits admitted pre-lifecycle candidates sequentially through the typed writer closure; Plan 02-06 alone binds that closure to publishSelectedFrame"
      pattern: "runConsumer|AdmittedCaptureCandidate|SelectedFrameCandidate"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureTransport.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift
      via: "Queue input is NetworkEligibleReceipt and echo output is GatewayAcknowledgement"
      pattern: "NetworkEligibleReceipt|GatewayAcknowledgement"
    - from: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/BoundedLatestQueue.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FrameSelectionPolicy.swift
      via: "Explicit priority/reason policy, never a learned score"
      pattern: "userEvent|keyframe|cadence|viewNovelty"
---

<objective>
Implement deterministic frame selection, bounded pre-durability admission, bounded newest-useful post-durability work, pressure degradation, and a typed offline gateway echo.

Purpose: Prove high-rate callbacks and live completion behavior cannot create an implicit task/actor-mailbox queue, backpressure capture, or redefine durable replay order.
Output: Pure selector, synchronous admission gate, one-consumer handoff, bounded receipt queues, pressure/metrics snapshots, fake acknowledgement adapter, and stress/reordering tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-01-SUMMARY.md
@docs/adr/ADR-004-atomic-capture-and-record-first-replay.md
@docs/canonical/MASTER_TECHNICAL_SPEC.md
@docs/canonical/TEST_AND_EVALUATION_PLAN.md

<interfaces>
Consume `SelectedFrameCandidate`, `NetworkEligibleReceipt`, `GatewayAcknowledgement`, `FrameSelectionPolicy`, `CapturePressurePolicy`, and `QueueMetricsSnapshot` from Plan 02-01. The selector is a pure value operation whose accepted result is still pre-lifecycle. Add `CaptureAdmissionCandidate`, `AdmittedCaptureCandidate`, `CaptureAdmissionDisposition`, `CaptureAdmissionCloseReason`, `CaptureAdmissionCloseMode`, `CaptureAdmissionSnapshot`, and `CaptureAdmissionGate`. `CaptureAdmissionGate.offer(_:)` is synchronous/nonblocking; it uses an `OSAllocatedUnfairLock`-protected fixed-capacity state, an injected positive ordinary capacity labeled HYPOTHESIS/TARGET, exactly one reserved user-event slot, a monotonic admission sequence, and a bounded wake-only `AsyncStream<Void>` with `bufferingNewest(1)`. Outstanding depth includes queued plus the single writer-in-flight candidate, so `maximumOutstanding <= ordinaryCapacity + 1`; lane occupancy is released only by terminal writer completion. `runConsumer` grants exactly one consumer lease, drains `AdmittedCaptureCandidate` values in admission-sequence order before awaiting another wake, and invokes an injected async writer closure sequentially. A second consumer, post-close offer, or invalid capacity fails deterministically. `close(reason:mode:)` supports exactly `drainThenClose` and `abortQueuedBeforeSelection`, rejects later offers, and reports the terminal result of every already-admitted candidate. No offer creates a `Task`, calls an actor, invokes user code while holding the lock, or writes bytes/events. `BoundedLatestQueue<Element>` remains the post-durability actor with `offer`, `next`, `complete`, `cancelAll`, and `snapshot`. `CaptureTransport` is an injected deterministic echo/blackhole/reorder adapter; it performs no socket/HTTP operation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select deterministically and bound admission before durability</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/FrameSelectionPolicy.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureAdmission.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/FrameSelectionTests.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/CaptureAdmissionTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift, docs/contracts/frame-packet.schema.json, .planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md, docs/canonical/RISK_AND_KILL_GATES.md, .agents/skills/swift-concurrency/references/actors.md, .agents/skills/swift-concurrency/references/tasks.md, .agents/skills/swift-testing-expert/references/async-testing-and-waiting.md</read_first>
  <behavior>
    - RED: user-event, cadence boundary, view-change boundary, quality rejection, bounded admission, reserved-lane, stalled-writer, close/cancellation, and deterministic-repeat tests fail before policy and gate exist.
    - An initial explicit user-event candidate is eligible regardless of ordinary cadence/view novelty; one admitted user-event remains outstanding until terminal completion, and a repeat rejects deterministically as user_event_busy without replacement.
    - Non-user candidates are accepted/rejected solely from injected cadence, view-change, and quality facts with the schema-owned selected reason.
    - Equal inputs produce equal decisions/reasons; exact boundary and adjacent values have explicit fixture expectations.
    - All numeric defaults are named HYPOTHESIS or TARGET in code/evidence and are injectable.
    - With the writer held at a deterministic filesystem-stall barrier, above-rate concurrent ordinary offers plus the reserved explicit offer complete synchronously, maximum outstanding depth never exceeds ordinaryCapacity + 1, and excess offers increment stable capacity/busy counters without reaching the writer hook.
    - Admission processes accepted candidates in monotonic admission-sequence order with one in flight; graceful close drains only the bounded admitted set, while cancellation/expiration/storage close explicitly terminates queued-not-started candidates before selected and exposes close reason/count/depth.
  </behavior>
  <action>Write exact selector and admission tests first and prove RED. Implement a Sendable pure selector with explicit policy identity and classification on each threshold. Preserve `user_event`, `keyframe`, `view_novelty`, and `cadence` reason meaning from CON-001, but do not construct/publish `SelectedFrameCandidate` or begin `frame_selected` until admission returns `.admitted`. Implement `CaptureAdmissionGate` with no new dependency: use `OSAllocatedUnfairLock` to protect a fixed-capacity ordinary ring, one reserved nonreplaceable user-event slot, the one-consumer lease, lifecycle/close state, bounded wake continuation, and exact metrics. Merge lanes by monotonic admission sequence so priority reserves capacity without inventing another replay order. Keep accepted candidates counted while queued or writer-in-flight and release their lane only after explicit writer completion. The single consumer drains `AdmittedCaptureCandidate` values sequentially through an injected closure; `offer` remains synchronous, never awaits, never creates a task/actor call, and never invokes the closure under the lock. Use a wake stream carrying no candidate payload with `bufferingNewest(1)` and drain available work before awaiting again so signaling itself cannot grow or lose queued work. Define `drainThenClose` for graceful stop/background: process the bounded admitted set and reject every later offer. Define `abortQueuedBeforeSelection` for cancellation/expiration/storage failure: allow the current synchronous archive transaction, if already entered, to return or throw atomically; do not start another; assign every queued-not-started candidate an explicit `cancelled_before_selection` or `storage_unavailable_before_selection` terminal result; then finish the stream. Emit offered/admitted/rejected-by-reason/queued/in-flight/outstanding/maximum-depth/completed/closed metrics. Apply pressure observations in the locked order: drop optional compute, pause upload, then request cadence/quality reduction; storage inability closes admission and delegates valid-prefix finalization to the writer owner. Use controlled confirmations/barriers rather than sleeps and fresh gates per test. Keep binary32 value handling exact where values cross the packet boundary; use no model, wall-clock randomness, hidden device SKU, mutable global, unbounded `AsyncStream`, or per-candidate task. Treat all capacities/thresholds as injected HYPOTHESIS/TARGET fixtures.</action>
  <acceptance_criteria>Parameterized tests prove user-event eligibility, exact threshold/max-adjacent behavior, deterministic reasons, repeatability, invalid/non-finite input rejection, and no provider/model dependency. Admission stress with a stalled writer proves synchronous offers, one consumer/in-flight write, total outstanding never above ordinaryCapacity + 1, exact ordinary-capacity/user-event-busy/closed reasons and counters, no writer invocation for rejected candidates, no replacement of an admitted explicit candidate, deterministic admission order, bounded close/drain/cancel behavior, and zero per-offer tasks or unbounded wake payloads.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter 'FrameSelectionTests|CaptureAdmissionTests'</automated></verify>
  <done>Selection is reproducible and pre-durability admission has a fixed observable bound before any selected lifecycle event, archive mutation, actor mailbox entry, or background task can exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound live work and prove completion-order independence</name>
  <files>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/BoundedLatestQueue.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureTransport.swift, ios/Packages/ReRoomContracts/Tests/ReRoomCaptureCoreTests/BoundedQueueTests.swift</files>
  <read_first>ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift, docs/adr/ADR-004-atomic-capture-and-record-first-replay.md, docs/canonical/TEST_AND_EVALUATION_PLAN.md, .agents/skills/swift-concurrency/references/actors.md, .agents/skills/swift-concurrency/references/tasks.md, .agents/skills/swift-testing-expert/references/async-testing-and-waiting.md</read_first>
  <behavior>
    - RED: capacity, stale replacement, priority, pressure ordering, cancellation, blackhole, and reversed-completion tests fail before queues exist.
    - Capacity zero rejects configuration; capacities one and greater never exceed their configured depth under concurrent producers.
    - User-event/keyframe receipts are never displaced by lower-priority cadence work; stale ordinary work is replaced by the newest useful candidate with an explicit drop reason/counter.
    - Pressure from pre-durability capacity rejection or post-durability live queues transitions optional-compute-drop → upload-paused → cadence/quality-reduction-requested and snapshots each state independently.
    - Echo/blackhole/reordered acknowledgement cases never create an acknowledgement without a receipt or change durable sequence order.
  </behavior>
  <action>Create async Swift Testing cases first without sleeps, then implement small actor queues over immutable receipts. Make cancellation/backpressure explicit and expose deterministic snapshots for offered/accepted/replaced/dropped/completed/cancelled counts, maximum depth, upload-paused state, and pressure reason. Feed pre-durability rejection/depth snapshots from `CaptureAdmissionGate` into the same `CapturePressurePolicy`, preserving the locked sequence of optional-compute drop, upload pause, then cadence/quality reduction; admission saturation never authorizes deletion of an already selected/durable packet. Use structured tasks or controlled continuations in tests, fresh actors/gates per argument, and no broad serialization. Add a fake echo that can deterministically delay/reorder/duplicate/blackhole candidate completions and returns typed acknowledgements for the writer to validate; do not introduce a live endpoint, URLSession, WebSocket, or package.</action>
  <acceptance_criteria>Stress tests submit above-rate concurrent inputs while proving maximum depth never exceeds capacity, stale counters increase, priority is preserved, blackhole causes no unbounded history, cancellation drains deterministically, reversed completion leaves the receipt journal-sequence set/order unchanged, and pressure states occur in the locked order.</acceptance_criteria>
  <verify><automated>swift test --package-path ios/Packages/ReRoomContracts --filter BoundedQueueTests</automated></verify>
  <done>Live work is bounded and observable, while durable accepted history is untouched by drops, pressure, cancellation, or completion order.</done>
</task>

</tasks>

## Artifacts this phase produces

- `FrameSelectionPolicy` deterministic selector with explicit reason and HYPOTHESIS/TARGET identity.
- `CaptureAdmissionGate` synchronous bounded pre-durability offer, reserved explicit lane, single consumer, and close/depth metrics.
- `BoundedLatestQueue` actors and `QueueMetricsSnapshot` stress evidence.
- `CaptureTransport` deterministic echo/blackhole/reorder test adapter and pressure state machine.

<verification>
Run both task commands and the complete Swift package suite; source-scan `CaptureAdmission.offer` for task creation/await/actor calls and the wake channel for an unbounded buffering policy; stress a deterministic stalled writer; scan the package for unbounded collection growth and live network/provider imports; verify every numeric policy value is labeled HYPOTHESIS/TARGET; run `git diff --check`.
</verification>

<success_criteria>
Pre-durability outstanding depth never exceeds ordinaryCapacity + 1 even while the writer is stalled, rejected offers begin no selected lifecycle, post-durability stress never exceeds configured capacity, drop/pressure metrics are exact, selected durable records remain intact, and replay order is provably independent of admission/live completion order.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-04-SUMMARY.md` when done.
</output>
