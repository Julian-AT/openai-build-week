---
phase: 02-atomic-capture-and-exact-replay
plan: "06"
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj/project.pbxproj
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift
  - ios/ReRoomDeviceProof/ReRoomDeviceProofUITests/DiagnosticSurfaceTests.swift
autonomous: true
requirements: [SEC-CONSENT-001, FR-CAPTURE-001, FR-B0-001, NFR-REPLAY-001]
must_haves:
  truths:
    - "From the ready camera, the user explicitly chooses Start room capture and only then sees disclosure; acceptance creates a new authorized session ID, while denial shows explanation and creates no archive bytes."
    - "During capture, Recording locally is persistent and upload/offline/share states remain visibly independent; upload pause says Capture continues locally — upload paused without implying data loss or sync completion."
    - "Stop explicitly finalizes; background/interruption requests bounded best-effort finalization, handles expiration by preserving the already-durable prefix, and always ends its background assertion."
    - "Next launch automatically discovers and verifies interrupted archives, never resumes them, and shows Recovered — capture may be incomplete with integrity summary plus Inspect replay and Start new capture actions."
    - "The inspector renders only verified ReplayReportV1 snapshots: archive verdict, finalized/recovered status, digest summary, and stable-ID timeline in authoritative global-journal order."
    - "ARFrame/image work stays on the native boundary, filesystem/replay work stays off MainActor, and the 60 Hz camera/render path never waits for archive, queue, provider, web, or network work."
  prohibitions:
    - requirement_id: SEC-CONSENT-001
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT present local durability as upload/share completion or hide an upload-paused/offline/recovered state behind a generic success label."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
    - requirement_id: FR-B0-001
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT let the inspector decode or expose an archive record that ReplayCore has not hash-verified and accepted."
      reason: "Flagged-unverified descriptor-less prohibition recall; no mechanical check descriptor is fabricated during planning."
  artifacts:
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
      provides: "Native ARFrame/lifecycle/background/storage adapter to ReRoomCaptureCore"
    - path: ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift
      provides: "Internal consent, capture state, recovery, and verified replay inspector"
  key_links:
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift
      via: "Sendable captured-frame snapshots and async actor calls outside ARKit/render callbacks"
      pattern: "CaptureArchiveStore|SelectedFrameCandidate"
    - from: ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift
      to: ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/ReplayReport.swift
      via: "View renders immutable verified report/timeline snapshots only"
      pattern: "ReplayReportV1|ReplayTimelineEntry"
---

<objective>
Connect the deterministic capture/replay core to the native seed with explicit consent, truthful capture/upload/recovery state, lifecycle finalization, and a verified internal inspector.

Purpose: Demonstrate the complete Phase 2 user-visible boundary without moving filesystem/replay/network work onto the camera/render thread or pulling forward Phase 7 web scope.
Output: Native session adapter, MainActor observable snapshots, accessible internal UI, background/recovery integration, and simulator/UI tests.
</objective>

<execution_context>
@/Users/julianschmidt/.codex/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-atomic-capture-and-exact-replay/02-CONTEXT.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-RESEARCH.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-02-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-03-SUMMARY.md
@.planning/phases/02-atomic-capture-and-exact-replay/02-04-SUMMARY.md
@ios/ReRoomDeviceProof/ReRoomDeviceProof/FramePacketBuilder.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift
@ios/ReRoomDeviceProof/ReRoomDeviceProof/App.swift

<interfaces>
Keep `DeviceProofModel` and `DiagnosticAppOwner` `@MainActor @Observable`. `CaptureSessionAdapter` owns no canonical mutable archive state; it converts one ARFrame into a Sendable snapshot through the existing `ARFrameCaptureAdapter`, evaluates the pure selector, and awaits `CaptureArchiveStore` outside the high-rate callback. The UI consumes immutable `CapturePresentationSnapshot`, `QueueMetricsSnapshot`, `RecoveredArchive`, and `ReplayReportV1` values. No task uses `Task.detached`; task entry isolation follows the synchronous-prefix rule and cancellation/background assertion lifetime is explicit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge ARFrame, app lifecycle, pressure, and recovery to the capture actor</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj/project.pbxproj, ios/ReRoomDeviceProof/ReRoomDeviceProof/CaptureSessionAdapter.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift</files>
  <read_first>ios/ReRoomDeviceProof/ReRoomDeviceProof/FramePacketBuilder.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/ARSessionController.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureSession.swift, ios/Packages/ReRoomContracts/Sources/ReRoomCaptureCore/CaptureArchiveStore.swift, .agents/skills/swift-concurrency/SKILL.md, .agents/skills/swift-concurrency/references/tasks.md</read_first>
  <behavior>
    - RED: injected camera, lifecycle, background-assertion, storage-pressure, archive, selector, and recovery drivers fail before the adapter exists.
    - Start creates a fresh session ID and explicit local-only authorization only after disclosure acceptance; denial initializes no writer/directory.
    - One ARFrame supplies the upright image/camera/time snapshot; high-rate callbacks never await filesystem/queue/replay work.
    - Stop finalizes; background/interruption attempts bounded finalization; expiration/cancellation ends cleanly at the current durable prefix and always releases the background assertion.
    - Storage write failure stops selection, finalizes the durable prefix, and surfaces a failure snapshot; launch discovery returns finalized/recovered/failure snapshots and never resumes an old archive.
  </behavior>
  <action>Write injected behavior tests first and prove RED. Add the local package product to the app/test target without editing dependency versions. Implement `CaptureSessionAdapter` around typed drivers for session IDs/time, archive root, UIApplication background assertions, lifecycle notifications, and storage availability. Convert ARKit/CoreImage objects to Sendable values on the native owner before crossing actor isolation; start off MainActor when the task's synchronous prefix has no UI work, check cancellation at natural boundaries, and return immutable presentation snapshots to MainActor. Background execution is best effort, never a durability guarantee. Stop/failure/recovery must preserve and label the verified prefix, never silently continue the archive.</action>
  <acceptance_criteria>Tests prove fresh session authorization, denial/no-write, ARFrame snapshot coherence, no high-rate await, explicit stop, background success/expiration/cancellation cleanup, storage failure finalization, automatic launch recovery, and no same-archive resume under Swift 6 concurrency checking.</acceptance_criteria>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/CaptureSessionAdapterTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The native boundary drives the deterministic actor without blocking ARKit/render work and truthfully preserves/finalizes/reports the durable prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present explicit consent, local/upload state, recovery, and verified replay</name>
  <files>ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofTests/CaptureSessionAdapterTests.swift, ios/ReRoomDeviceProof/ReRoomDeviceProofUITests/DiagnosticSurfaceTests.swift</files>
  <read_first>ios/ReRoomDeviceProof/ReRoomDeviceProof/DiagnosticChecklistView.swift, ios/ReRoomDeviceProof/ReRoomDeviceProof/DeviceProofModel.swift, .agents/skills/swiftui-expert-skill/SKILL.md, .agents/skills/swiftui-expert-skill/references/latest-apis.md, .agents/skills/swiftui-expert-skill/references/state-management.md, .agents/skills/swiftui-expert-skill/references/accessibility-patterns.md</read_first>
  <behavior>
    - RED: model/UI tests cannot find the required Start/disclosure/recording/upload/recovery/inspector states and accessibility identifiers.
    - Ready camera shows Start room capture; activation presents disclosure before any archive action; accept starts a new session and deny shows only non-capture explanation/retry.
    - Recording locally persists throughout capture; upload/offline/not-shared state is a separate row; paused upload uses the locked warning and Stop remains available.
    - Recovery card uses the locked incomplete label, integrity summary, Inspect replay, and Start new capture; inspector has verdict/status/digests then stable authoritative timeline.
    - Scrub/selection can reference only verified timeline items and invalid input produces precise diagnostic with no frame exposure.
  </behavior>
  <action>Add model and XCUITest expectations first, observe RED, then extend the internal diagnostic surface only. Use native `Button` controls and the current `confirmationDialog`/`alert` interfaces, `@State private` for view-owned values, immutable/injected values for read-only snapshots, built-in Dynamic Type text styles, stable timeline identity, and explicit accessibility labels/values/identifiers. Keep local durability, upload/offline, share, recovery, and archive-integrity rows independent. Exclude Liquid Glass, a polished product/web imitation, ordinary-video/session-sharing controls, raw archive reads in the view, and diagnostics in the independently protected Release root.</action>
  <acceptance_criteria>Unit/UI tests exercise accept/deny, per-session restart, recording indicator, upload pause/offline/not-shared separation, stop, recovered status/actions, verdict/digest/timeline ordering, scrub rejection, VoiceOver identifiers, Dynamic Type-safe layout, and unchanged Release diagnostic exclusion.</acceptance_criteria>
  <verify><automated>xcodebuild test -project ios/ReRoomDeviceProof/ReRoomDeviceProof.xcodeproj -scheme ReRoomDeviceProof -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ReRoomDeviceProofTests/CaptureSessionAdapterTests -only-testing:ReRoomDeviceProofUITests/DiagnosticSurfaceTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The internal native proof surface makes consent, local recording, upload/share, recovery, integrity, and verified replay unmistakably distinct and accessible.</done>
</task>

</tasks>

## Artifacts this phase produces

- `CaptureSessionAdapter` native ARFrame/app-lifecycle bridge with explicit cancellation/background cleanup.
- `CapturePresentationSnapshot` state delivered to the existing MainActor observable owner.
- Internal consent/capture/recovery/replay inspector and simulator/UI regression coverage.

<verification>
Run both task commands and the complete Debug unit/UI suite; rebuild/inspect the same Release product with the existing release-surface verifier; scan for MainActor file/hash/replay loops, live provider/network waits, deprecated SwiftUI APIs, raw/private output, and `git diff --check`.
</verification>

<success_criteria>
Users explicitly consent before any session bytes, always see truthful local/upload/recovery state, can inspect only verified replay input, and the camera/render path never waits for background work.
</success_criteria>

<output>
Create `.planning/phases/02-atomic-capture-and-exact-replay/02-06-SUMMARY.md` when done.
</output>
