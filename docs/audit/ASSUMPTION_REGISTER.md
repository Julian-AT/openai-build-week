# Assumption Register

Status: active pre-implementation assumptions  
Date: 2026-07-14

Confidence describes current evidence, not importance. Every assumption has a validation deadline and an automatic fallback; none silently changes a human-locked decision.

| ID | Statement | Reason | Confidence | Validation method | Deadline / gate | Fallback |
|---|---|---|---|---|---|---|
| ASM-001 | The controlled room and freestanding armchair or small side table can be staged with visible floor, simple background, stable light, and an unobstructed half-circle path. | Human confirms a stageable hero scene, but visual evidence is not yet recorded. | High | Photograph/checklist and dry walk with marked viewpoints. | Before fixture capture / GATE-013 | Choose the easier allowed target/position; retain B0 recording and report the unsupported live path if no locked-compliant fixture exists. |
| ASM-002 | Non-LiDAR ARKit tracking, plane detection, and raycasts are adequate in that room for the active session. | Official APIs support these features, but scene texture/light determine quality. | Medium | Physical-device tracking/plane/raycast run with known markers. | First device slice / GATE-002 and GATE-003 | Improve lighting/coverage, restart world frame, narrow view envelope, or stop new live edits and preserve `.rrcap`. |
| ASM-003 | The phone can durably encode/write selected frames while rendering for four minutes without storage, memory, or thermal failure. | Work is off the render thread but unmeasured on the actual device. | Medium | 60-second crash/recovery fixture and four-minute load run with FPS, memory, thermal, disk metrics. | Capture/runtime slices / GATE-001 and GATE-012 | Lower selected-frame rate/resolution/keyframe count and pause upload before sacrificing durability. |
| ASM-004 | RealityKit can render the required reveal/occluder/asset order at acceptable quality and frame rate. | APIs exist; this exact diminished-reality composition is not guaranteed by documentation. | Medium-low | Eight-pose canned artifact test and four-minute device run. | First compositor slice / GATE-003 | Use the simplest validated RealityKit composite; use Metal only if its bounded prototype passes; otherwise Mode A remains unready. |
| ASM-005 | The hero capture exposes enough floor/wall evidence to build a credible reveal within a bounded view envelope. | A static object may hide most of the needed background. | Medium-low | At least eight view projections, provenance/synthesized-fraction analysis, foreground-overpaint test, blinded visual vote. | Reveal slice / GATE-006 | Request views, shrink envelope, choose easier allowed target, or keep remove unavailable; hero failure blocks P0. |
| ASM-006 | SAM 2.1 Hiera Small provides adequate one-target masks and identity for the fixture. | It is small/public/mature but not yet measured on ReRoom imagery. | Medium | Compare shared annotated frames/replay with accessible SAM 3.1 and manual reference masks. | Before semantics integration / GATE-004 | Upgrade only if SAM 3.1 wins and access/runtime/license pass; otherwise explicit reseed/frozen validated masks. |
| ASM-007 | Three-to-five calibrated masks with useful baseline produce a conservative volume/OBB/support relation adequate for readiness checks. | Visual hulls are underconstrained and concavities are not recovered. | Medium | Projection leakage, support error, OBB variation, and view-envelope tests. | Fast-geometry slice / GATE-005 | Request additional views, dilate conservatively, narrow envelope, or keep replace/remove unavailable. |
| ASM-008 | Place, target, replacement, removal gating, restore, and provider-independent B0 can be implemented without dense TSDF. | Architecture intentionally separates dense enhancement from core behavior. | High | Execute core fixtures with every dense provider disabled. | Before dense work / GATE-005, GATE-008, GATE-009 | If a specific capability proves to require dense geometry, keep that capability unavailable and record a new ADR; do not silently add TSDF to all P0 paths. |
| ASM-009 | At least one available hardware tier can run the selected live semantic profile without OOM or queue growth. | No specific GPU is locked and provider VRAM is unmeasured. | Medium | Four-minute selected-profile replay with p50/p95 latency, VRAM, and queue telemetry. | Runtime selection / GATE-012 | Choose smaller provider, unload/serialize models, lower rate/resolution, and disable dense work. |
| ASM-010 | A small catalog can supply enough licensed, dimensionally suitable USDZ/GLB pairs to demonstrate candidate choice and rejection. | Final assets were not selected in the archived inputs. | High | License/source review, hash, dimensions, derivative parity, collision, cover, device load. | Before transaction integration / GATE-011 | Exclude failed assets and use the smallest compliant set; no runtime marketplace conversion. |
| ASM-011 | Project access permits server-side ephemeral Realtime credentials and GPT-5.6 Sol Responses calls during the demo. | APIs are documented, but account/project availability and network are external state. | Medium | Non-secret health check and fixed intent/tool fixture. | Voice slice / GATE-010 | Typed intent through the same deterministic service; no credential embedded in client. |
| ASM-012 | A Next.js client can import/replay the golden `.rrcap` and inspect sparse/cached artifacts without a learned provider. | Browser replay is conventional, but archive/media size and codec behavior are unmeasured. | High | Disable all learned providers and run the complete B0 fixture twice. | B0 baseline slice / GATE-008 | Reduce fixture/artifact size, use server-side unpacking in gateway, retain exact timeline/error inspection. |
| ASM-013 | P0 has one active editor, so no concurrent scene merge algorithm is required. | Human/demo scope is single-user; retries/reconnect still need idempotency. | High | Contract test rejects concurrent stale mutation and reconciles one client. | Transaction slice / GATE-009 | Stop mutations and reload canonical snapshot on conflict; multi-editor work is deferred. |
| ASM-014 | Current Xcode, signing, Developer Mode, and physical-device installation remain usable throughout the week. | Human confirmed readiness, but certificates/build state can expire or drift. | High | Install/launch minimal signed shell and recheck before each recorded session. | Before device work and daily / GATE-013 | Repair signing within the bounded preflight; preserve B0/replay work, but native release remains blocked if installation cannot be restored. |
| ASM-015 | Ordinary MP4/MOV inputs can at least be normalized, stored, and replayed with an explicit geometry-unavailable state. | Learned trajectory/geometry is optional, but media decode still depends on supported codecs. | Medium-high | Upload short supported-codec fixtures, verify duration/frame order/status and unsupported-codec error. | B0 baseline / GATE-008 | Publish supported codec/container limits; reject unsupported media without corrupting the session. |
| ASM-016 | SQLite WAL plus content-addressed filesystem storage is sufficient for one active session/demo workload. | Scope has no multi-host or high-concurrency requirement. | High | Transaction/replay concurrency and crash-recovery fixture. | Gateway integration / GATE-009 and GATE-012 | Serialize writes and repair local storage; add no datastore without measured failure and superseding ADR. |

## Closure rule

When validated, record the evidence in the research ledger/test record and mark the assumption closed without deleting its ID. When disproved, activate the listed fallback and update the affected ADR, risk gate, requirement, and contract together.
